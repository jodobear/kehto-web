---
context: default
phase: v1.30-milestone-initialization
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-07-31T02:39:54.003Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Nested Claude worktrees contaminate repository-wide tests — clean worktrees under `.claude/worktrees/` were scanned as active package consumers and caused false package-alignment failures — temporarily relocate them outside the repository scan root for local full/focused verification, restore them afterward, or fix the scanner exclusion in a separately justified change.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Implicit GitHub repository selection | `gh` inferred read-only `kehto/web` via `upstream` instead of writable `jodobear/kehto-web`, blocking PR creation | advisory | Use explicit `-R jodobear/kehto-web` for PR, issue, review, and merge operations on this checkout |
| Nested worktree test contamination | Tests recursively scanned `.claude/worktrees/*` and treated stale package snapshots as active source | blocking | Verify nested worktrees are outside scan root during local gates; restore them after each temporary relocation |

<current_state>
Branch `milestone/v1.30-visual-recovery` is clean and based on merged v1.29 archive commit `23e170cc`. `/gsd-new-milestone` was invoked with complete v1.30 and v1.31 sequencing instructions. The workflow loaded current PROJECT/MILESTONES/STATE context but paused before writing any v1.30 artifacts.
</current_state>

<completed_work>

Completed Tasks:
- Merged and archived v1.29 through PR #1 — Done
- Ran repeated Codex review/repair loops and merged with green CI — Done
- Compared Blossom, visual recovery, and issue #187 — Done
- Locked sequence: v1.30 Visual Recovery, then v1.31 Runtime Conformance Suite — Done
- Created `milestone/v1.30-visual-recovery` from current `main` — Done
- Loaded `/gsd-new-milestone` context — Done
</completed_work>

<remaining_work>

- Complete `/gsd-new-milestone` for v1.30:
  - Auto-confirm the supplied milestone summary; no additional user questioning is needed.
  - Skip project-level ecosystem research because the archived Phase 105 UI review is the authority.
  - Update PROJECT.md and switch STATE.md to v1.30 using `state.milestone-switch`.
  - Create REQUIREMENTS.md from the locked outcomes.
  - Create roadmap continuing phase numbering at Phase 107.
- Run `/gsd-ui-phase` for the first UI phase, then plan and execute autonomously.
- Ship v1.30 with full gates, PR, external Codex review loop, merge, and archive.
- Start v1.31 for kehto/web#187; re-fetch and record all current upstream refs before planning or coding.
</remaining_work>

<decisions_made>

- v1.30 Visual Recovery first because it has the best return/time ratio and closes explicit 12/24 product debt.
- v1.31 issue #187 second because it has the highest strategic return but needs a large engine + CLI + browser UI milestone.
- Blossom is already shipped; no Blossom milestone without a concrete new acceptance criterion.
- v1.30 must not change NAP behavior.
- v1.31 must update moved upstream pins and stale issue assumptions without asking permission.
- Use explicit `-R jodobear/kehto-web` for GitHub operations.
</decisions_made>

<blockers>
- None requiring human action.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable resume state and exact next action.
2. `.planning/milestones/v1.29-phases/105-published-convention-adoption-and-host-flows/105-UI-REVIEW.md` — authoritative 12/24 visual debt and required fixes.
3. `.planning/PROJECT.md` — project constraints and current product state.
4. `.planning/MILESTONES.md` — shipped v1.29 context.
5. `packages/paja/src/host-page.ts` — Paja layout, typography, colors, mobile breakpoint.
6. `packages/paja/src/browser-target-frame.ts` — raw target-load failure surface.
7. `apps/playground/napplets/feed/src/main.ts` and `apps/playground/napplets/profile-viewer/src/main.ts` — denial/unavailable states.
8. `https://github.com/kehto/web/issues/187` — v1.31 outcome; treat pins and protocol claims as stale until revalidated.

## Critical Anti-Patterns (do NOT repeat these)
- Do not expand v1.30 into protocol behavior changes; it is UX/accessibility repair.
- Do not treat issue #187's pinned `@napplet/nap@0.28.0` or `supports(domain, protocol?)` wording as current authority.
- Do not call Blossom unfinished; its upload rail, Paja integration, consent, BUD-03 warming, and deterministic E2E are shipped.
- Do not run recursive package-alignment tests with nested `.claude/worktrees` inside the repository scan root.

## Infrastructure State
- Branch: `milestone/v1.30-visual-recovery`
- Working tree before handoff files: clean
- GitHub writable repo: `jodobear/kehto-web`; upstream `kehto/web` is read-only for current token
- Background monitors/jobs: none
- v1.29 merged PR: `https://github.com/jodobear/kehto-web/pull/1`

## Pre-Execution Critique Required
- Design artifact: create via `/gsd-ui-phase` after roadmap initialization.
- Critique focus: mobile information hierarchy, recoverable-state accessibility, semantic token reuse, first-viewport utility, no protocol behavior drift.
- Gate: Do not begin UI implementation until UI-SPEC validation passes.

<context>
The user explicitly authorized uninterrupted autonomous progression through v1.30 and then v1.31, including upstream pin refreshes, contextual issues, external Codex review loops, PR creation, and merges. Ask only when a genuinely human-only decision or action is required.
</context>

<next_action>
Start with: `/gsd-resume-work`, then continue the interrupted `/gsd-new-milestone` workflow for v1.30 using the locked scope above.
</next_action>
