---
phase: 101-baseline-writer-contribution-preflight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md
  - .planning/phases/101-baseline-writer-contribution-preflight/101-WRITER-CONTRIBUTION-PLAN.md
autonomous: false
requirements: [PRE-01, PRE-02]
user_setup:
  - service: canonical Writer upstream
    why: "A human must provide and approve the canonical Writer URL, default branch, and baseline SHA before Phase 104 may create an isolated Writer worktree."

must_haves:
  truths:
    - "A maintainer can inspect one timestamped audit to identify the current kehto/web main SHA, the full incoming range from historical d4ba157dfb14876f878cb9055da3d17150d0b01d, affected Paja paths, and a read-only Writer snapshot."
    - "Writer's concurrent branch, history, porcelain status, staged and unstaged paths, untracked paths, remotes, and worktrees are recorded as observations rather than altered state."
    - "The contribution plan makes canonical Writer URL, default branch, verified baseline SHA, and exact completed Paja commit or artifact blocking fields; it cannot be mistaken for authorization to edit Writer."
    - "The contribution plan specifies only standard NAP-IDENTITY, NAP-OUTBOX, NAP-RESOURCE, and NAP-UPLOAD behavior for Writer and distinguishes each pinned conformance determination from the NAP-BLOSSOM draft-spec gap."
    - "Phase 101 ends with Writer authorization BLOCKED/PENDING; only the Phase 104 gate may record approve Writer implementation after the canonical Writer base is refreshed and the exact Phase 102/103 Paja commit or artifact exists."
  artifacts:
    - path: .planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md
      provides: "Timestamped Kehto upstream/Paja-impact and read-only Writer evidence for PRE-01."
    - path: .planning/phases/101-baseline-writer-contribution-preflight/101-WRITER-CONTRIBUTION-PLAN.md
      provides: "Approval-gated Phase 104 contribution scope, protocol authority matrix, setup template, checks, and exclusions for PRE-02."
  key_links:
    - from: "101-BASELINE-AUDIT.md"
      to: "kehto/web upstream/main and /workspace/projects/writer Git evidence"
      via: "UTC-stamped command/result table with the actual refreshed SHA and Writer observation timestamps."
    - from: "101-WRITER-CONTRIBUTION-PLAN.md"
      to: "Phase 104"
      via: "Blocking canonical-base/Paja-dependency fields, Phase 101 BLOCKED/PENDING status, and a later Phase 104 approval gate before the worktree template."
    - from: "Writer protocol matrix"
      to: "pinned napplet/naps references and NIP-01"
      via: "A row per service marks conformant usage, upstream drift, or an intentional draft-spec/product-policy gap."
  prohibitions:
    - requirement_id: PRE-01
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT normalize, absorb, discard, or otherwise alter unrelated Writer shortcut WIP while collecting milestone evidence."
      reason: "Writer is concurrently mutable local work whose preservation is a prerequisite for safe integration."
    - requirement_id: PRE-02
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT create a Writer remote, ref, worktree, integration branch, index change, or source edit before explicit human approval."
      reason: "The canonical Writer base and Paja dependency are intentionally unresolved and user approval is the authorization boundary."
    - requirement_id: PRE-02
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT replace shell-mediated identity, relay, resource, upload, consent, signing, or network authority with a Writer-specific path."
      reason: "The milestone's value and security boundary require standard NAP services only."
  flagged_assumptions:
    - requirement_id: PRE-01
      category: unclassified
      status: unresolved
      probe: "unclassified — review manually"
      handling: "Treat every Writer and upstream value as timestamped evidence; the audit reviewer compares command transcript and snapshots rather than inferring stability from earlier research."
    - requirement_id: PRE-02
      category: empty
      status: unresolved
      probe: "What is the result for empty, single-element, or null input?"
      handling: "Canonical Writer URL, default branch, baseline SHA, and Paja implementation commit/artifact are fail-closed blocking fields: absent, blank, or unverifiable values stop at the human checkpoint."
    - requirement_id: PRE-02
      category: encoding
      status: unresolved
      probe: "Whose definition of length/equality applies — bytes, code points, grapheme clusters, or normalized form?"
      handling: "Compare Git object IDs and ref names exactly as Git emits them; record full 40-hex commit IDs and do not substitute abbreviated, normalized, or prose-equivalent identifiers."
    - requirement_id: PRE-02
      category: concurrency
      status: unresolved
      probe: "If interrupted or run in parallel, what is guaranteed?"
      handling: "The audit records before/after observation times and classifies a changed Writer snapshot as concurrent external activity unless the Phase 101 transcript shows a Writer-mutating command; no task may parallelize Writer mutation with the audit."
---

<objective>
Create the complete, approval-gated evidence path for the Phase 101 goal: maintainers can safely start Kehto/Paja and Writer work without losing unrelated Writer shortcut WIP.

Purpose: Establish a reviewable fact base and a Phase 104 plan without changing Kehto/Paja source or Writer state.
Output: `101-BASELINE-AUDIT.md` and `101-WRITER-CONTRIBUTION-PLAN.md`, followed by a non-authorizing BLOCKED/PENDING review checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md
@.planning/phases/101-baseline-writer-contribution-preflight/101-RESEARCH.md
@.planning/phases/101-baseline-writer-contribution-preflight/101-PATTERNS.md
@.planning/phases/101-baseline-writer-contribution-preflight/101-VALIDATION.md
</context>

## Artifacts this phase produces

1. `.planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md` — a UTC-timestamped, reproducible Kehto upstream/Paja-impact and Writer-preservation audit.
2. `.planning/phases/101-baseline-writer-contribution-preflight/101-WRITER-CONTRIBUTION-PLAN.md` — an approval-gated Phase 104 Writer contribution plan, not a grant to change Writer.

<flagged_assumptions>
The plan carries every unresolved row from `/tmp/phase101-coverage.json` without silently resolving it:

| Requirement | Category | Probe | Phase 101 disposition |
|---|---|---|---|
| PRE-01 | unclassified | unclassified — review manually | Flagged: audit facts are observations at their command timestamps; reviewer must inspect the transcript and timestamped evidence. |
| PRE-02 | empty | What is the result for empty, single-element, or null input? | Flagged: blank or missing canonical URL/default branch/SHA/Paja artifact is BLOCKED; no inferred value may unlock setup. |
| PRE-02 | encoding | Whose definition of length/equality applies — bytes, code points, grapheme clusters, or normalized form? | Flagged: Git uses exact full object IDs and ref spelling; record and compare the 40-hex IDs without abbreviation or text normalization. |
| PRE-02 | concurrency | If interrupted or run in parallel, what is guaranteed? | Flagged: Writer remains externally mutable; a changed snapshot is classified from timestamped evidence and command transcript, never repaired by this phase. |
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Produce a refreshed, read-only baseline audit for Kehto and Writer</name>
  <read_first>
    - .planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md (D-01, D-02, D-04 through D-07, D-09, and D-10)
    - .planning/phases/101-baseline-writer-contribution-preflight/101-RESEARCH.md (live upstream and mutable Writer evidence)
    - .planning/phases/101-baseline-writer-contribution-preflight/101-PATTERNS.md (audit layout and reproducibility commands)
    - AGENTS.md (dirty-state protocol and NAP contribution guardrails)
  </read_first>
  <files>.planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md</files>
  <action>Per D-01 and D-02, create only the bounded evidence artifact; do not change Kehto/Paja source or broaden the work. Per D-04, retain d4ba157dfb14876f878cb9055da3d17150d0b01d as the historical 2026-07-24 implementation baseline. Per D-05, inspect the local upstream remote URL; if it is absent, add `https://github.com/kehto/web.git`, and if it exists with a different URL stop for a human decision rather than rewriting it. Fetch `upstream` main in the Kehto checkout, record UTC time and the full fetched SHA, then inspect the actual range from the historical baseline to that refreshed SHA. Record commit subjects and a Paja-path impact table, including whether the known historical 50d5787b5bd6a75479f72654de98cdfcaa902f50 / target-CORS evidence remains in range; do not claim it remains current without the refresh. Record the future source-branch rule: a Paja implementation branch starts from the refreshed upstream main and its range is re-audited immediately before source work. Per D-06, document that planning commits and these artifacts remain in this fork and do not enter an upstream implementation PR. Per D-07, D-09, and D-10, run only Writer read commands: status porcelain, branch/HEAD/master refs, left-right commit count, recent local history, unstaged and staged name-status, untracked paths, remotes, worktrees, and relevant read-only config. Timestamp each observation and explicitly classify the local master as comparison-only until canonical data is human-verified. Capture the opening and closing Writer snapshots using the same read-only command set; if they differ, report the delta as concurrent evidence for review. State that Writer mutation commands are absent from this task and that the frozen D-07 snapshot is a preservation decision rather than a claim about the current concurrent checkout.</action>
  <verify>
    <automated>test -f .planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md && rg -n 'd4ba157dfb14876f878cb9055da3d17150d0b01d|upstream/main|Paja|Writer snapshot|UTC|comparison-only|Blocking Preconditions' .planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md && ! rg -n '[[:blank:]]+$' .planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md && git diff --check -- .planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>A timestamped audit exists with the refreshed full upstream SHA and actual historical range/Paja impact, an opening and closing read-only Writer inventory, a visible canonical-Writer block, and a clear distinction between external concurrent changes and Phase 101 actions.</acceptance_criteria>
  <done>PRE-01 has reproducible evidence for the Kehto baseline and Writer state, while neither Kehto/Paja source nor Writer files have been modified.</done>
</task>

<task type="auto">
  <name>Task 2: Author the blocked Writer contribution plan and protocol authority matrix</name>
  <read_first>
    - .planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md (fresh evidence and unresolved blocks)
    - .planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md (D-03, D-06, D-08, D-11, D-12, and D-15 through D-19)
    - .planning/phases/101-baseline-writer-contribution-preflight/101-RESEARCH.md (architecture map, Writer seams, and validation commands)
    - .planning/phases/101-baseline-writer-contribution-preflight/101-PATTERNS.md (approval-gated contribution-plan structure)
    - /workspace/projects/writer/AGENTS.md, /workspace/projects/writer/README.md, /workspace/projects/writer/package.json, and current Writer mention/upload source files (read-only only, to name the observed source and test scope)
  </read_first>
  <files>.planning/phases/101-baseline-writer-contribution-preflight/101-WRITER-CONTRIBUTION-PLAN.md</files>
  <action>Per D-03, retain standard-NAP mediation, Writer WIP preservation, focused-PR hygiene, and the human approval boundary even where routine documentation choices are discretionary. Per D-06 and D-08, write a Phase 104 contribution plan whose branch is `feat/paja-social-blossom-integration` unless the verified canonical Writer repository requires a different name; it uses a new linked worktree and never the observed dirty worktree. Make canonical Writer URL, default branch, verified full SHA, exact completed Paja implementation commit or consumable artifact, and post-canonical-base source/test revalidation explicit BLOCKED fields. Use the audit and read-only Writer inspection to list observed tentative owning source/test paths, including the existing orchestration seam and a focused deterministic-profile reducer/test seam where the canonical base confirms it; do not represent a tentative path as already changed. Include one visibly headed `Post-approval-only commands` section containing the remote-add, fetch, verified-ref, and linked-worktree template. State before that heading that none of those commands may run during this phase. Per D-11 and D-12, set Paja-first delivery and the exact implementation branch/worktree-to-Writer target URL proof as dependency order, disallow workspace links, source copies, and unpublished dependency noise, and require a released or explicitly stacked dependency decision before an upstream Writer PR. Include the required Writer standard-NAP matrix with exact pinned references: NAP-IDENTITY 6461e4b37c29dc09a20dff35d9515889c4433874 for read-only `identity.getPublicKey`, `identity.getFollows`, and pushed `identity.changed`, with an empty pubkey for signed-out state, no polling, and no private-key/signing exposure; NAP-OUTBOX draft 4589a8f9a16d8aa29b3740e2b3b0cdca11e0976e plus NIP-01 for bounded kind-0 `outbox.query`, retained valid partial events, and greatest-created_at then lexically-lowest-ID reduction; Per D-16, the pinned NAP-OUTBOX ref `4589a8f9a16d8aa29b3740e2b3b0cdca11e0976e` is the authority for batched `outbox.query` kind-0 reads with query-wide `events`, `incomplete`, and `error`; it provides no ordering or newest-per-author guarantee, and deterministic newest-profile reduction remains Writer responsibility under NIP-01; NAP-RESOURCE draft fa6bcc6935aa19e7b70ab2a2c721dafca77c78e1 for `resource.bytes`, cancellation, Blob object URLs, and revocation; Writer must never assign remote metadata or upload-server URLs directly to media elements; NAP-UPLOAD draft a7cc17463cbf5d9cb87884b31071bc4fc826034c for `upload.upload` with `rail: "blossom"` and Paja-owned server/consent/signing/transfer/validation; and NAP-BLOSSOM draft ca1d7ba594e6790785dc770227085d8648d39631 as not a Writer API. Per D-15 through D-18, mark the first four rows conformant planned behavior. Per D-19, mark request-bound descriptor URL/hash/size validation as Kehto product security and an intentional NAP-BLOSSOM draft-spec gap, not a claim that the draft mandates it. Include focused Writer checks, `corepack pnpm type-check`, `corepack pnpm build`, `corepack pnpm test:conformance`, focused tests, Paja-hosted smoke proof, applicable Kehto gates, explicit-path staging, and PR exclusions: planning files, Graphify output, generated artifacts, screenshots, unrelated cleanup, preserved WIP, and cross-repository changes belonging in a different PR.</action>
  <verify>
    <automated>test -f .planning/phases/101-baseline-writer-contribution-preflight/101-WRITER-CONTRIBUTION-PLAN.md && rg -n 'BLOCKED|approve Writer implementation|feat/paja-social-blossom-integration|identity.getFollows|outbox.query|rail: "blossom"|empty pubkey for signed-out state|no polling|no private-key/signing exposure|Writer must never assign remote metadata or upload-server URLs directly to media elements|6461e4b37c29dc09a20dff35d9515889c4433874|4589a8f9a16d8aa29b3740e2b3b0cdca11e0976e|fa6bcc6935aa19e7b70ab2a2c721dafca77c78e1|a7cc17463cbf5d9cb87884b31071bc4fc826034c|ca1d7ba594e6790785dc770227085d8648d39631|conformant planned behavior|draft-spec gap|Post-approval-only commands|corepack pnpm type-check' .planning/phases/101-baseline-writer-contribution-preflight/101-WRITER-CONTRIBUTION-PLAN.md && ! rg -n '[[:blank:]]+$' .planning/phases/101-baseline-writer-contribution-preflight/101-WRITER-CONTRIBUTION-PLAN.md && git diff --check -- .planning/phases/101-baseline-writer-contribution-preflight/101-WRITER-CONTRIBUTION-PLAN.md</automated>
  </verify>
  <acceptance_criteria>The contribution plan provides a complete approval packet: preserved-WIP inventory, human-supplied canonical base fields, Paja dependency block, isolated branch/worktree template, source/test scope, all exact pinned NAP references with their conformant planned-behavior determinations, the intentional NAP-BLOSSOM draft-spec/Kehto product-security gap, gates, and focused PR exclusions. Per D-15, it explicitly requires read-only identity, an empty pubkey for signed-out state, no polling, and no private-key or signing exposure. Per D-17, it explicitly requires `resource.bytes` with cancellation, revocable Blob object URLs, and prohibits assigning remote metadata or upload-server URLs directly to media elements.</acceptance_criteria>
  <done>PRE-02 is documented in a reviewable plan that stops before every Writer-mutating operation and treats missing canonical/Paja values as blockers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Record a non-authorizing pending review for the future Writer implementation</name>
  <read_first>
    - .planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md
    - .planning/phases/101-baseline-writer-contribution-preflight/101-WRITER-CONTRIBUTION-PLAN.md
    - .planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md (D-11, D-13, and D-14)
  </read_first>
  <files>None; this task reviews the two Phase 101 artifacts and records only BLOCKED/PENDING status in the execution summary.</files>
  <action>Per D-13, present the approval-packet template for a non-authorizing review: preserved-WIP inventory and timestamps, required canonical Writer URL/default branch/full baseline SHA, clean worktree/branch template, required exact Paja commit or artifact, Writer source/test scope, pinned standard-NAP matrix with conformance and draft-gap determinations, focused and full checks, and PR exclusions. Per D-11 and D-14, record `BLOCKED/PENDING` rather than granting or requesting Writer implementation authorization: the canonical Writer base must be refreshed and the exact Phase 102/103 Paja implementation commit or consumable artifact must exist first. Actual `approve Writer implementation` authorization occurs only at the Phase 104 gate after that refreshed packet is reviewed. Do not run the post-approval template or Writer source work in this phase, and do not treat this Phase 101 review as a user decision to create a worktree, branch, remote, or source edit.</action>
  <what-built>Two reviewable Phase 101 artifacts establish refreshed Kehto/Writer evidence and a blocked Writer contribution-plan template.</what-built>
  <how-to-verify>Review the audit's refreshed SHA/range, Writer timestamps, and preservation inventory. Review that the contribution plan has BLOCKED canonical-base and Paja-dependency fields, pinned protocol classifications, Phase 104 setup template, checks, and exclusions. Confirm the execution summary records `BLOCKED/PENDING`; no approval is requested or recorded in Phase 101.</how-to-verify>
  <resume-signal>Type `reviewed` to retain BLOCKED/PENDING status, or describe packet corrections. Actual Writer authorization is deferred to the Phase 104 gate.</resume-signal>
  <verify>
    <automated>test -f .planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md && test -f .planning/phases/101-baseline-writer-contribution-preflight/101-WRITER-CONTRIBUTION-PLAN.md && ! rg -n '[[:blank:]]+$' .planning/phases/101-baseline-writer-contribution-preflight/101-BASELINE-AUDIT.md && ! rg -n '[[:blank:]]+$' .planning/phases/101-baseline-writer-contribution-preflight/101-WRITER-CONTRIBUTION-PLAN.md && git diff --check -- .planning/phases/101-baseline-writer-contribution-preflight</automated>
    <human_check>The reviewer confirms the packet is ready to remain pending or returns corrections; Phase 101 records no Writer implementation approval.</human_check>
  </verify>
  <acceptance_criteria>Phase 101 has one bounded, non-authorizing review of the complete packet; status is explicitly BLOCKED/PENDING and no action in this plan continues into Writer source work.</acceptance_criteria>
  <done>Phase 101 completes with Writer implementation approval BLOCKED/PENDING. The Phase 104 gate alone may record approval after a refreshed canonical Writer base and exact Phase 102/103 Paja artifact are available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kehto checkout to kehto/web upstream | A mutable remote ref becomes the source baseline for a later Paja implementation branch. |
| Phase 101 executor to `/workspace/projects/writer` | Phase 101 may observe a concurrently mutable, dirty external checkout but must not change its Git or source state. |
| Writer napplet to Paja/shell | Future Writer behavior requests standard NAP services while Paja/shell retains network, consent, signing, credential, and policy authority. |
| Human approval to Phase 104 | The approval packet is the only authority that can allow a separate Writer worktree and scoped source work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-101-01 | Tampering | `/workspace/projects/writer` dirty checkout | high | mitigate | Task 1 records timestamped read-only evidence; Task 2 confines every Writer-mutating command to a Phase 104-only template; Task 3 records only BLOCKED/PENDING and cannot authorize continuation. |
| T-101-02 | Spoofing/Tampering | canonical Writer upstream and Kehto `upstream/main` | high | mitigate | Task 1 refreshes and records full Kehto refs/range; Task 2 treats Writer URL, default branch, full SHA, and Paja artifact as human-verified blockers rather than local defaults. |
| T-101-03 | Elevation of Privilege | Writer-to-Paja service boundary | high | mitigate | Task 2's pinned authority matrix keeps identity, relay, resource, upload, consent, signing, and network authority in Paja/shell; it records signed-out identity and RESOURCE media constraints per D-15 and D-17. |
| T-101-04 | Repudiation | protocol-conformance claim | medium | mitigate | Task 2 names each immutable NAP ref and labels planned behavior conformant, upstream drift, or intentional NAP-BLOSSOM draft-spec/product-policy gap. |
| T-101-05 | Denial of Service | concurrent Writer evidence | low | accept | A later snapshot may differ due to unrelated activity; UTC timestamps and before/after classification make the evidence honest without attempting to freeze another checkout. |
| T-101-SC | Tampering | package-manager installs | low | accept | This phase installs no packages and its file scope is limited to planning artifacts. |

## ASVS L1 Control-to-Task Mapping

L1 requires present, grep-verifiable mitigations on the primary trust boundaries. Every high-severity threat below is mitigated; the low-severity accepted threats have the specific rationale in the STRIDE register.

| Applicable ASVS L1 control | Threat | Owning task | Required artifact statement | Verification |
|-----------------------------|--------|-------------|-----------------------------|--------------|
| V4 Access Control — approval-gated mutation | T-101-01 | Task 3 | `101-WRITER-CONTRIBUTION-PLAN.md` confines mutation to the Phase 104-only template; the summary records `BLOCKED/PENDING`, not authorization. | Task 3 automated artifact, trailing-whitespace, and diff checks plus the human review status check. |
| V5 Input Validation — exact canonical refs and dependency values | T-101-02 | Task 2 | The contribution plan makes canonical URL, default branch, full SHA, and exact Phase 102/103 Paja commit or artifact fail-closed BLOCKED fields; Task 1 records the refreshed Kehto SHA/range. | Task 1 required-marker check; Task 2 `BLOCKED` marker check; direct trailing-whitespace checks. |
| V2 Authentication, V4 Access Control, and V6 Cryptography — shell-owned authority | T-101-03 | Task 2 | The NAP matrix requires read-only identity, signed-out empty pubkey, no polling, no private-key/signing exposure, RESOURCE Blob delivery only, and Paja/shell-owned upload/signing/network authority. | Task 2 pinned-reference and D-15/D-17 marker assertions. |
| V5 Input Validation — accurate immutable protocol claims | T-101-04 | Task 2 | The matrix records each pinned NAP ref as conformant planned behavior, upstream drift, or draft-spec/product-policy gap without attributing Kehto hardening to NAP-BLOSSOM. | Task 2 pinned-reference, `conformant planned behavior`, and `draft-spec gap` marker assertions. |
</threat_model>

<verification>
1. Run both task-level automated commands, direct `rg -n '[[:blank:]]+$'` checks on both newly created artifacts, and `git diff --check -- .planning/phases/101-baseline-writer-contribution-preflight`.
2. Confirm the audit's Kehto SHA is freshly observed at execution, not copied from the historical 50d5787 research observation, and its range names Paja impact.
3. Confirm every Writer observation is timestamped and no Writer-mutating command was executed outside the visibly headed Phase 104-only template.
4. Confirm the contribution plan contains all pinned NAP refs; D-15's signed-out empty-pubkey/no-polling/no-signing constraints; D-17's media-URL prohibition; and the conformant-versus-draft-spec/product-security classification.
5. At the non-authorizing Phase 101 checkpoint, record BLOCKED/PENDING only. The Phase 104 gate may request actual approval after refreshed canonical Writer base values and the exact Phase 102/103 Paja artifact are in the refreshed packet.
</verification>

<success_criteria>
- PRE-01: Audit evidence identifies a fresh Kehto upstream baseline/range and a read-only classification of mutable Writer WIP, history, remote absence, and provisional baseline.
- PRE-02: A maintainer can review an approval-gated Writer plan with its WIP inventory, canonical-base/Paja blockers, branch strategy, exact protocol behavior, test plan, and PR boundaries.
- No Kehto/Paja source files and no Writer files are changed by Phase 101.
- All four supplied unresolved probe rows are visible as flagged assumptions, and all retained prohibitions are descriptor-less fail-closed review items.
</success_criteria>

<output>
Create `.planning/phases/101-baseline-writer-contribution-preflight/101-01-SUMMARY.md` when done.
</output>
