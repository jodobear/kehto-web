---
phase: 107-readable-responsive-paja-system
plan: 03
type: execute
wave: 3
depends_on: [107-01, 107-02]
files_modified:
  - packages/paja/src/host-page.ts
  - packages/paja/src/host-page.test.ts
  - tests/unit/phase-107-visual-system.test.ts
  - tests/e2e/paja-single-window.spec.ts
  - tests/e2e/paja-runtime-pointer.spec.ts
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, PAJA-01, PAJA-02, PAJA-03]
must_haves:
  truths:
    - "D-03: Paja declares the exact bounded semantic palette and 4/8/16/24/32/48/64 spacing scale once; component rules consume semantic tokens instead of repeated literals."
    - "D-04: Scoped Paja text uses only 12/14/18/24px and 400/600; every routine label, status, control, log, target, and detail computes to at least 12px."
    - "D-05: At 1280x720 Paja keeps a 48px context header, 360px console, flexible active stage, and wrapping footer of at least 32px."
    - "D-06/D-07: At 375x812 under max-width:640px, identity/target, horizontal tabs, essential command row, 224px independently scrollable controls, at least 320px stage, and wrapping two-column footer remain reachable with zero page horizontal overflow."
    - "VIS-01 empty edge: empty, loading, error, and message-log-empty surfaces still consume the complete semantic palette rather than falling back to unstyled/default colors."
    - "VIS-01 encoding edge: raw color values occur only in the Paja root declaration block; component selectors contain semantic variable references only."
    - "VIS-02 adjacency edge: adjacent labels, values, actions, and status text remain visually distinct and each routine computed size is at least 12px at both reference viewports."
    - "VIS-02 empty edge: the empty stage heading/body and empty message log use declared heading/body/label roles and remain readable."
    - "VIS-02 encoding edge: declaration-aware checks accept only 12/14/18/24px font sizes and 400/600 weights on scoped Paja rules."
    - "VIS-02 ordering edge: DOM/focus order follows identity -> tabs -> commands -> controls -> stage -> footer while desktop visual priority retains active stage first."
    - "VIS-03 empty edge: empty stage and zero-message layout use named spacing tokens and do not collapse/clamp controls or copy."
    - "VIS-03 encoding edge: margin, padding, and gap declarations use named scale tokens; borders/focus strokes/dimensions are classified separately."
    - "PAJA-01 manual-review edge resolved by exact browser measurements and visible-context assertions at 1280x720."
    - "PAJA-02 manual-review edge resolved by exact browser measurements, reachability, keyboard, and overflow assertions at 375x812."
    - "UI covered — Zero runtime tabs show the exact `No runtime loaded` stage while target loading remains available."
    - "UI covered — One or more runtime tabs retain labelled active/inactive hierarchy and exactly one visible active stage."
    - "UI covered — Runtime tabs scroll only inside their bounded strip and immediately reveal the active trigger without page scrolling."
    - "UI covered — Zero, one, and many runtime-tab states remain distinct and bounded."
    - "UI covered — Whitespace-only target input leaves the documented empty stage and starts no load."
    - "UI covered — Development-console load/retry progress remains explicit, busy, and disabled only during the current attempt."
    - "UI covered — Malformed or unreachable target input remains visible while the stage shows recoverable host error UI."
    - "UI covered — Incomplete/non-resolving input cannot start a second attempt and settles through the same error contract."
    - "UI covered — Console sections scroll inside the bounded controls panel while headings/actions remain reachable."
    - "UI covered — Lifecycle text stays inside its one status region and phone wrapping rules."
    - "UI covered — Runtime stage empty state uses exact heading/body and Load target path."
    - "UI covered — Runtime stage loading/retrying state exposes visible text, aria-busy, and one disabled current action until settlement."
    - "UI covered — Runtime stage error uses host explanation/retry/return/collapsed literal diagnostics and no raw iframe error document."
    - "UI covered — Successful loading shows one active iframe with persistent identity, target, tab, and lifecycle context."
    - "UI covered — Long error diagnostics wrap within bounded details and cannot widen the stage/page."
    - "UI covered — Error-panel actions remain reachable while expanded diagnostics scroll vertically inside their own region."
    - "UI covered — Error-panel long text stays literal, wraps anywhere, and cannot become executable markup."
    - "UI covered — Empty message log renders `No messages yet. Runtime traffic appears here.` and disables Clear messages."
    - "UI covered — One or many message rows preserve readable chronological hierarchy."
    - "UI covered — Message rows scroll inside a bounded log while its heading and Clear messages remain reachable."
    - "UI covered — Zero, one, and many message states retain explicit copy/hierarchy and bounded scrolling."
    - "UI covered — Long message diagnostics wrap/scroll inside the log and cannot widen the host page."
    - statement: "UI backstop — Browser evidence proves target/tab loading remains visible without displacing product identity or target context."
      verification: backstop
    - statement: "UI backstop — Browser evidence proves an errored active tab retains identifiable target context and access to recovery."
      verification: backstop
    - statement: "UI backstop — At 375x812 the independent tab strip contains overflow and document scrollWidth equals clientWidth."
      verification: backstop
    - statement: "UI backstop — A 160-character target wraps to two lines before ellipsis while the full text remains in accessible name and title."
      verification: backstop
    - statement: "UI backstop — Held-out tab-state proof shows loading without duplicate attempts and retains the intended active tab."
      verification: backstop
    - statement: "UI backstop — Held-out tab-state proof shows visible error and retry through the same verified reload path."
      verification: backstop
    - statement: "UI backstop — A multi-tab fixture shows ready, loading, and error tabs coexisting without hiding the active target."
      verification: backstop
    - statement: "UI backstop — Long tab labels ellipsize visually while retaining specific full accessible labels at both viewports."
      verification: backstop
    - statement: "UI backstop — Long pointer, signer, and simulation values wrap or truncate within bounds without obscuring focus."
      verification: backstop
    - statement: "UI backstop — Static/browser proof constrains lifecycle output to its finite documented vocabulary while diagnostics remain in details."
      verification: backstop
    - statement: "UI backstop — Reference-viewport proof shows at least 320px phone stage height, allowed internal scrolling, and no clipped host control/focus ring."
      verification: backstop
    - statement: "UI backstop — Negative browser proof shows loading does not invent a remote-loading placeholder inside the append-only message log."
      verification: backstop
    - statement: "UI backstop — Transport errors remain escaped message rows and never replace the log with an unrelated error screen."
      verification: backstop
    - statement: "UI backstop — Partial/unknown envelope fields remain readable escaped row content without breaking surrounding rows."
      verification: backstop
    - statement: "UI backstop — At 375x812 the two-column auto-row footer keeps every labelled status/value reachable."
      verification: backstop
    - statement: "UI backstop — Long runtime/simulation footer values wrap anywhere while labels and full accessible values remain intact."
      verification: backstop
  artifacts:
    - path: "tests/unit/phase-107-visual-system.test.ts"
      provides: "Declaration-aware Paja palette/type/spacing guard with line-numbered offenders"
    - path: "packages/paja/src/host-page.ts"
      provides: "Exact semantic tokens, landmarks, desktop grid, and phone composition"
    - path: "tests/e2e/paja-single-window.spec.ts"
      provides: "External-target computed style, desktop/phone geometry, state, focus, overflow, and screenshot proof"
    - path: "tests/e2e/paja-runtime-pointer.spec.ts"
      provides: "Verified-pointer tab/state/geometry/context proof at both viewports"
  key_links:
    - from: "packages/paja/src/host-page.ts"
      to: "packages/paja/src/browser-host.ts"
      via: "stable IDs/roles for context, status, controls, stage, error, log, and footer"
      pattern: "napplet-tabs|napplet-stage|target-status|message-log"
    - from: "tests/unit/phase-107-visual-system.test.ts"
      to: "packages/paja/src/host-page.ts"
      via: "declaration-block extraction plus property-aware palette/type/spacing assertions"
      pattern: "ui-color|ui-type|ui-space"
    - from: "tests/e2e/paja-runtime-pointer.spec.ts"
      to: "packages/paja/src/host-page.ts"
      via: "computed CSS and measured layout through the real verified host path"
      pattern: "setViewportSize|getComputedStyle|scrollWidth"
  prohibitions:
    - statement: "Phone presentation must not hide Paja identity/current target, create page-level horizontal scrolling, or make any host control, focus ring, console content, or footer value permanently unreachable."
      status: resolved
      verification: judgment
---

<objective>
Implement and prove Paja's exact semantic visual system and responsive desktop/phone composition.

Purpose: Make normal, empty, loading, error, and populated Paja states readable and intentionally composed without changing the recovered loader/session behavior from Plans 107-01/02.
Output: Declaration-aware static guard, semantic host template/CSS, and real-browser computed/layout/state evidence at 1280x720 and 375x812.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-readable-responsive-paja-system/107-CONTEXT.md
@.planning/phases/107-readable-responsive-paja-system/107-RESEARCH.md
@.planning/phases/107-readable-responsive-paja-system/107-PATTERNS.md
@.planning/phases/107-readable-responsive-paja-system/107-VALIDATION.md
@.planning/phases/107-readable-responsive-paja-system/107-UI-SPEC.md
@.planning/phases/107-readable-responsive-paja-system/107-01-SUMMARY.md
@.planning/phases/107-readable-responsive-paja-system/107-02-SUMMARY.md

<interfaces>
- `renderPajaHtml(config: PajaHostConfig): string` remains the complete escaped HTML renderer.
- Preserve controller-consumed element IDs and `sandbox="allow-scripts"`; add semantic roles/containers without renaming active controller hooks unless their callers/tests change atomically.
- Plan 107-01 `PajaTargetSurface` owns state projection; this plan styles its stable nodes and measures their states.
- `tests/unit/phase-107-visual-system.test.ts` must expose local declaration-extraction helpers only inside the test; it is not production API.
</interfaces>
</context>

## Decision traceability

- D-01: Approved UI-SPEC is exact implementation authority.
- D-02: Native framework-free HTML/CSS/TypeScript.
- D-03: Exact bounded semantic palette and spacing scale.
- D-04: Exact type sizes/weights and 12px routine-text floor.
- D-05: Preserved 1280x720 console/runtime composition.
- D-06: Purpose-built max-width:640px phone composition.
- D-07: 375x812 no-overflow/no-unreachable-control acceptance.

## Artifacts this phase produces

- New `tests/unit/phase-107-visual-system.test.ts` with declaration-block extraction and property-aware palette/type/spacing assertions.
- Modified `packages/paja/src/host-page.ts` with all canonical `--ui-color-*`, `--ui-type-*`, and `--ui-space-*` declarations plus exact desktop/phone layout.
- Modified `packages/paja/src/host-page.test.ts` with semantic landmark, copy, token, breakpoint, and sandbox/config-escaping contracts.
- Extended both Paja Playwright specs with computed style, geometry, state-shape, focus/reachability, overflow, long-text, and stable screenshot/attachment evidence.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the Paja token contract, then implement semantic desktop and phone CSS</name>
  <reversibility rating="costly">The locked shared token names and geometry span Paja plus Phase 107 feed/profile consumers; renaming later requires coordinated source, static-test, browser-test, and documentation edits.</reversibility>
  <read_first>
    - packages/paja/src/host-page.ts — current raw CSS, 900px rule, semantic scaffold, IDs, and escaped target/config rendering
    - packages/paja/src/host-page.test.ts — direct renderer-string contract style and sandbox/config escaping assertions
    - tests/unit/playground-dom-safety-guard.test.ts — recursive source-analysis and line-numbered offender pattern
    - .planning/phases/107-readable-responsive-paja-system/107-UI-SPEC.md — exact token values, typography, spacing, geometry, copy, targets, focus, and considerations per D-01 through D-07
    - .planning/phases/107-readable-responsive-paja-system/107-RESEARCH.md — declaration-aware guard guidance and false-positive exclusions
    - .planning/phases/107-readable-responsive-paja-system/107-PATTERNS.md — host-page renderer/grid/landmark analogs and required IDs
  </read_first>
  <files>tests/unit/phase-107-visual-system.test.ts, packages/paja/src/host-page.ts, packages/paja/src/host-page.test.ts</files>
  <behavior>
    - RED: Paja token declaration block is missing exact palette/type/spacing values and current component rules contain forbidden raw/irregular declarations.
    - GREEN: declaration-aware parser permits raw values only in the root token block and checks component color, font-size, font-weight, margin, padding, and gap consumption by property class.
    - GREEN: desktop renderer retains exact semantic landmarks/copy and 48px/360px/flexible-stage/wrapping-footer composition.
    - GREEN: max-width:640px renderer defines identity/tabs/command rows, 224px controls, >=320px stage, two-column auto-row footer, local scrollers, min-width:0, and long-token wrapping.
  </behavior>
  <action>Create `phase-107-visual-system.test.ts` first and confirm RED against current Paja CSS. Extract only the root declaration block for exact raw palette values; inspect component blocks separately. Check only color-bearing declarations for semantic variable consumption, only `font-size`/`font-weight` for the exact type set, and only margin/padding/gap properties for named spacing consumption so borders, radii, line heights, focus strokes, and fixed geometry are not misclassified. Then rewrite scoped Paja HTML/CSS in `renderPajaHtml` per D-01 through D-07: declare the exact semantic tokens once, use the approved font stacks/roles, reserve accent roles, preserve escaped config/target values and controller IDs, use semantic landmarks, and implement exact desktop/phone geometry. Replace the old 900px hide/reflow behavior with max-width:640px composition; use `min-width:0`, bounded internal scroll, and `overflow-wrap:anywhere` at grid/flex/identifier seams. Keep no-motion behavior and 32px desktop/48px phone targets. Update renderer tests for exact copy/tokens/roles/breakpoint while retaining sandbox and config escaping.</action>
  <verify>
    <automated>corepack pnpm exec vitest run tests/unit/phase-107-visual-system.test.ts packages/paja/src/host-page.test.ts &amp;&amp; corepack pnpm dlx aislop@0.12.0 scan --changes --base HEAD</automated>
  </verify>
  <acceptance_criteria>
    - New static guard reports line-numbered offenders and passes exact Paja palette/type/spacing declarations without misclassifying strokes, line heights, radii, or layout dimensions.
    - Paja component selectors repeat no raw color literal outside root declarations and use only the exact type sizes/weights and spacing tokens.
    - Renderer contains the exact semantic landmarks/state copy plus max-width:640px phone composition and preserves sandbox/config escaping.
    - Focused Vitest exits 0; the pinned changed-file AI-slop result is 100/100 before commit.
  </acceptance_criteria>
  <done>Paja source has a mechanically enforced semantic vocabulary and exact desktop/phone composition ready for browser measurement.</done>
  <commit_checkpoint>After green verification, stage only the three listed files and commit `feat(107-03): apply semantic responsive Paja system` with the required Codex co-author trailer; continue only from a clean task-owned diff.</commit_checkpoint>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove all Paja states, geometry, overflow, targets, and focus at both viewports</name>
  <read_first>
    - tests/e2e/paja-single-window.spec.ts — Plan 107-01 external failure/retry fixtures and existing one-frame assertions
    - tests/e2e/paja-runtime-pointer.spec.ts — Plan 107-02 verified multi-tab/failure/recovery fixtures
    - packages/paja/src/host-page.ts — Task 1 token/layout implementation to measure and refine
    - tests/e2e/shell-ui-state-surfaces.spec.ts — repository one-evaluate measurement/diagnostic assertion pattern
    - .planning/phases/107-readable-responsive-paja-system/107-UI-SPEC.md — exact 46-consideration Paja subset and verification contract
    - .planning/phases/107-readable-responsive-paja-system/107-VALIDATION.md — required dual-Paja browser commands and sampling rate
  </read_first>
  <files>tests/e2e/paja-single-window.spec.ts, tests/e2e/paja-runtime-pointer.spec.ts, packages/paja/src/host-page.ts</files>
  <behavior>
    - At 1280x720, computed geometry is 48px header, 360px console, flexible visible stage, and >=32px wrapping footer; identity/target/tabs/controls/status remain distinct.
    - At 375x812, page scrollWidth equals clientWidth, controls are 224px with independent overflow, stage is >=320px, footer/actions/focus rings are reachable, and phone actions are >=48px.
    - At a real Chromium effective CSS viewport of 640x360 (1280x720 at 200% zoom/reflow), routine text remains readable and the document never requires simultaneous horizontal and vertical page scrolling.
    - Normal, empty, loading, error, retrying, recovered, zero/one/many/partial tabs, zero/one/many messages, and long target/diagnostic/footer states satisfy their covered/backstop truths.
    - Computed routine text is >=12px and only declared type sizes/weights occur; desktop actions are >=32px.
  </behavior>
  <action>Add browser assertions before any CSS correction. Run the real external and verified-pointer servers at 1280x720 and 375x812. Add a separate Chromium reflow case using an effective 640x360 CSS viewport to represent 1280x720 at 200% zoom; do not substitute a screenshot-only or device-pixel-ratio check. In that case, measure document `scrollWidth/clientWidth` and `scrollHeight/clientHeight`, assert readable computed text, permit vertical page movement, forbid horizontal page movement, and prove the page never requires two-dimensional scrolling to reach host context, controls, recovery actions, or footer values. Collect computed styles and geometry in bounded `page.evaluate()` snapshots with diagnostic assertion messages: document/client width, header/footer/console/stage rectangles, internal overflow ownership, action target sizes, text sizes/weights, visible/focusable controls, target title/accessibility, and active-tab reveal. Exercise normal, empty, loading, error, retrying, recovered, zero/one/many/partial, message-log, and 160-character target/diagnostic/footer fixtures; assert exact finite lifecycle copy and no invented log-loading screen. Keyboard-test Load/Reload/Retry/return/tab/share/close and unobscured focus. Capture stable screenshots or Playwright attachments for normal/error/retrying/recovered states at both locked viewports. Adjust only `host-page.ts` presentation if a measurement fails; do not modify loader/protocol behavior from Plans 107-01/02.</action>
  <verify>
    <automated>corepack pnpm exec playwright test tests/e2e/paja-single-window.spec.ts tests/e2e/paja-runtime-pointer.spec.ts --project=chromium --workers=1 &amp;&amp; corepack pnpm dlx aislop@0.12.0 scan --changes --base HEAD</automated>
  </verify>
  <acceptance_criteria>
    - Exact desktop and phone geometry, computed type floor, action targets, overflow ownership, long-text accessibility, footer reachability, and focus assertions pass.
    - Both real host paths cover normal, empty/loading/error/retrying/recovered and zero/one/many/partial states; every UI backstop in this plan has an explicit passing assertion or attachment.
    - 375x812 has no page-level horizontal overflow and no permanently clipped host control/focus ring/context/footer value.
    - The dedicated 640x360 effective-viewport Chromium case proves 200% readable reflow with no horizontal page scroll and therefore no two-dimensional page scrolling.
    - Stable visual evidence exists for normal/error/retrying/recovered states at both viewports.
    - Both focused Paja Playwright specs exit 0; the pinned changed-file AI-slop result is 100/100 before commit.
  </acceptance_criteria>
  <done>Paja's full Phase 107 state matrix is browser-proven at both locked viewports with no layout, readability, reachability, or focus gap.</done>
  <commit_checkpoint>After green verification, stage only the three listed files and commit `test(107-03): prove Paja visual and responsive states` with the required Codex co-author trailer; leave no task-owned uncommitted changes.</commit_checkpoint>
</task>

</tasks>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Clear product/target context and usable controls on desktop/phone | 107-03 | COVERED | Exact viewport proof. |
| REQ | VIS-01/VIS-02/VIS-03 | Semantic palette, readable type, named spacing/non-clipping | 107-03 | COVERED | Static + computed/browser guards. |
| REQ | PAJA-01/PAJA-02 | Desktop split and purpose-built phone composition | 107-03 | COVERED | Exact geometry/reachability. |
| REQ | PAJA-03 | Semantic error surface remains readable/reachable | 107-03 | COVERED | All recovery visual states measured. |
| RESEARCH | Locked Visual Contract / Pitfalls 4/6 | Exact values; clipping/static-guard risks | 107-03 | COVERED | Property-aware parser + measurements. |
| CONTEXT | D-01 through D-07 | Approved visual contract and compositions | 107-03 | COVERED | Cited in tasks/truths. |
| UI-SPEC | 38 Paja considerations | 22 covered + 16 backstop rows | 107-03 | COVERED | No unresolved UI row; every row lifted above. |
| FALLBACK EDGE | 10 visual/composition items | VIS empty/encoding/adjacency/ordering and PAJA-01/02 review | 107-03 | COVERED | Exact acceptance criteria lifted above. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Escaped config/target values -> host template | User-controlled long identifiers enter privileged HTML attributes/text. |
| Host CSS/layout -> security controls | Presentation determines whether target identity, recovery, and development controls remain perceivable/reachable. |
| Sandboxed frame -> host stage | Layout must not visually merge trusted host controls with untrusted target content. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-107-03-01 | Spoofing | Target/context presentation | medium | mitigate | Preserve escaped full target in visible context, accessible name, and title; synchronize it with active tab and browser-test long/multi-tab cases. |
| T-107-03-02 | Denial of Service | Phone overflow/clipping | medium | mitigate | Exact grid rows, min-width:0, bounded internal scrollers, wrap-anywhere, reachability, and scrollWidth equality assertions. |
| T-107-03-03 | Elevation of Privilege | Host/iframe visual boundary | high | mitigate | Retain labelled host stage, visible host context, sandbox attributes, and distinct semantic host surfaces; both E2E paths reassert sandbox. |
| T-107-03-04 | Tampering | Template interpolation | high | mitigate | Preserve `escapeAttribute`, `escapeHtml`, and escaped config JSON; renderer tests keep malicious target/config vectors. |
| T-107-03-05 | Information Disclosure | Screenshots/attachments | low | accept | Deterministic local fixtures contain no secrets or signer material; evidence captures only synthetic development state. |
</threat_model>

<verification>
- Run both task commands.
- Run `corepack pnpm exec vitest run packages/paja/src/host-page.test.ts packages/paja/src/browser-target-surface.test.ts packages/paja/src/browser-runtime-tabs.test.ts packages/paja/src/browser-host.test.ts tests/unit/phase-107-visual-system.test.ts tests/unit/nip5d-conformance-guard.test.ts tests/unit/select-e2e-tests.test.ts`.
- Run `corepack pnpm --filter @kehto/paja type-check` and `corepack pnpm --filter @kehto/paja build`.
- Run `git diff --check`.
</verification>

<success_criteria>
- Exact semantic palette/type/spacing contract is enforced without brittle false positives.
- Paja desktop split remains intact and phone composition meets every locked measurement.
- All covered/backstop Paja UI considerations have explicit automated evidence.
- Recovery remains host-owned, accessible, readable, and protocol-neutral.
</success_criteria>

<output>
Create `.planning/phases/107-readable-responsive-paja-system/107-03-SUMMARY.md` when done.
</output>
