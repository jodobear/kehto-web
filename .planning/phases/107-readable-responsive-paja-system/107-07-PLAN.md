---
phase: 107-readable-responsive-paja-system
plan: 07
type: execute
wave: 7
depends_on: [107-06]
files_modified:
  - .planning/phases/107-readable-responsive-paja-system/107-REVIEW-FIX.iter2.md
  - .planning/phases/107-readable-responsive-paja-system/107-REVIEW.iter2.md
  - .planning/phases/107-readable-responsive-paja-system/107-REVIEW.iter3.md
autonomous: true
gap_closure: true
requirements: [VIS-01, VIS-02, VIS-03, PAJA-01, PAJA-02, PAJA-03, PAJA-04]
must_haves:
  truths:
    - "The Phase 107 base b7d045f560d6945e7974f9719fcd9c02314f9588 is validated as an exact existing ancestor before any range-based gate runs."
    - "Only trailing whitespace is removed from the three named review artifacts; their evidence, findings, refs, status, and prose remain semantically unchanged."
    - "git diff --check b7d045f560d6945e7974f9719fcd9c02314f9588 exits 0 after the complete timeout and evidence closure."
    - "From repository root, pinned aislop 0.12.0 scans changes against the exact Phase 107 base and returns 100/100 with no config, threshold, rule-disable, base substitution, or accepted override."
    - "Build, type, unit, docs, conformance, visual/static, selector, theme, and every Phase 107-owned Chromium case pass after 107-06."
    - "Inherited upload-domain defect 28 is reproduced/classified separately as baseline debt and remains non-blocking; no upload behavior, expectation, allowlist, or capability mapping changes."
    - "All 16 spec-less edge items, all applicable UI-SPEC considerations, all seven requirements, and all locked decisions have reproducible evidence or an explicit non-applicable rationale."
    - "The three descriptor-less prohibitions remain unverified judgment gates for human review; automated evidence supports but does not silently resolve them."
  artifacts:
    - path: ".planning/phases/107-readable-responsive-paja-system/107-REVIEW-FIX.iter2.md"
      provides: "Whitespace-clean review-fix evidence"
    - path: ".planning/phases/107-readable-responsive-paja-system/107-REVIEW.iter2.md"
      provides: "Whitespace-clean first review evidence"
    - path: ".planning/phases/107-readable-responsive-paja-system/107-REVIEW.iter3.md"
      provides: "Whitespace-clean second review evidence"
    - path: ".planning/phases/107-readable-responsive-paja-system/107-07-SUMMARY.md"
      provides: "Exact base validation, pinned 100/100 scan, diff, full gates, edge audit, and defect-28 disposition"
  key_links:
    - from: "Phase 107 base SHA"
      to: "git diff --check and aislop changed-file scan"
      via: "same validated immutable 40-hex commit, never HEAD or a named ref"
      pattern: "b7d045f560d6945e7974f9719fcd9c02314f9588"
    - from: "107-06 timeout behavior"
      to: "final browser/conformance gates"
      via: "both real Paja host paths plus exact defect-28 separation"
      pattern: "paja-runtime-pointer|paja-single-window"
  prohibitions:
    - statement: "No NAP/capability/routing/lifecycle/sandbox/verified-byte/package-version expansion entered Phase 107."
      status: unverified
      verification: judgment
    - statement: "At 375x812, Paja identity/current target and host content remain reachable with no page-level horizontal scrolling."
      status: unverified
      verification: judgment
    - statement: "Feed/profile recovery behavior remains Phase 108 scope and did not enter Phase 107."
      status: unverified
      verification: judgment
---

<objective>
Make final Phase 107 acceptance reproducible from its immutable base.

Purpose: Remove the evidence-only whitespace blocker, prove the pinned quality scan without overrides, and rerun the complete owned acceptance surface after timeout closure while preserving inherited defect 28 as separate baseline debt.
Output: Three whitespace-clean review artifacts and one exact, reproducible final acceptance record.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/107-readable-responsive-paja-system/107-CONTEXT.md
@.planning/phases/107-readable-responsive-paja-system/107-UI-SPEC.md
@.planning/phases/107-readable-responsive-paja-system/107-VALIDATION.md
@.planning/phases/107-readable-responsive-paja-system/107-VERIFICATION.md
@.planning/phases/107-readable-responsive-paja-system/107-05-SUMMARY.md
@.planning/phases/107-readable-responsive-paja-system/107-06-SUMMARY.md
</context>

## Decision traceability

- D-01 through D-07: Existing semantic system and desktop/phone evidence remain authoritative and are rerun.
- D-08 through D-10: Existing host recovery/verified-loader boundaries plus 107-06 timeout closure are rerun on both real paths.
- D-11/D-12: Feed/profile semantic presentation remains unchanged; behavioral recovery remains Phase 108.

<tasks>

<task type="tracer">
  <name>Task 1: Reproduce clean Phase 107 acceptance from the exact base</name>
  <read_first>
    - .planning/phases/107-readable-responsive-paja-system/107-VERIFICATION.md — exact whitespace/slop gap, owned browser evidence, prohibitions, and defect-28 disposition
    - .planning/phases/107-readable-responsive-paja-system/107-05-SUMMARY.md — recorded Phase base and prior full-gate command/evidence
    - .planning/phases/107-readable-responsive-paja-system/107-06-SUMMARY.md — timeout closure files, current upstream refs, and focused proof
    - .planning/phases/107-readable-responsive-paja-system/107-REVIEW-FIX.iter2.md — trailing-whitespace target only
    - .planning/phases/107-readable-responsive-paja-system/107-REVIEW.iter2.md — trailing-whitespace target only
    - .planning/phases/107-readable-responsive-paja-system/107-REVIEW.iter3.md — trailing-whitespace target only
    - .aislop/config.yml — pinned policy; read only and unchanged from the Phase base
  </read_first>
  <files>.planning/phases/107-readable-responsive-paja-system/107-REVIEW-FIX.iter2.md, .planning/phases/107-readable-responsive-paja-system/107-REVIEW.iter2.md, .planning/phases/107-readable-responsive-paja-system/107-REVIEW.iter3.md</files>
  <action>Start at repository root. Validate that `b7d045f560d6945e7974f9719fcd9c02314f9588` resolves to that exact commit object and is an ancestor of HEAD; stop if either fact fails. Remove trailing spaces/tabs only from the three named review artifacts, preserving every non-whitespace byte of meaning and leaving all completed plans, summaries, verification, code-review conclusions, and unrelated files untouched. Confirm the exact base-range diff check passes. Then run the pinned changed-file scanner from repository root against that same literal SHA with `--json` and require numeric `.scoreable == true` plus `.score == 100`; do not change `.aislop/config.yml`, disable a rule, loosen a threshold, substitute HEAD/named refs, add an ignore, or record an override. If and only if parent Git has already validated the literal base and the scanner hits the known sandbox child-git EPERM condition (direct `spawn git EPERM`, or its derived `Could not resolve base ref` error for that already-validated SHA), rerun the exact pinned JSON scan unsandboxed and apply the same numeric assertion; this environment rerun is never a score override. Any other scanner failure remains blocking. Run full build/type/unit/docs and the focused conformance/visual/selector/controller suites. Run all Phase 107-owned Paja Chromium cases from both real host specs with the exact defect-28 upload case separated by its full test title, plus theme-broadcast Chromium. Reproduce defect 28 with that anchored exact title, require a nonzero status, exactly one failed case, and the exact pre-existing `Required shell domains unavailable` symptom; if the case passes, the symptom is absent, or any other failure appears, stop rather than swallowing it. Keep the classified defect non-blocking only because blame/base evidence already establishes it as inherited; do not edit upload code/tests/capabilities. Record commands, exit statuses, numeric scanner score, any child-git EPERM rerun, exact upstream refs inherited from 107-06, base/diff evidence, defect output, edge audit, and the three remaining human-judgment flags in 107-07-SUMMARY.md.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; phase_base=b7d045f560d6945e7974f9719fcd9c02314f9588; defect_title="stores disclosed bytes through a signed Blossom upload and fails closed on denial or incomplete proof"; test "$(git rev-parse "$phase_base^{commit}")" = "$phase_base"; git merge-base --is-ancestor "$phase_base" HEAD; git diff --quiet "$phase_base" -- .aislop/config.yml; git diff --check "$phase_base"; slop_report=$(mktemp /tmp/kehto-107-aislop.XXXXXX); slop_error=$(mktemp /tmp/kehto-107-aislop-error.XXXXXX); upload_report=$(mktemp /tmp/kehto-107-upload-defect.XXXXXX); trap "rm -f \"$slop_report\" \"$slop_error\" \"$upload_report\"" EXIT; set +e; corepack pnpm dlx aislop@0.12.0 scan --changes --base "$phase_base" --json &gt;"$slop_report" 2&gt;"$slop_error"; slop_status=$?; set -e; if [ "$slop_status" -ne 0 ]; then if rg -q "spawn git EPERM|Could not resolve base ref.*$phase_base" "$slop_report" "$slop_error"; then echo "KNOWN_CHILD_GIT_EPERM: rerun the exact pinned JSON scan unsandboxed and require the same jq assertion"; exit 77; fi; cat "$slop_error" "$slop_report"; exit "$slop_status"; fi; jq -e ".scoreable == true and .score == 100" "$slop_report"; corepack pnpm build; corepack pnpm type-check; corepack pnpm exec vitest run --exclude ".claude/worktrees/**"; corepack pnpm docs:check; corepack pnpm exec vitest run tests/unit/phase-107-visual-system.test.ts tests/unit/identity-theme-conformance-guard.test.ts tests/unit/nip5d-conformance-guard.test.ts tests/unit/select-e2e-tests.test.ts packages/paja/src/options.test.ts packages/paja/src/server.test.ts packages/paja/src/host-page.test.ts packages/paja/src/browser-target-surface.test.ts packages/paja/src/browser-host.test.ts packages/paja/src/browser-runtime-tabs.test.ts; corepack pnpm exec playwright test tests/e2e/paja-single-window.spec.ts tests/e2e/paja-runtime-pointer.spec.ts --project=chromium --workers=1 --grep-invert "$defect_title"; set +e; corepack pnpm exec playwright test tests/e2e/paja-single-window.spec.ts --project=chromium --workers=1 --grep "^$defect_title$" &gt;"$upload_report" 2&gt;&amp;1; upload_status=$?; set -e; if [ "$upload_status" -eq 0 ]; then echo "defect 28 unexpectedly passed; revisit baseline classification"; cat "$upload_report"; exit 1; fi; if ! rg -Fq "Required shell domains unavailable" "$upload_report" || ! rg -Eq "1 failed" "$upload_report"; then cat "$upload_report"; exit "$upload_status"; fi; corepack pnpm exec playwright test tests/e2e/theme-broadcast.spec.ts --project=chromium --workers=1; git diff --check "$phase_base"'</automated>
    <human-check>Review the Phase 107 base diff and current-ref notes for the three unverified prohibitions: no NAP/capability/routing/lifecycle/sandbox/verified-byte/package-version expansion; phone identity/target/content remain reachable without page horizontal scroll; feed/profile behavioral recovery remains Phase 108.</human-check>
  </verify>
  <done>The immutable-base diff is whitespace-clean, pinned aislop measures 100/100 without overrides, every Phase 107-owned automated gate passes, defect 28 remains separately evidenced baseline debt, and judgment-tier prohibitions remain explicit for human verification.</done>
</task>

</tasks>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan(s) | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Clear context and usable controls across desktop, phone, and target failure | 107-01..07 | COVERED | Final gates include all completed behavior plus timeout closure. |
| REQ | VIS-01 | Coherent semantic palette | 107-03/04/07 | COVERED | Existing static/theme evidence rerun. |
| REQ | VIS-02 | 12px readable type and hierarchy | 107-03/04/07 | COVERED | Existing computed/static evidence rerun. |
| REQ | VIS-03 | Named spacing and phone non-clipping | 107-03/04/07 | COVERED | Existing layout/static evidence rerun. |
| REQ | PAJA-01 | Desktop split and distinct context | 107-02/03/07 | COVERED | Existing Chromium geometry rerun. |
| REQ | PAJA-02 | Purpose-built phone composition | 107-02/03/07 | COVERED | Existing phone/overflow/keyboard proof rerun. |
| REQ | PAJA-03 | Every target failure becomes actionable recovery | 107-01/02/03/06/07 | COVERED | Timeout gaps closed then rerun. |
| REQ | PAJA-04 | Existing verified retry/security boundary | 107-01/02/05/06/07 | COVERED | Current refs, conformance, provenance, and stale safety. |
| RESEARCH | Visual/validation/security | Exact UI contract, both Paja specs, full gates, unchanged boundary | 107-03..07 | COVERED | Final reproducible commands retain all research constraints. |
| CONTEXT | D-01..D-07 | Approved visual system and viewport composition | 107-03/04/07 | COVERED | No reimplementation; final evidence rerun. |
| CONTEXT | D-08..D-10 | Host recovery and existing verified loader | 107-01/02/06/07 | COVERED | Root gap closed without alternate path. |
| CONTEXT | D-11/D-12 | Feed/profile semantic-only boundary | 107-04/07 | COVERED | No behavioral recovery work. |
| VERIFICATION | Gap 2 | Diff whitespace and unreproducible pinned scan | 107-07 | COVERED | Exact base, clean diff, pinned score, no override. |
</source_audit>

<edge_probe_coverage>
- VIS-01 empty/encoding; VIS-02 adjacency/empty/encoding/ordering; VIS-03 empty/encoding: existing declaration-aware and browser state tests are rerun because this evidence-only task contributes no new visual transform.
- PAJA-01 and PAJA-02 unclassified: existing desktop/phone Chromium geometry, reachability, keyboard, context, and overflow checks are rerun; no new descriptor is invented.
- PAJA-03 idempotency/concurrency and PAJA-04 boundary/precision/idempotency/concurrency: 107-06's new timeout races plus existing retry/provenance tests are included in final gates.
- Descriptor-less prohibitions remain `status: unverified`, appear in frontmatter and human-check, and are not converted into autonomous PASS claims.
- API coverage matrix is non-applicable: 107-06 bounds an existing fetch/runtime path and 107-07 changes evidence whitespace only; neither adds an external API contribution.
</edge_probe_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git base/range -> acceptance evidence | An incorrect or substituted base could hide changed files or fabricate a clean result. |
| Package-executed scanner -> quality verdict | Tool/version/config drift could produce a non-reproducible or weakened score. |
| Known baseline defect -> release decision | An unrelated inherited failure must remain visible without blocking or masking Phase-owned regressions. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-107-07-01 | Tampering / Repudiation | base-range gates | high | mitigate | Validate the literal commit object and ancestor relation; reuse the same SHA for diff and scanner; record commands/statuses. |
| T-107-07-02 | Tampering | aislop verdict | high | mitigate | Pin 0.12.0, use JSON, assert numeric score 100 with scoreable true, and prove `.aislop/config.yml` unchanged; allow an unsandboxed rerun only for the known child-git EPERM signature, never as an override. |
| T-107-07-03 | Denial of Service / Repudiation | inherited defect 28 | medium | mitigate | Run Phase-owned cases separately, reproduce exact inherited symptom, preserve blame/base evidence, and forbid upload-path edits. |
| T-107-07-04 | Information Disclosure | gate logs and artifacts | low | accept | Tests use deterministic public fixtures; summaries omit tokens, signer material, passphrases, bunker URIs, and secrets. |
</threat_model>

<verification>
- Run Task 1's automated command from repository root after 107-06 is complete.
- Separately reproduce inherited defect 28 by exact full title, require its known symptom, and record nonzero status without making it a passing Phase-owned gate.
- Confirm `git status --short` names only the three task-owned review artifacts before commit and no completed plan/summary/verification artifact was rewritten.
- Confirm all edge/source audit rows are COVERED or explicitly non-applicable; none are MISSING.
</verification>

<success_criteria>
- Exact Phase 107 base validates and both base-range diff and pinned scanner use it.
- Three review artifacts contain no trailing whitespace and complete phase-range diff check passes.
- Pinned aislop 0.12.0 returns 100/100 without policy or base overrides.
- All Phase 107-owned automated behavior and UI gates pass after timeout closure.
- Upload defect 28 remains explicit, independently evidenced, and non-blocking without any upload-domain change.
- Three prohibition flags remain explicit human-judgment items, supported but not silently resolved by automation.
</success_criteria>

<output>
Create `.planning/phases/107-readable-responsive-paja-system/107-07-SUMMARY.md` when done.
</output>
