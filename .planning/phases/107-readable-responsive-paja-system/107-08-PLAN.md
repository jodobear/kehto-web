---
phase: 107-readable-responsive-paja-system
plan: 08
type: execute
wave: 8
depends_on: [107-07]
files_modified:
  - packages/paja/src/browser-host.ts
  - packages/paja/src/browser-runtime-pointer.ts
  - packages/paja/src/browser-target-diagnostics.ts
  - packages/paja/src/browser-host.test.ts
  - tests/e2e/paja-single-window.spec.ts
  - tests/e2e/paja-runtime-pointer.spec.ts
autonomous: true
gap_closure: true
requirements: [PAJA-03, PAJA-04]
gap_ids:
  - PRRT_kwDOTgR9Tc6VbmEU
  - PRRT_kwDOTgR9Tc6VbmEb
  - PRRT_kwDOTgR9Tc6VbmEh
must_haves:
  truths:
    - "Reload and interface-driven reload supersede an owned external attempt instead of silently returning while it is in flight."
    - "Runtime-pointer Relay plus Blossom resolution settles within config.runtime.readyTimeoutMs, aborts the owned resolver, and exposes the existing retry surface."
    - "A target whose module fetch would be blocked by CORS cannot become ready from the injected prelude's early shell.ready; it remains in host-owned recovery with Retry enabled."
    - "Late completion from a superseded or timed-out generation is inert and cannot clear or overwrite the current attempt."
    - "The existing readyTimeoutMs remains the sole availability budget; no NAP message, capability, route, lifecycle, sandbox, verified-byte, or package API changes."
    - "Current napplet/naps master and NIP-5D draft PR 2303 head are checked before implementation and remain semantically compatible."
  artifacts:
    - path: "packages/paja/src/browser-host.ts"
      provides: "External-attempt supersession and CORS-aware readiness settlement"
    - path: "packages/paja/src/browser-runtime-pointer.ts"
      provides: "Owned whole-attempt pointer resolution timeout"
    - path: "tests/e2e/paja-single-window.spec.ts"
      provides: "Reload supersession and CORS-blocked recovery proof"
    - path: "tests/e2e/paja-runtime-pointer.spec.ts"
      provides: "Hanging Blossom resolution timeout and retry proof"
  key_links:
    - from: "PR #2 exact-head review"
      to: "Phase 107 recovery behavior"
      via: "one regression per unresolved review thread"
      pattern: "PRRT_kwDOTgR9Tc6VbmE(U|b|h)"
    - from: "config.runtime.readyTimeoutMs"
      to: "runtime-pointer resolver AbortController"
      via: "generation-owned deadline and AbortSignal"
      pattern: "readyTimeoutMs|AbortController|signal"
    - from: "target CORS diagnostic"
      to: "external navigation settlement"
      via: "diagnostic must allow navigation before shell.ready can settle ready"
      pattern: "target-cors|navigateFrame|shell.ready"
  prohibitions:
    - statement: "Do not add, remove, reshape, or reinterpret NAP messages, capabilities, routing rules, lifecycle contracts, sandbox permissions, verified bytes, or package APIs."
      status: unverified
      verification: judgment
    - statement: "Do not add feed/profile recovery behavior; that remains Phase 108."
      status: unverified
      verification: judgment
---

<objective>
Close the three actionable defects reported by the external Codex reviewer on the exact Phase 107 PR head.

Purpose: Make reload authoritative during in-flight external work, bound pre-tab pointer resolution, and prevent the injected prelude from masking CORS-blocked target modules.
Output: Generation-safe recovery behavior, focused unit/Chromium regressions, and exact-head external re-review.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/107-readable-responsive-paja-system/107-CONTEXT.md
@.planning/phases/107-readable-responsive-paja-system/107-UI-SPEC.md
@.planning/phases/107-readable-responsive-paja-system/107-06-SUMMARY.md
@.planning/phases/107-readable-responsive-paja-system/107-07-SUMMARY.md
@.planning/phases/107-readable-responsive-paja-system/107-VERIFICATION.md
</context>

## Authority check

- `napplet/naps` master: `5ac0490461ca6fec2f0d2e45b4835cf9bc08de24`
- NIP-5D draft PR 2303 head: `eb45dfd7335b7f88cb53781984c553581d2b4c34`
- Result: NAP-SHELL still requires one bare child `shell.ready` and one parent `shell.init`; NIP-5D still requires verified bytes through `srcdoc`, injected bootstrap outside signed bytes, register-before-navigation, `allow-scripts` without `allow-same-origin`, and trusted `MessageEvent.source`. The fixes preserve those contracts.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact regressions for all three external-review defects</name>
  <files>packages/paja/src/browser-host.test.ts, tests/e2e/paja-single-window.spec.ts, tests/e2e/paja-runtime-pointer.spec.ts</files>
  <behavior>
    - RED: Reload during an unresolved external attempt does not start a fresh generation.
    - RED: A resolved Relay event followed by a Blossom fetch that never settles leaves Load/Retry disabled past readyTimeoutMs.
    - RED: A target classified CORS-blocked can emit prelude shell.ready and incorrectly hide recovery before its module runs.
  </behavior>
  <action>Add focused static/unit guards plus real Chromium tracers. Hold and later release superseded work to prove stale settlement is inert. Use the existing signed pointer fixture and hold only Blossom retrieval. Make the external fixture's CORS result switchable so Retry can prove recovery after the diagnostic becomes allowed. Do not synthesize a protocol-ready message beyond the existing real injected prelude.</action>
  <verify><automated>corepack pnpm exec vitest run packages/paja/src/browser-host.test.ts tests/unit/nip5d-conformance-guard.test.ts &amp;&amp; corepack pnpm exec playwright test tests/e2e/paja-single-window.spec.ts tests/e2e/paja-runtime-pointer.spec.ts --project=chromium --workers=1 --grep "supersede|pointer resolution|CORS-blocked"</automated></verify>
  <done>Each remote thread has a failing regression on the reviewed head and late old work is observable but inert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Supersede external work and gate navigation on CORS viability</name>
  <files>packages/paja/src/browser-host.ts, packages/paja/src/browser-target-diagnostics.ts, packages/paja/src/browser-host.test.ts, tests/e2e/paja-single-window.spec.ts</files>
  <action>Remove the reload early-return and cancel only the currently owned external attempt before incrementing generation and starting the replacement. Make the existing local target-CORS diagnostic an awaited, signal-aware part of the same external attempt after config refresh and before `navigateFrame`; a blocked or unreachable result must log the existing diagnostic and reject through `settleExternalNavigationFailure`. Remove the detached fire-and-forget probe. Keep source/generation guards, register-before-srcdoc, CSP/prelude ordering, sandbox, verified bytes, and message shapes unchanged.</action>
  <verify><automated>corepack pnpm exec vitest run packages/paja/src/browser-host.test.ts tests/unit/nip5d-conformance-guard.test.ts &amp;&amp; corepack pnpm exec playwright test tests/e2e/paja-single-window.spec.ts --project=chromium --workers=1 --grep "supersede|CORS-blocked"</automated></verify>
  <done>Reload creates one replacement attempt and CORS-blocked modules remain in actionable host recovery despite early prelude readiness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bound runtime-pointer resolution and close the remote review gate</name>
  <files>packages/paja/src/browser-runtime-pointer.ts, packages/paja/src/browser-host.test.ts, tests/e2e/paja-runtime-pointer.spec.ts</files>
  <action>Arm a generation-owned deadline using `config.runtime.readyTimeoutMs` when pointer resolution starts. Abort the existing controller on expiry; distinguish attempt ownership from non-aborted currency so the timeout rejection projects through the existing resolver error surface while teardown and replacement remain inert. Clear the deadline in `finally`, restore focus/busy state only for the owned generation, and add no second timeout option. Run all Phase 107 protected gates, push the exact head, request `@codex review`, and require all actionable exact-head findings to be cleared before verification resumes.</action>
  <verify><automated>corepack pnpm exec vitest run packages/paja/src/browser-host.test.ts packages/paja/src/browser-runtime-tabs.test.ts tests/unit/nip5d-conformance-guard.test.ts &amp;&amp; corepack pnpm exec playwright test tests/e2e/paja-runtime-pointer.spec.ts --project=chromium --workers=1 --grep "pointer resolution"</automated></verify>
  <done>Hanging pointer resolution times out into Retry, retry succeeds through the existing loader, full Phase 107 gates pass, and external exact-head review has no actionable finding.</done>
</task>

</tasks>

<verification>
- Run focused RED/GREEN tests for each thread, then Phase 107 build, type, unit, docs, AI-slop, conformance, and owned Chromium gates.
- Confirm `git diff --check b7d045f560d6945e7974f9719fcd9c02314f9588` and the pinned changed-file AI-slop scan remain clean.
- Push the branch and obtain a new external Codex review on the resulting exact head; do not treat green CI or the older review as acceptance.
- Re-run Phase 107 verification and resume the existing four-item UAT only after remote review passes.
</verification>

<success_criteria>
- All three PR review threads have regression-backed fixes.
- Timeout, cancellation, stale-generation, CORS, source-trust, verified-byte, sandbox, and retry contracts pass together.
- Exact-head external review reports no actionable finding.
- Phase 107 verification/UAT can resume without any Phase 108 work.
</success_criteria>

<output>
Create `.planning/phases/107-readable-responsive-paja-system/107-08-SUMMARY.md` when done.
</output>
