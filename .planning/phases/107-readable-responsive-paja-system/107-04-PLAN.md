---
phase: 107-readable-responsive-paja-system
plan: 04
type: execute
wave: 4
depends_on: [107-03]
files_modified:
  - tests/unit/phase-107-visual-system.test.ts
  - apps/playground/napplets/feed/index.html
  - apps/playground/napplets/feed/src/main.ts
  - apps/playground/napplets/profile-viewer/index.html
  - apps/playground/napplets/profile-viewer/src/main.ts
  - tests/e2e/theme-broadcast.spec.ts
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03]
must_haves:
  truths:
    - "D-03/D-04/D-11: Feed and profile declare the same bounded `--ui-*` palette/type/spacing names as local aliases over existing `--nap-theme-*` inputs and use only exact Phase 107 type/spacing values."
    - "D-11/D-12: Feed/profile loading, data, failure, identity, intent, resource, and theme behavior/copy remain unchanged; only semantic token, type, spacing, and local tone consumption changes."
    - "VIS-01: Feed/profile component rules contain no raw color literal outside their one alias/fallback declaration block, and TypeScript sets semantic tone data instead of inline colors."
    - "VIS-02: Feed/profile author, timestamp, title, name, pubkey, about, detail, status, and content text compute to at least 12px with only 12/14/18/24px and 400/600."
    - "VIS-03: Feed/profile margin/padding/gap values consume the named 4/8/16/24/32/48/64 scale and long/partial content stays inside each iframe at 375x812."
    - statement: "UI backstop — Existing empty feed/profile states remain distinct and readable after token/type/spacing migration."
      verification: backstop
    - statement: "UI backstop — Existing feed/profile loading states retain visible semantic status text at 12px or larger."
      verification: backstop
    - statement: "UI backstop — Existing feed/profile failures remain readable; no Phase 108 recovery action or state-specific replacement copy appears."
      verification: backstop
    - statement: "UI backstop — Populated author, timestamp, profile, and content hierarchy is readable at the declared scale on desktop and phone fixtures."
      verification: backstop
    - statement: "UI backstop — Missing avatar/media and partial profile metadata remain legible without collapsing surrounding layout."
      verification: backstop
    - statement: "UI backstop — Long feed/profile content remains inside each iframe and does not create host-level horizontal overflow."
      verification: backstop
    - statement: "UI backstop — Feed zero/one/many fixtures and profile empty/populated fixtures remain visually and semantically distinct."
      verification: backstop
    - statement: "UI backstop — Long names, public keys, metadata, and content wrap/truncate within bounds while preserving full accessible values where truncated."
      verification: backstop
  artifacts:
    - path: "tests/unit/phase-107-visual-system.test.ts"
      provides: "Declaration-aware cross-surface palette/type/spacing/tone contract"
    - path: "apps/playground/napplets/feed/index.html"
      provides: "Feed semantic aliases and readable/scaled component consumption"
    - path: "apps/playground/napplets/feed/src/main.ts"
      provides: "File-local FeedStatusTone dataset projection without inline color"
    - path: "apps/playground/napplets/profile-viewer/index.html"
      provides: "Profile semantic aliases and readable/scaled component consumption"
    - path: "apps/playground/napplets/profile-viewer/src/main.ts"
      provides: "File-local ProfileStatusTone dataset projection without inline color"
    - path: "tests/e2e/theme-broadcast.spec.ts"
      provides: "Real iframe theme/computed-style and existing-state readability proof"
  key_links:
    - from: "apps/playground/src/theme.ts"
      to: "feed/profile local CSS"
      via: "existing `--nap-theme-*` variables mapped once to `--ui-*` aliases"
      pattern: "--nap-theme-(background|text|primary|surface|border|muted|success|danger)"
    - from: "feed/profile `setStatus`"
      to: "feed/profile CSS `[data-tone]` rules"
      via: "dataset tone neutral/success/danger with textContent unchanged"
      pattern: "dataset\.tone"
    - from: "tests/e2e/theme-broadcast.spec.ts"
      to: "real feed/profile iframes"
      via: "existing NAP-THEME broadcast plus computed style/state fixtures"
      pattern: "getComputedStyle"
  prohibitions:
    - statement: "Phase 107 feed/profile work must not add recovery buttons, reconnect/retry behavior, state-specific recovery copy, new load transitions, or any other Phase 108 behavioral recovery."
      status: resolved
      verification: judgment
---

<objective>
Make feed and profile consume the same readable semantic vocabulary without crossing the Phase 108 recovery boundary.

Purpose: Complete VIS-01 through VIS-03 across all three scoped surfaces while proving existing feed/profile states and NAP-THEME behavior remain intact.
Output: Local semantic aliases, dataset-based status tones, expanded static guard, and computed browser evidence for existing state shapes.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-readable-responsive-paja-system/107-CONTEXT.md
@.planning/phases/107-readable-responsive-paja-system/107-RESEARCH.md
@.planning/phases/107-readable-responsive-paja-system/107-PATTERNS.md
@.planning/phases/107-readable-responsive-paja-system/107-VALIDATION.md
@.planning/phases/107-readable-responsive-paja-system/107-UI-SPEC.md
@.planning/phases/107-readable-responsive-paja-system/107-03-SUMMARY.md

<interfaces>
- `apps/playground/src/theme.ts` already injects `--nap-theme-background`, `--nap-theme-text`, `--nap-theme-primary`, `--nap-theme-surface-1`, `--nap-theme-surface-2`, `--nap-theme-border`, `--nap-theme-muted`, `--nap-theme-success`, and `--nap-theme-danger`; consume them unchanged.
- Feed/profile `setStatus(text, color)` currently assigns `textContent` plus inline color. Replace only the second argument/type with file-local semantic tone and assign `statusEl.dataset.tone`.
- Extend the Plan 107-03 declaration-aware visual guard rather than creating a competing static test.
</interfaces>
</context>

## Decision traceability

- D-01: UI-SPEC exact cross-surface visual contract.
- D-02: Native framework-free HTML/CSS/TypeScript.
- D-03/D-04: Exact canonical palette/type/spacing values.
- D-11: Feed/profile semantic token/type/spacing consumption only.
- D-12: Feed/profile recovery and state-specific recovery copy remain Phase 108.

## Artifacts this phase produces

- New file-local type `FeedStatusTone = 'neutral' | 'success' | 'danger'` in feed `main.ts`.
- New file-local type `ProfileStatusTone = 'neutral' | 'success' | 'danger'` in profile-viewer `main.ts`.
- New local `--ui-*` declaration/alias blocks and `[data-tone]` rules in both HTML files; no new protocol/theme symbol.
- Expanded `phase-107-visual-system.test.ts` and `theme-broadcast.spec.ts`; no new test file.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the static contract, then migrate feed/profile aliases and tones</name>
  <reversibility rating="costly">These aliases intentionally match Paja's locked semantic vocabulary; undoing/renaming them requires coordinated changes across three surfaces, tests, and docs.</reversibility>
  <read_first>
    - tests/unit/phase-107-visual-system.test.ts — Plan 107-03 declaration-aware parser and Paja assertions to extend
    - apps/playground/napplets/feed/index.html — current raw color/type/spacing declarations and content/overflow seams
    - apps/playground/napplets/feed/src/main.ts — `setStatus`, existing copy/call sites, and safe `textContent` behavior
    - apps/playground/napplets/profile-viewer/index.html — current raw color/type/spacing declarations and profile detail/media seams
    - apps/playground/napplets/profile-viewer/src/main.ts — `setStatus`, loading/error/data behavior, and existing copy/call sites
    - apps/playground/src/theme.ts — read-only canonical `--nap-theme-*` producer; do not modify
    - .planning/phases/107-readable-responsive-paja-system/107-UI-SPEC.md — exact alias table, type/spacing roles, and feed/profile boundary per D-03/D-04/D-11/D-12
  </read_first>
  <files>tests/unit/phase-107-visual-system.test.ts, apps/playground/napplets/feed/index.html, apps/playground/napplets/feed/src/main.ts, apps/playground/napplets/profile-viewer/index.html, apps/playground/napplets/profile-viewer/src/main.ts</files>
  <behavior>
    - RED: static guard detects current feed/profile raw component colors, sub-12px type, irregular spacing, and inline status color assignment.
    - GREEN: each HTML declares the complete canonical names once as existing NAP-THEME aliases/fallbacks; components consume only semantic names.
    - GREEN: all scoped font/weight/spacing declarations match the exact allowed sets; long/partial-content safety properties remain.
    - GREEN: status functions preserve every current string/transition and project neutral/success/danger through dataset tone only.
  </behavior>
  <action>Extend the existing visual-system test first and confirm RED. Reuse its declaration/property-aware helpers across the two HTML files; permit raw colors only inside each local alias/fallback block and explicitly reject inline status color writes. Declare the complete canonical palette/type/spacing names once per HTML, mapping colors to the existing `--nap-theme-*` values and deriving danger surface from existing semantic theme inputs with its approved fallback. Convert component rules to semantic variables and exact type/spacing roles while preserving semantic DOM, `min-width:0`, `overflow-wrap:anywhere`, media/resource behavior, and current state copy. Add file-local `FeedStatusTone` and `ProfileStatusTone`; keep `setStatus` textContent/call order but set `dataset.tone` to neutral/success/danger, with CSS owning color. Per D-11/D-12, add no action, retry/reconnect state, status string, loading branch, service call, or protocol/theme change.</action>
  <verify>
    <automated>corepack pnpm exec vitest run tests/unit/phase-107-visual-system.test.ts tests/unit/identity-theme-conformance-guard.test.ts tests/unit/nip5d-conformance-guard.test.ts &amp;&amp; corepack pnpm --filter @kehto/demo-feed build &amp;&amp; corepack pnpm --filter @kehto/demo-profile-viewer build &amp;&amp; corepack pnpm dlx aislop@0.12.0 scan --changes --base HEAD</automated>
  </verify>
  <acceptance_criteria>
    - Static guard covers Paja, feed, and profile declaration/consumption boundaries and reports no raw component color, sub-12px type, invalid weight/spacing, or inline status color.
    - Both napplets declare complete canonical aliases over unchanged `--nap-theme-*` inputs and use dataset tones with unchanged status text/flow.
    - No feed/profile recovery control/copy/branch or `apps/playground/src/theme.ts` change exists.
    - Focused visual/theme/NIP guards and both napplet builds exit 0; the pinned changed-file AI-slop result is 100/100 before commit.
  </acceptance_criteria>
  <done>Feed and profile consume the canonical readable semantic system while retaining their exact existing runtime behavior.</done>
  <commit_checkpoint>After green verification, stage only the five listed files and commit `feat(107-04): align feed and profile semantic styling` with the required Codex co-author trailer; continue only from a clean task-owned diff.</commit_checkpoint>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove existing feed/profile states and live theme consumption in real iframes</name>
  <read_first>
    - tests/e2e/theme-broadcast.spec.ts — existing real profile iframe, theme broadcast, and computed-style proof
    - apps/playground/napplets/feed/index.html — Task 1 selectors/state containers to measure
    - apps/playground/napplets/profile-viewer/index.html — Task 1 selectors/state containers to measure
    - tests/e2e/playground-profile-intent.spec.ts — deterministic feed/profile loading and populated fixture controls to reuse without changing behavior
    - tests/e2e/profile-open.spec.ts — profile empty/populated/partial and long-data fixture analogs
    - .planning/phases/107-readable-responsive-paja-system/107-UI-SPEC.md — eight feed/profile backstops and computed type/overflow contract
  </read_first>
  <files>tests/e2e/theme-broadcast.spec.ts, apps/playground/napplets/feed/index.html, apps/playground/napplets/profile-viewer/index.html</files>
  <behavior>
    - Real theme broadcast changes semantic computed colors through existing NAP-THEME variables in both feed and profile, with no new payload field/message.
    - Empty/loading/error/populated/partial and feed zero/one/many states remain distinct/readable at desktop and 375px phone width.
    - Every routine selector computes to >=12px and an approved size/weight.
    - Long names/pubkeys/metadata/content and missing media stay inside each iframe; no iframe or host page horizontal overflow is introduced.
  </behavior>
  <action>Add held-out browser assertions before any styling correction. Extend the existing real theme broadcast test to load both feed and profile iframes through current host paths and exercise deterministic existing empty, loading, error, populated, partial, zero/one/many, missing-media, and long-text fixtures at 1280x720 and 375x812. Read computed font size/weight/color for status, author/time/content, title/name/pubkey/about/detail; assert the 12px floor and existing theme changes through semantic aliases. Measure each iframe document scrollWidth/clientWidth and visible content bounds; preserve full accessible values where visual truncation occurs. Assert no `Retry`, `Reconnect`, or new recovery action/copy appears. If a measurement fails, adjust only the two HTML presentation files; do not edit TypeScript behavior, host theme producer, or NAP-THEME surface.</action>
  <verify>
    <automated>corepack pnpm exec playwright test tests/e2e/theme-broadcast.spec.ts --project=chromium --workers=1 &amp;&amp; corepack pnpm exec playwright test tests/e2e/paja-single-window.spec.ts tests/e2e/paja-runtime-pointer.spec.ts --project=chromium --workers=1 &amp;&amp; corepack pnpm dlx aislop@0.12.0 scan --changes --base HEAD</automated>
  </verify>
  <acceptance_criteria>
    - Existing theme broadcast changes feed/profile computed semantic colors through unchanged NAP-THEME behavior.
    - Each of the eight UI backstops in this plan has an explicit passing browser assertion at applicable desktop/phone fixtures.
    - Routine computed text is >=12px; long/partial/missing-media content remains bounded and readable without host-level overflow.
    - No feed/profile recovery behavior or copy is added.
    - Theme broadcast and both Paja focused Playwright commands exit 0; the pinned changed-file AI-slop result is 100/100 before commit.
  </acceptance_criteria>
  <done>All scoped surfaces share one browser-proven readable semantic system, with feed/profile behavior still reserved for Phase 108.</done>
  <commit_checkpoint>After green verification, stage only the three listed files and commit `test(107-04): prove feed and profile visual states` with the required Codex co-author trailer; leave no task-owned uncommitted changes.</commit_checkpoint>
</task>

</tasks>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One coherent visual system across Paja/feed/profile | 107-04 | COVERED | Local alias consumption completes cross-surface goal. |
| REQ | VIS-01/VIS-02/VIS-03 | Palette/type/spacing across feed/profile | 107-04 | COVERED | Static + computed browser proof. |
| RESEARCH | Pattern 4 / scoped tone | Declaration-only tokens and dataset tone | 107-04 | COVERED | Exact files and guards. |
| CONTEXT | D-11/D-12 | Semantic-only feed/profile change; recovery deferred | 107-04 | COVERED | Negative and positive acceptance. |
| UI-SPEC | 8 feed/profile considerations | All eight backstop rows | 107-04 | COVERED | Every row lifted above with explicit evidence. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Existing host theme -> sandboxed napplet CSS | Host-controlled semantic values enter feed/profile through the existing NAP-THEME channel. |
| Relay/profile/error data -> napplet DOM | Untrusted text and long values enter existing feed/profile renderers. |
| Phase 107 styling -> Phase 108 behavior boundary | Presentation edits must not silently alter recovery/runtime behavior. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-107-04-01 | Tampering | Theme consumption | high | mitigate | Alias existing `--nap-theme-*` locally only; conformance/browser guards prove no producer, field, or message change. |
| T-107-04-02 | Elevation of Privilege | Feed/profile status rendering | high | mitigate | Preserve `textContent`; dataset carries only local tone enum and CSS owns presentation. |
| T-107-04-03 | Spoofing | Semantic status color | medium | mitigate | Visible text remains authoritative and unchanged; neutral/success/danger colors never stand alone. |
| T-107-04-04 | Denial of Service | Long relay/profile content | medium | mitigate | Preserve min-width/overflow-wrap/media bounds and browser-test iframe/host scroll widths. |
| T-107-04-05 | Information Disclosure | Browser fixtures | low | accept | Deterministic synthetic public keys/profile content only; no signer or secret material enters evidence. |
</threat_model>

<verification>
- Run both task commands.
- Run `corepack pnpm exec vitest run tests/unit/phase-107-visual-system.test.ts tests/unit/identity-theme-conformance-guard.test.ts tests/unit/nip5d-conformance-guard.test.ts`.
- Run both demo builds plus `corepack pnpm --filter @kehto/playground build`.
- Run `git diff --check`.
</verification>

<success_criteria>
- Paja/feed/profile share exact semantic names and allowed type/spacing values.
- Feed/profile computed styles and state shapes pass in real themed iframes.
- No recovery behavior or NAP-THEME change leaks into Phase 107.
- All 46 UI considerations across Plans 107-03/04 are represented with passing explicit/backstop evidence.
</success_criteria>

<output>
Create `.planning/phases/107-readable-responsive-paja-system/107-04-SUMMARY.md` when done.
</output>
