---
phase: 107-readable-responsive-paja-system
plan: 06
type: execute
wave: 6
depends_on: [107-01, 107-02, 107-03, 107-04, 107-05]
files_modified:
  - .planning/phases/107-readable-responsive-paja-system/107-RESEARCH.md
  - packages/paja/src/browser-runtime-tabs.ts
  - packages/paja/src/browser-runtime-tabs.test.ts
  - packages/paja/src/browser-host.ts
  - packages/paja/src/options.ts
  - packages/paja/src/options.test.ts
  - packages/paja/src/browser-target-frame.ts
  - packages/paja/src/server.ts
  - packages/paja/src/server.test.ts
  - packages/paja/src/browser-host-runtime.ts
  - packages/paja/src/browser-host.test.ts
  - tests/e2e/paja-runtime-pointer.spec.ts
  - tests/e2e/paja-single-window.spec.ts
autonomous: true
gap_closure: true
requirements: [PAJA-03, PAJA-04]
must_haves:
  truths:
    - "D-08/D-09/D-10: Every current runtime-tab generation and complete external-target attempt settles ready or into the existing host-owned error surface within config.runtime.readyTimeoutMs; Retry target is then enabled."
    - "PAJA-03 idempotency/concurrency: a deadline, iframe error, fetch rejection, and stale promise settlement can race, but exactly one current-generation failure is projected and logged."
    - "PAJA-04 boundary/precision: timeout handling reuses reloadActiveRuntimeTab() or the existing external state.reload()/navigateFrame path and adds no NAP message, capability, routing rule, or alternate loader."
    - "Runtime-tab deadlines are armed per generation and cleared on trusted ready, error, reload/replacement, close, and destroy; stale callbacks cannot mutate a newer generation."
    - "External-attempt deadlines cover the browser proxy fetch, server outbound target fetch, iframe injection, and current-source shell.ready wait; outbound fetches receive an AbortSignal."
    - "A timed-out registered window/session is destroyed and unregistered before recovery appears; a later stale shell.ready or fetch settlement cannot reopen it."
    - "D-01/D-05/D-06/D-07: Existing product/target/tab context, responsive composition, literal diagnostics, focus rules, and phone no-overflow behavior remain unchanged while timeout errors use the current semantic recovery surface."
    - "D-11/D-12: Feed/profile behavior remains untouched; recovery actions and state transitions there remain Phase 108 scope."
    - "No identity, authorization, schema, public protocol, sandbox, verified-byte, or package-version model changes are introduced."
    - "Before protocol-adjacent edits, exact current NAP-SHELL, NAP-THEME, and NIP-5D authority refs are rechecked and recorded; a moved draft alone is not a blocker."
  artifacts:
    - path: "packages/paja/src/browser-runtime-tabs.ts"
      provides: "Generation-scoped readiness deadline, stale safety, failed-session cleanup, and retryable error settlement"
    - path: "packages/paja/src/browser-host-runtime.ts"
      provides: "Whole external-attempt deadline and AbortController lifecycle"
    - path: "packages/paja/src/browser-target-frame.ts"
      provides: "AbortSignal propagation into the browser target proxy fetch"
    - path: "packages/paja/src/server.ts"
      provides: "Bounded server-side outbound target fetch using the serialized ready timeout"
    - path: "tests/e2e/paja-runtime-pointer.spec.ts"
      provides: "Verified iframe never-ready timeout, stale-generation, retry, and preserved provenance proof"
    - path: "tests/e2e/paja-single-window.spec.ts"
      provides: "Never-settling external fetch timeout and successful retry proof on the real host path"
  key_links:
    - from: "packages/paja/src/browser-runtime-tabs.ts"
      to: "packages/paja/src/browser-host.ts"
      via: "per-generation deadline clears only after trusted current-source markRuntimeTabReady succeeds"
      pattern: "readyTimeoutMs|shell\.ready|clear.*Deadline"
    - from: "packages/paja/src/browser-host-runtime.ts"
      to: "packages/paja/src/browser-target-frame.ts"
      via: "one external attempt owns a deadline and AbortSignal passed through navigateFrame"
      pattern: "AbortController|signal|navigateFrame"
    - from: "packages/paja/src/server.ts"
      to: "target URL"
      via: "server proxy bounds its outbound fetch with hostConfig.runtime.readyTimeoutMs"
      pattern: "readyTimeoutMs|AbortSignal|fetch"
  prohibitions:
    - statement: "Phase 107 timeout closure must not add, remove, reshape, or reinterpret any NAP message, capability, routing rule, lifecycle contract, sandbox permission, verified-byte boundary, or package version."
      status: unverified
      verification: judgment
    - statement: "Phone presentation must retain Paja identity/current target and reachable host content with no page-level horizontal scrolling."
      status: unverified
      verification: judgment
    - statement: "Feed/profile recovery behavior remains exclusively Phase 108 work."
      status: unverified
      verification: judgment
---

<objective>
Close the incomplete failure-settlement edge for runtime-pointer and external Paja targets.

Purpose: Ensure a verified iframe that never sends shell.ready and an external target fetch that never settles both reach the existing stable recovery UI within the configured timeout, without changing protocol or presentation contracts.
Output: Generation-scoped runtime-tab deadlines, whole-attempt external cancellation, bounded server fetches, failed-session cleanup, and TDD/unit/Chromium proof.

Scope cohesion: The 12 implementation/test files form one bounded timeout lifecycle: browser runtime ownership starts and settles the deadline, proxy and server boundaries propagate the same cancellation budget, and paired unit/Chromium tests prove cleanup and stale-generation safety. Splitting those seams would leave an intermediate unbounded path or duplicate timeout authority; the additional RESEARCH.md edit is heading-only planning metadata, not implementation scope.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/107-readable-responsive-paja-system/107-CONTEXT.md
@.planning/phases/107-readable-responsive-paja-system/107-RESEARCH.md
@.planning/phases/107-readable-responsive-paja-system/107-PATTERNS.md
@.planning/phases/107-readable-responsive-paja-system/107-VALIDATION.md
@.planning/phases/107-readable-responsive-paja-system/107-UI-SPEC.md
@.planning/phases/107-readable-responsive-paja-system/107-VERIFICATION.md
@.planning/phases/107-readable-responsive-paja-system/107-01-SUMMARY.md
@.planning/phases/107-readable-responsive-paja-system/107-02-SUMMARY.md
@.planning/phases/107-readable-responsive-paja-system/107-05-SUMMARY.md

<interfaces>
- `PajaHostConfig.runtime.readyTimeoutMs: number` is the existing serialized timeout authority. Do not add a second setting or API matrix.
- `startRuntimeTabNavigation(tab, state, context)` owns one runtime-tab generation from verified srcdoc navigation until trusted current-source `shell.ready`.
- `destroyRuntimeTabSession(tab, context)` already destroys runtime/session/origin ownership for reload, replacement, and close; deadline cleanup must compose with this path.
- `startExternalFrameNavigation(state, context)` and `settleExternalNavigationFailure(...)` are the existing external single-flight settlement seam.
- `navigateFrame(...)` remains authoritative for register-before-srcdoc, verified bytes, CSP/prelude ordering, and sandbox behavior; only optional AbortSignal transport may be added.
- `startPajaServer()` serves `/__kehto/target.html`; its outbound fetch must use the already serialized ready timeout.
</interfaces>
</context>

## Decision traceability

- D-01: Approved UI-SPEC remains exact presentation authority.
- D-05/D-06/D-07: Desktop/phone identity, target, controls, stage, footer, and overflow contracts remain unchanged.
- D-08/D-09: Failure remains stable host DOM with exact copy, focus, actions, and literal diagnostics.
- D-10: Existing verified loaders, current-generation/source checks, single-flight retry, and zero protocol additions remain authoritative.
- D-11/D-12: Feed/profile remain semantic-only in Phase 107; no recovery behavior enters this plan.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Time out one never-ready verified runtime tab end to end</name>
  <read_first>
    - packages/paja/src/browser-runtime-tabs.ts — PajaRuntimeTab fields, startRuntimeTabNavigation, handleRuntimeTabError, destroyRuntimeTabSession, and generation guards
    - packages/paja/src/browser-host.ts — trusted current-source shell.ready branch and runtime-tab ready projection
    - packages/paja/src/browser-runtime-tabs.test.ts — current internal runtime-tab contracts
    - tests/e2e/paja-runtime-pointer.spec.ts — real signed Relay/Blossom fixture, held shell.ready helper, retry, sandbox, CSP, source, and session assertions
    - .planning/phases/107-readable-responsive-paja-system/107-RESEARCH.md — resolved open-question heading and unchanged explicit resolution
    - .planning/phases/107-readable-responsive-paja-system/107-VERIFICATION.md — exact never-ready gap and required cleanup lifecycle
    - .planning/phases/107-readable-responsive-paja-system/107-UI-SPEC.md — D-08/D-09/D-10 recovery, focus, busy, and context invariants
  </read_first>
  <files>.planning/phases/107-readable-responsive-paja-system/107-RESEARCH.md, packages/paja/src/browser-runtime-tabs.ts, packages/paja/src/browser-runtime-tabs.test.ts, packages/paja/src/browser-host.ts, tests/e2e/paja-runtime-pointer.spec.ts</files>
  <behavior>
    - RED: a verified runtime iframe whose real injected shell.ready is held past a 100ms configured deadline remains loading with Retry disabled.
    - RED: timeout cleanup destroys/unregisters the failed window/session and a later release of the held stale shell.ready cannot mark that generation ready.
    - GREEN: the same stable tab/target context shows the existing error surface, Retry is enabled, and Retry creates one new generation that reaches ready through the existing verified loader.
    - GREEN: ready, iframe error, reload/replacement, close, and destroy each clear their generation deadline; a prior callback cannot affect a current generation.
  </behavior>
  <action>Before RED work, change only the `107-RESEARCH.md` heading `## Open Questions` to `## Open Questions (RESOLVED)`; retain the adjacent explicit no-blocking-question resolution and all other research substance byte-for-byte. Then perform the repository-mandated read-only authority check: resolve the current exact `napplet/naps` master and NIP-5D PR 2303 head, read NAP-SHELL/NAP-THEME and verified-byte/srcdoc/sandbox/source clauses at those immutable refs, compare them with the refs recorded in 107-VERIFICATION.md, and record the exact result in 107-06-SUMMARY.md. Stop only for a semantic contradiction or missing decision. Then add a failing fake-timer unit contract and a failing Chromium tracer using the real signed pointer/verified bytes while holding the real injected shell.ready. Per D-08/D-09/D-10, give each `PajaRuntimeTab` a private generation-scoped readiness deadline, arm it when navigation for that generation starts, and route expiry through the existing host error surface. Centralize current-generation settlement so timer, iframe error, promise rejection, and ready races are idempotent. On failure, capture the old window ID for diagnostics, clear the deadline/listener/focus intent, destroy and unregister runtime/session/origin/readiness ownership, null failed tab/window ownership, retain the tab/pointer and responsive host context, and re-enable its existing Retry target action. Clear the deadline only after `markRuntimeTabReady` accepts the registered current source. Reuse `reloadActiveRuntimeTab()` for retry. Add no protocol message, alternate navigation path, iframe, public barrel export, dependency, sandbox permission, or verified-byte mutation.</action>
  <verify>
    <automated>corepack pnpm exec vitest run packages/paja/src/browser-runtime-tabs.test.ts packages/paja/src/browser-host.test.ts tests/unit/nip5d-conformance-guard.test.ts &amp;&amp; corepack pnpm exec playwright test tests/e2e/paja-runtime-pointer.spec.ts --project=chromium --workers=1 --grep "never-ready|recovers resolver and active-frame failures"</automated>
  </verify>
  <done>A never-ready verified runtime generation settles once into retryable host recovery, its failed session is gone, stale readiness is inert, and Retry reaches one new verified ready generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound the proxy and server target fetches with the existing timeout</name>
  <read_first>
    - packages/paja/src/options.ts — PajaHostConfig.runtime.readyTimeoutMs and createPajaRuntimeHostConfig default
    - packages/paja/src/options.test.ts — runtime config serialization contract
    - packages/paja/src/browser-target-frame.ts — external target proxy fetch and navigateFrame signature
    - packages/paja/src/server.ts — /__kehto/target.html handler and outbound target fetch
    - packages/paja/src/server.test.ts — local proxy endpoint fixture/test conventions
    - .planning/phases/107-readable-responsive-paja-system/107-VERIFICATION.md — complete external attempt and server-fetch gap
  </read_first>
  <files>packages/paja/src/options.ts, packages/paja/src/options.test.ts, packages/paja/src/browser-target-frame.ts, packages/paja/src/server.ts, packages/paja/src/server.test.ts</files>
  <behavior>
    - Server fetch of an upstream target that never responds aborts at readyTimeoutMs and returns a stable non-success diagnostic instead of holding the proxy response open.
    - Browser proxy fetch accepts an AbortSignal from its owning external attempt and reports abort/timeout through the existing rejection path.
    - Runtime-pointer host config carries a usable readiness timeout value through the existing field; no new public option or config key is introduced.
  </behavior>
  <action>Write failing options and server tests first using a local upstream response that intentionally remains open. Keep `PajaHostConfig.runtime.readyTimeoutMs` as the sole timeout authority. Replace the runtime-pointer helper's former sentinel value with the existing default timeout constant so the new per-tab deadline is usable in real static hosts without changing the config shape. Extend `navigateFrame` with an optional AbortSignal passed only to the external proxy fetch; runtime-pointer verified-byte injection remains byte-for-byte on its existing branch. Pass the serialized timeout into the `/__kehto/target.html` server handler and bound its outbound fetch with AbortSignal/timeout cleanup, returning a stable timeout diagnostic through the existing 502 handler. Clear server timer/controller resources on response, error, request replacement, and server shutdown. Do not change target validation, headers, base injection, CSP, namespace prelude, sandboxing, capability resolution, or any NAP surface.</action>
  <verify>
    <automated>corepack pnpm exec vitest run packages/paja/src/options.test.ts packages/paja/src/server.test.ts packages/paja/src/browser-host.test.ts tests/unit/nip5d-conformance-guard.test.ts &amp;&amp; corepack pnpm --filter @kehto/paja type-check</automated>
  </verify>
  <done>Both browser proxy and server outbound target fetches are abortable and bounded by the existing serialized timeout, with no new config or protocol surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cover the complete external attempt and all cancellation paths</name>
  <read_first>
    - packages/paja/src/browser-host-runtime.ts — current post-fetch readiness timer and external failure settlement helper
    - packages/paja/src/browser-host.ts — PajaBrowserStateContext, external ready branch, reload, error handler, and pagehide lifecycle
    - packages/paja/src/browser-host.test.ts — source/session/provenance and external recovery guards
    - tests/e2e/paja-single-window.spec.ts — held upstream failure fixture and missing-shell.ready recovery case
    - packages/paja/src/browser-target-frame.ts — Task 2 AbortSignal-enabled navigateFrame contract
    - packages/paja/src/server.ts — Task 2 bounded target proxy behavior
  </read_first>
  <files>packages/paja/src/browser-host-runtime.ts, packages/paja/src/browser-host.ts, packages/paja/src/browser-host.test.ts, tests/e2e/paja-single-window.spec.ts</files>
  <behavior>
    - RED: an upstream external target fetch held forever exceeds readyTimeoutMs and still leaves Paja loading with Retry disabled.
    - GREEN: one deadline starts before navigateFrame, covers fetch plus handshake, aborts the active fetch on expiry, unregisters any failed session, and shows the existing retryable error.
    - GREEN: ready, error, retry/reload, replacement, and host destroy clear the deadline/controller; late fetch, timer, iframe error, or shell.ready settlement is ignored.
    - GREEN: after releasing the fixture, Retry uses the same external loader, reaches Target ready, focuses the iframe only for that user retry, and preserves current UI/context.
  </behavior>
  <action>Add the failing never-settling upstream Chromium case before changing controller code. Move the external deadline start to the beginning of the current attempt so it covers the browser proxy request, bounded server fetch, injection, and trusted shell.ready wait as one budget. Store one AbortController with the current external generation, pass its signal through Task 2's navigateFrame seam, and use one idempotent settlement helper for timeout, abort rejection, iframe error, and other failures. Per D-08/D-09/D-10, timeout must clear attempt/focus/deadline/controller state, destroy and unregister any current failed window/session/origin/readiness entry, log one inert diagnostic, preserve target/context and the stable semantic surface, and enable Retry. Trusted ready clears deadline/controller without aborting the ready frame. Reload/replacement and host pagehide/destroy cancel prior resources before later generations can start. Extend unit guards for exact ownership and Chromium assertions for one request/attempt, late-settlement safety, Retry re-entry, focus, one iframe/session, sandbox, and current-source readiness. Preserve all Phase 107 phone/context assertions and do not touch feed/profile behavior.</action>
  <verify>
    <automated>corepack pnpm exec vitest run packages/paja/src/browser-host.test.ts packages/paja/src/browser-runtime-tabs.test.ts packages/paja/src/server.test.ts packages/paja/src/options.test.ts tests/unit/nip5d-conformance-guard.test.ts &amp;&amp; corepack pnpm exec playwright test tests/e2e/paja-single-window.spec.ts --project=chromium --workers=1 --grep "never-settling external target fetch|settles a missing external shell.ready handshake|recovers an external target"</automated>
  </verify>
  <done>The complete external attempt is bounded from first fetch through trusted readiness, every termination path cleans resources once, and user retry recovers through the unchanged loader.</done>
</task>

</tasks>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Usable Paja recovery across target-load failure states while context remains visible | 107-06 | COVERED | Both forever-loading edges become host recovery. |
| REQ | PAJA-03 | Failure always exposes explanation, diagnostics, return, and Retry | 107-06 | COVERED | Never-ready and never-settling fetch cases added. |
| REQ | PAJA-04 | Retry uses existing verified path and preserves security boundary | 107-06 | COVERED | Same reload routes plus conformance/provenance assertions. |
| RESEARCH | Architecture/Pattern 1 | Host surface surrounds existing loaders; source/session ownership remains in current tiers | 107-06 | COVERED | Deadline orchestration is browser/server-local; no parallel loader. |
| RESEARCH | Security | Generation/source checks, unregister/destroy, sandbox, verified bytes, and one shell lifecycle | 107-06 | COVERED | Timer races and failed sessions are explicitly tested. |
| CONTEXT | D-01/D-05/D-06/D-07 | Exact UI and desktop/phone context remain authoritative | 107-06 | COVERED | Preserved and rerun, not redesigned. |
| CONTEXT | D-08/D-09/D-10 | Host DOM recovery, exact interaction, existing verified retry | 107-06 | COVERED | Direct task actions and done criteria. |
| CONTEXT | D-11/D-12 | Feed/profile semantic-only boundary | 107-06 | COVERED | Explicit no-touch boundary. |
| VERIFICATION | Gap 1 | Runtime ready and full external attempt are unbounded | 107-06 | COVERED | Root-cause fix, not symptom copy. |
</source_audit>

<edge_probe_coverage>
- PAJA-03 idempotency/concurrency: Task 1 and Task 3 race timer/error/rejection/ready and prove one settlement plus one retry.
- PAJA-04 boundary/precision/idempotency/concurrency: both tasks preserve exact loader, source, session, sandbox, CSP, verified bytes, and one current generation.
- VIS-01 empty/encoding, VIS-02 adjacency/empty/encoding/ordering, VIS-03 empty/encoding, PAJA-01 unclassified, and PAJA-02 unclassified do not contribute new deterministic cases to timeout orchestration; existing Phase 107 static/browser tests remain the authority and are rerun by 107-07.
- No new external API, identity, authorization, or schema contribution exists, so an API coverage matrix is non-applicable.
</edge_probe_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Sandboxed verified iframe -> host readiness | Only the registered current source may clear a generation deadline and become ready. |
| Browser host -> local target proxy | A hostile or unavailable target can hold a privileged host request open. |
| Local Paja server -> external target | Outbound fetch crosses into untrusted network-controlled availability and content. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-107-06-01 | Denial of Service | runtime-tab shell.ready wait | high | mitigate | Per-generation readyTimeoutMs deadline; current-generation idempotent settlement; cleanup on ready/error/reload/close/destroy; unit and Chromium proof. |
| T-107-06-02 | Spoofing / Tampering | stale timer or shell.ready | high | mitigate | Preserve registered MessageEvent.source/window ID checks and reject callbacks whose tab/external generation is no longer current. |
| T-107-06-03 | Denial of Service | browser and server target fetch | high | mitigate | Whole-attempt AbortController plus server outbound AbortSignal bounded by the existing timeout; cleanup on every terminal path. |
| T-107-06-04 | Elevation of Privilege | verified frame replacement | high | mitigate | Preserve signature/hash verification, CSP-before-bootstrap, register-before-srcdoc, allow-scripts without same-origin, and current-source readiness guards. |
| T-107-06-05 | Repudiation | timeout diagnostics | low | accept | Development-only host already records one literal message-log error and visible lifecycle status; durable audit logging is outside this local tool. |
</threat_model>

<verification>
- Run all three task commands after their RED/GREEN cycles.
- Run `corepack pnpm --filter @kehto/paja build` and `corepack pnpm --filter @kehto/paja type-check`.
- Run focused Paja unit/conformance coverage: `corepack pnpm exec vitest run packages/paja/src/options.test.ts packages/paja/src/server.test.ts packages/paja/src/browser-target-surface.test.ts packages/paja/src/browser-host.test.ts packages/paja/src/browser-runtime-tabs.test.ts tests/unit/nip5d-conformance-guard.test.ts tests/unit/select-e2e-tests.test.ts`.
- Run Phase-owned Chromium coverage from both host paths; the inherited upload-domain defect 28 remains explicitly outside this plan and must not be repaired or masked here.
- Run `git diff --check` before summary creation.
</verification>

<success_criteria>
- Runtime-tab never-ready and external never-settling-fetch cases reach retryable host recovery within readyTimeoutMs.
- Timers, AbortControllers, listeners, registrations, sessions, and focus intent are cleaned exactly once across ready/error/reload/replacement/close/destroy paths.
- Late/stale callbacks cannot affect a newer generation or revive a failed session.
- Retry preserves current target/context and re-enters the existing verified loader with one current iframe/session.
- NAP/capability/routing/sandbox/verified-byte/package behavior remains unchanged and supported by current-ref checks plus conformance tests.
</success_criteria>

<output>
Create `.planning/phases/107-readable-responsive-paja-system/107-06-SUMMARY.md` when done.
</output>
