---
phase: 102-paja-standard-nap-social-poc
plan: 04
type: execute
wave: 3
depends_on: [102-03]
files_modified:
  - packages/paja/README.md
  - docs/packages/paja.md
  - docs/how-tos/paja-local-authoring.md
  - .changeset/paja-standard-nap-social-cache.md
autonomous: true
requirements: [PAJA-01, PAJA-02, PAJA-03]
must_haves:
  truths:
    - "Paja users can understand that standard identity.getFollows and outbox.query expose the social tracer while cache state remains host-private, account-scoped, memory-only, and subject to normal query degradation."
    - "Released @kehto/paja behavior has a changeset, synchronized package documentation, exact protocol authority/drift language, and passing browser-inclusive project gates."
    - "Phase 102 reports deterministic Paja proof only; the real Paja smoke remains after Phase 103 and downstream source work remains unmodified."
  artifacts:
    - path: packages/paja/README.md
      provides: "Consumer-facing Paja behavior and standard identity/outbox social-cache boundary."
    - path: docs/packages/paja.md
      provides: "Package reference documentation for Paja social-cache behavior, protocol authority, preserved target-CORS guidance, and an exact 0.8.2 manifest row."
    - path: docs/how-tos/paja-local-authoring.md
      provides: "Authoring guidance for relay-backed follows and ordinary kind-0 queries."
    - path: .changeset/paja-standard-nap-social-cache.md
      provides: "Release record for the @kehto/paja behavior change."
  key_links:
    - from: packages/paja/src/browser-social-cache.ts
      to: packages/paja/README.md
      via: "documentation describes the same private account-scoped cache and standard identity/outbox wire path"
    - from: packages/paja/README.md
      to: docs/packages/paja.md
      via: "package README and published package guide use matching authority and scope language"
    - from: .changeset/paja-standard-nap-social-cache.md
      to: packages/paja/package.json
      via: "the sole shipped package output change receives the release version record"
  prohibitions:
    - requirement_id: PAJA-02
      category: values
      status: unverified
      flagged: true
      verification: judgment
      statement: "Documentation must not present Paja’s internal social cache as a napplet-owned capability or durable storage facility."
      reason: "The host is the authority and the PoC cache is memory-only."
    - requirement_id: PAJA-03
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "Documentation and release evidence must not claim that cached data makes an incomplete or errored OUTBOX query complete."
      reason: "Query-wide degradation remains an observable standard result."
    - requirement_id: PAJA-03
      category: values
      status: unverified
      flagged: true
      verification: judgment
      statement: "Phase evidence must not claim real downstream smoke or current-master OUTBOX conformance."
      reason: "The real composition is sequenced after Phase 103 and OUTBOX uses a documented pinned draft."
---

<objective>
Publish the Paja social-cache boundary accurately and run the complete browser-inclusive release gate for the finished Phase 102 implementation.

Purpose: Make the shipped behavior reviewable without exposing a new napplet surface, and prove it through every required repository gate before Phase 103 begins.
Output: Updated Paja documentation, one @kehto/paja changeset, full verification evidence, and an explicit protocol/upstream-drift closeout record.

Per D-03, document standard NAP mediation only. Per D-04 through D-06, preserve focused contribution hygiene. Planning integrates upstream/main before `/gsd-execute-phase`; isolated executors begin at the orchestrator-provided HEAD. A later focused upstream contribution selects only Phase 102 implementation, test, documentation, and changeset commits onto a clean upstream/main base; this plan does not perform that repository operation. Per D-11 and D-12, this ends with deterministic Paja proof and does not enter downstream implementation. Per D-15 and D-16, state the exact identity authority and draft OUTBOX/type authority without overstating current-master status.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md
@.planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md
@.planning/phases/102-paja-standard-nap-social-poc/102-PATTERNS.md
@.planning/phases/102-paja-standard-nap-social-poc/102-VALIDATION.md
@.planning/phases/102-paja-standard-nap-social-poc/102-03-SUMMARY.md
@packages/paja/README.md
@docs/packages/paja.md
@docs/how-tos/paja-local-authoring.md
@packages/paja/package.json
@AGENTS.md
</context>

## Artifacts this plan produces

1. `packages/paja/README.md` — consumer-facing standard identity/outbox social-cache boundary that retains the upstream target-CORS authoring guidance.
2. `docs/packages/paja.md` — package reference with the repaired `0.8.2` manifest row, preserved target-CORS diagnostic guidance, and social-cache protocol boundary.
3. `docs/how-tos/paja-local-authoring.md` — local-authoring guidance that retains the opaque-origin CORS requirement and explains standard social behavior without a custom Paja API.
4. `.changeset/paja-standard-nap-social-cache.md` — the single feature changeset for the shipped `@kehto/paja` social-cache behavior; the documentation version-row repair has no changeset of its own.

<tasks>

<task type="auto">
  <name>Task 1: Document the standard Paja social boundary and add its release record</name>
  <precondition>The completed Plan 03 cache/host tests pass from the orchestrator-supplied cwd, which already includes the merged upstream/main CORS baseline.</precondition>
  <read_first>
    - .planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md (D-03 through D-06, D-11 through D-12, D-15, and D-16)
    - .planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md (authority status, private-cache boundary, and out-of-scope list)
    - packages/paja/README.md (current relay/identity and upload boundary text)
    - docs/packages/paja.md (NAP and Service Parity section)
    - docs/how-tos/paja-local-authoring.md (Verify the Runtime Surface section)
    - packages/paja/package.json (version and shipped package identity)
  </read_first>
  <files>packages/paja/README.md, docs/packages/paja.md, docs/how-tos/paja-local-authoring.md, .changeset/paja-standard-nap-social-cache.md</files>
  <action>From the orchestrator-supplied cwd, update the README, package guide, and local-authoring guide together. Describe that a signed-in napplet uses existing `identity.getPublicKey`, `identity.getFollows`, and normal kind-0 `outbox.query`; Paja privately validates a replacement kind-3 contact list, warms followed profile records through its established router, and keeps cache state active-account-scoped and memory-only. State that captured-key request correlation and generation-safe background writes prevent stale account data from becoming active. State that a normal query can include matching cached RelayEventResult values but retains query-wide `incomplete` and `error` from the base router, and that profile winner selection, pagination, follow mutation, durable cache controls, and per-author completeness are outside this behavior. Keep generic simulation cache mode separate from this required internal snapshot. Identify NAP-IDENTITY `6461e4b37c29dc09a20dff35d9515889c4433874` as master-identical for the phase; identify NAP-OUTBOX `4589a8f9a16d8aa29b3740e2b3b0cdca11e0976e` plus installed `@napplet/nap@0.28.0` types as the PoC implementation contract because the current master path is absent. Do not claim current-master OUTBOX conformity. Keep Blossom material unchanged because it belongs to Phase 103. First repair `docs/packages/paja.md`'s exact Manifest Facts version row from `0.8.1` to `0.8.2` so it matches the upstream package manifest and `scripts/audit-docs.mjs`; classify that row correction as an upstream-baseline documentation repair, not shipped social-cache behavior. Preserve the merged target-CORS paragraphs and examples in the README, package guide, and local-authoring guide, including the opaque `Origin: null` requirement, the `GET /__kehto/target-cors.json` diagnostic, and the `paja.target.cors.error` message-log remedy; do not overwrite, relocate, or duplicate them while adding social-cache material. Add `.changeset/paja-standard-nap-social-cache.md` with a minor `@kehto/paja` entry because the shipped host behavior gains the social tracer; do not add changesets for the baseline documentation repair or test-only service files. Do not add a custom napplet service, direct networking/key guidance, real downstream smoke instructions, or any Writer source, test, fixture, smoke, or documentation change.</action>
  <verify>
    <automated>test -f .changeset/paja-standard-nap-social-cache.md && rg -n 'identity.getFollows|outbox.query|active-account|memory|incomplete|error|6461e4b37c29dc09a20dff35d9515889c4433874|4589a8f9a16d8aa29b3740e2b3b0cdca11e0976e|@napplet/nap@0.28.0' packages/paja/README.md docs/packages/paja.md docs/how-tos/paja-local-authoring.md && rg -n '\| Version \| `0.8.2` \|' docs/packages/paja.md && rg -n 'paja.target.cors.error|Origin: null|target-cors.json' packages/paja/README.md docs/packages/paja.md docs/how-tos/paja-local-authoring.md && rg -n '"@kehto/paja": minor' .changeset/paja-standard-nap-social-cache.md && pnpm docs:check</automated>
  </verify>
  <acceptance_criteria>All three Paja documentation surfaces describe the same standard identity/outbox behavior, private active-account memory cache, base-result degradation semantics, exact NAP authority, and OUTBOX drift limitation. The upstream target-CORS guidance remains once on each existing surface, and `docs/packages/paja.md` has the exact upstream `0.8.2` version row required by the docs audit. A single @kehto/paja minor changeset covers shipped social-cache output, while the baseline version-row repair, Blossom, real downstream smoke, and every Writer source/test/fixture/documentation path remain untouched.</acceptance_criteria>
  <done>The released Paja behavior and its known protocol boundary are accurately documented and versioned for review.</done>
</task>

<task type="auto">
  <name>Task 2: Run complete Phase 102 gates and record the precise closeout</name>
  <precondition>The configured `/usr/bin/chromium` executable remains runnable and Task 1’s Paja documentation plus changeset changes are present in the orchestrator-supplied cwd.</precondition>
  <read_first>
    - AGENTS.md (build, type, unit, browser, documentation, slop, changeset, and contribution gates)
    - .planning/phases/102-paja-standard-nap-social-poc/102-VALIDATION.md (sampling and phase gate)
    - .planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md (browser and protocol authority constraints)
    - package.json (root verification commands)
    - playwright.config.ts (configured Chromium path)
  </read_first>
  <files>None — this task runs verification and records evidence in the execution summary; it must not alter source, test, documentation, configuration, or dependency files.</files>
  <action>From the orchestrator-supplied cwd, run the full required chain after all source, test, documentation, and changeset work is committed: `pnpm build`, `pnpm type-check`, `pnpm test:unit`, `pnpm test:e2e`, `pnpm docs:check`, and `npx --no-install aislop scan -d`; then run `git diff --check`. Do not omit or soften browser proof if the executable fails after Wave 0; stop with the actual failed command/output instead. Verify the execution changes cover only Phase 102 Paja implementation, tests, docs, and its feature changeset, with no Writer work or Phase 103 Blossom implementation. In the execution summary, record the integrated upstream baseline, all gate results, no-new-package result, NAP-IDENTITY master-identical status, NAP-OUTBOX pinned-draft plus `@napplet/nap@0.28.0` status, current-master OUTBOX path absence, repaired `0.8.2` docs row, preserved target-CORS baseline, and the sequencing note that real Paja smoke is after Phase 103. Per D-04 through D-06, make no repository-topology or contribution-selection operation: after execution, the separate focused contribution selects only implementation/test/docs/changeset commits onto a clean upstream/main base; planning artifacts, Graphify output, generated files, unrelated cleanup, and external Writer work remain excluded.</action>
  <verify>
    <automated>test -x /usr/bin/chromium && pnpm build && pnpm type-check && pnpm test:unit && pnpm test:e2e && pnpm docs:check && npx --no-install aislop scan -d && git diff --check</automated>
  </verify>
  <acceptance_criteria>All required build, type, unit, deterministic browser, documentation, and slop gates pass using the configured Chromium executable. Phase 102 execution contains only Paja implementation/test/documentation/changeset work, contains no Writer source, test, fixture, smoke, or documentation path, preserves the integrated CORS baseline, and accurately records the draft OUTBOX drift plus the post-Phase-103 real-smoke sequence.</acceptance_criteria>
  <done>PAJA-01, PAJA-02, and PAJA-03 are fully verified, documented, release-tracked Paja behavior; subsequent Phase 103 work remains out of scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal Paja behavior to consumer documentation | Documentation can overstate cache authority, durability, or query completeness if it diverges from tested code. |
| Source change to released package | A changed @kehto/paja output needs explicit release metadata and synchronized public docs. |
| Phase 102 execution changes to later focused contribution | Scope drift can mix planning, generated output, external work, or the next phase’s Blossom implementation into this concern. |
| Browser test configuration to phase completion | Required deterministic browser evidence remains dependent on the configured Chromium executable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-102-15 | Repudiation/Tampering | protocol and behavior documentation | high | mitigate | Task 1 synchronizes all Paja docs with exact identity/draft OUTBOX authority, cache scope, and preserved degradation semantics. |
| T-102-16 | Tampering | @kehto/paja release metadata | medium | mitigate | Task 1 adds one package-specific changeset; Task 2 validates docs and final diff. |
| T-102-17 | Elevation of Privilege | contribution scope | high | mitigate | Task 2 uses explicit allowed paths and excludes external Writer, planning, generated, and Phase 103 implementation work. |
| T-102-18 | Denial of Service | required browser proof | medium | mitigate | Task 2 reasserts Chromium and blocks on an actual full browser-gate failure. |
| T-102-SC | Tampering | package-manager supply chain | low | accept | Phase 102 adds no dependency; final commands use the locked existing tree and no package add/upgrade action. |

## ASVS L1 Control-to-Task Mapping

| Control | Threat | Owning task | Evidence |
|---------|--------|-------------|----------|
| V5 accurate security/integrity claims | T-102-15 | Task 1 | Exact refs, private-cache scope, and incomplete/error behavior appear in all Paja docs. |
| V1 release integrity | T-102-16 | Tasks 1-2 | Package-specific changeset plus documentation gate. |
| V4 scope and authority control | T-102-17 | Task 2 | Explicit final diff allowlist and contribution exclusions. |
| V8 operational verification | T-102-18 | Task 2 | Full browser-inclusive command chain using the configured executable. |
</threat_model>

<verification>
1. Ensure the Paja README, package guide, and authoring guide agree on follows, kind-0 query behavior, private cache scope, and base partial/error preservation.
2. Verify the changeset applies only to @kehto/paja, while the separate baseline repair makes the Paja package-doc row exactly `0.8.2` before the docs audit.
3. Run the full project chain with Chromium; no browser command may be skipped or substituted.
4. Run diff whitespace and explicit allowlist checks after all commands succeed.
5. Record exact authority/drift, integrated upstream baseline, package/dependency result, repaired docs version row, preserved CORS baseline, gate outputs, and the Phase 103/real-smoke sequence in the summary.
</verification>

<success_criteria>
- PAJA-01 through PAJA-03 are described through the same standard Paja identity/outbox interface that tests exercise.
- @kehto/paja has its required social-cache changeset, while the upstream baseline documentation repair synchronizes `docs/packages/paja.md` to `0.8.2` without a second changeset.
- All required project gates, including Paja browser coverage, pass with the configured Chromium executable.
- The closeout preserves the integrated target-CORS documentation and keeps external Writer work, Phase 103 Blossom source, real downstream smoke, and unrelated artifacts out of Phase 102 execution.
</success_criteria>

<output>
Create `.planning/phases/102-paja-standard-nap-social-poc/102-04-SUMMARY.md` when done.
</output>
