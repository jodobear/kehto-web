---
phase: 102-paja-standard-nap-social-poc
plan: 03
type: execute
wave: 2
depends_on: [102-02]
files_modified:
  - packages/paja/src/browser-social-cache.ts
  - packages/paja/src/browser-social-cache.test.ts
  - packages/paja/src/browser-host.test.ts
autonomous: true
requirements: [PAJA-01, PAJA-02, PAJA-03]
must_haves:
  truths:
    - "Only a verified replaceable kind-3 record for the captured account can define follows; equal timestamps always use the lexicographically lowest event ID."
    - "Invalid or boundary-adjacent p tags cannot enter the follow set, and account A’s late refresh cannot alter account B’s active profile snapshot."
    - "A cache-aware normal query adds only matching cached RelayEventResult values, removes duplicate event IDs, and preserves the base result’s incomplete/error fields exactly."
    - "Paja still exposes the existing identity/outbox host path without a new social service, changed ACL mapping, or direct napplet relay client."
  artifacts:
    - path: packages/paja/src/browser-social-cache.ts
      provides: "Validated replaceable contact selection, generation-safe active snapshots, and exact base-result merge behavior."
    - path: packages/paja/src/browser-social-cache.test.ts
      provides: "Deterministic boundary, ordering, concurrent refresh, cache/base combination, and filter regression tests."
    - path: packages/paja/src/browser-host.test.ts
      provides: "Static host composition guard for private social cache wiring and retained standard service construction."
  key_links:
    - from: packages/paja/src/browser-social-cache.ts
      to: "NIP-01/NIP-02 kind-3 rules"
      via: "verified author-scoped candidate selection by greatest created_at then lexically lowest id, followed by valid p-tag normalization"
    - from: packages/paja/src/browser-social-cache.ts
      to: packages/services/src/outbox-service.ts
      via: "decorated query forwards base incomplete/error unchanged and returns only RelayEventResult values"
    - from: packages/paja/src/browser-host.test.ts
      to: packages/paja/src/browser-adapter.ts
      via: "source guard asserts cache composition around the one existing base router and identity/outbox services"
  prohibitions:
    - requirement_id: PAJA-01
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "Unverified, wrong-author, malformed, or nondeterministically selected contact events must not determine Paja follows."
      reason: "Relay data is untrusted and kind 3 is replaceable."
    - requirement_id: PAJA-02
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "A stale or interrupted background refresh must not overwrite the active account snapshot."
      reason: "Identity-scoped social state must not cross account boundaries."
    - requirement_id: PAJA-03
      category: values
      status: unverified
      flagged: true
      verification: judgment
      statement: "A cache hit must not conceal the base OUTBOX query’s incomplete or error outcome."
      reason: "Those fields describe query-wide transport truth, not cache availability."
    - requirement_id: PAJA-03
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "The cache decorator must not bypass existing authorization, relay routing, signature verification, or subscription lifecycle."
      reason: "Paja owns mediation through the existing identity/outbox service and router chain."
---

<objective>
Expand the proven Paja tracer into deterministic, account-safe social cache behavior and exact standard OUTBOX result preservation.

Purpose: Close the replaceable-event, validation, concurrency, filtering, and partial-result failures most likely to make the standard path unsafe or misleading.
Output: Complete cache edge coverage and a host composition guard around the established service boundaries.

Per D-03 and D-11, preserve the standard Paja host boundary and keep all downstream work outside the phase. Per D-15, follows remain read-only identity data. Per D-16, cache/base queries use the pinned OUTBOX draft/type contract without adding semantics beyond normal event IDs, filters, incomplete, and error behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md
@.planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md
@.planning/phases/102-paja-standard-nap-social-poc/102-PATTERNS.md
@.planning/phases/102-paja-standard-nap-social-poc/102-VALIDATION.md
@.planning/phases/102-paja-standard-nap-social-poc/102-02-SUMMARY.md
@packages/paja/src/browser-social-cache.ts
@packages/paja/src/browser-social-cache.test.ts
@packages/paja/src/browser-host.test.ts
@packages/paja/src/browser-relay-runtime.ts
@packages/services/src/outbox-service.ts
@packages/services/src/outbox-service.test.ts
@packages/acl/src/resolve.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make contact selection and active-account warm behavior deterministic</name>
  <precondition>The Plan 02 tracer is green in `/workspace/projects/kehto/paja-social-cache-102`, including its focused unit and deterministic browser commands.</precondition>
  <read_first>
    - .planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md (D-03, D-11, and D-15)
    - .planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md (Patterns 1 and 3; pitfalls 1 and 3)
    - packages/paja/src/browser-social-cache.ts (tracer cache state, refresh, and follows provider)
    - packages/paja/src/browser-social-cache.test.ts (injected seams and event factories)
    - packages/paja/src/browser-upload.ts (generation guard lifecycle precedent)
    - packages/paja/src/browser-relay-runtime.ts (filter and bootstrap boundaries)
  </read_first>
  <files>packages/paja/src/browser-social-cache.ts, packages/paja/src/browser-social-cache.test.ts</files>
  <behavior>
    - Red: selection ignores invalid signatures, wrong kind, wrong author, malformed tag arrays, and p-tag values shorter than 64, longer than 64, or containing non-hex characters; a valid 64-hex tag is lowercased and duplicates collapse.
    - Red: among verified same-author kind-3 candidates, greatest created_at wins; equal timestamps pick lexicographically lowest event ID independently of fixture arrival order.
    - Red: signed-out/invalid-key follows return an empty standard list, an empty selected contact list warms no broad profile query, and a single valid p tag warms only that author’s kind-0 records.
    - Red: an A refresh that resolves after a B refresh cannot commit A profiles into B’s active snapshot; overlapping refreshes for one account retain only the newest generation’s result.
    - Green: a correlated request started for A can complete with A’s follows even when active account state has moved to B, while obsolete background writes are discarded.
  </behavior>
  <action>In the isolated Paja worktree, first add the listed failing social-cache cases using deterministic event factories, injected verifier responses, and deferred contact/base-router promises. Then refine `browser-social-cache.ts` so it verifies only candidate kind-3 records for the captured account before choosing one replacement. Apply NIP-01 replacement ordering exactly: highest `created_at`, then lexicographically lowest full event ID on a tie. Derive follows only from `p` tags whose first value is exactly 64 hexadecimal characters; normalize to lowercase, preserve first valid occurrence order, and deduplicate. Treat missing/invalid captured keys and no selected contact event as an empty follow list, and avoid a broad kind-0 query when the follow set is empty. Keep request results bound to their supplied key while using generation plus active-key checks for background commits. Cache each accepted profile only when the base router returned it as a RelayEventResult matching the active account’s validated follows. Retain all cache state in memory, partitioned by account, independent of generic simulation cache mode. Do not add identity lifecycle wire behavior reserved for Phase 105, relax signature checks, or touch Writer paths.</action>
  <verify>
    <automated>cd /workspace/projects/kehto/paja-social-cache-102 && ./node_modules/.bin/vitest run packages/paja/src/browser-social-cache.test.ts packages/services/src/identity-service.test.ts</automated>
  </verify>
  <acceptance_criteria>Kind-3 contact selection is signature-validated, author-scoped, deterministic at timestamp ties, and rejects every stated p-tag boundary case. Empty/single and overlapping A/B refresh cases produce the defined follow/cache behavior without cross-account writes. No Writer source, test, fixture, smoke, or documentation path is changed.</acceptance_criteria>
  <done>PAJA-01 and PAJA-02 safely handle contact-list input boundaries, deterministic replacement ordering, and interrupted/parallel active-account refreshes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete cache-aware OUTBOX merge semantics and host-boundary guards</name>
  <precondition>Task 1’s validated account-scoped cache tests pass in `/workspace/projects/kehto/paja-social-cache-102`.</precondition>
  <read_first>
    - .planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md (D-03, D-11, and D-16)
    - .planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md (Pattern 2 and pitfalls 2 and 4)
    - packages/paja/src/browser-social-cache.ts (decorated router implementation)
    - packages/paja/src/browser-social-cache.test.ts (existing base-router mock and cache fixtures)
    - packages/services/src/outbox-service.ts (OutboxRouter/OutboxResult and result forwarding)
    - packages/services/src/outbox-service.test.ts (degraded query envelope contract)
    - packages/paja/src/browser-host.test.ts (source guard conventions)
    - packages/acl/src/resolve.ts (identity/outbox capability mappings)
  </read_first>
  <files>packages/paja/src/browser-social-cache.ts, packages/paja/src/browser-social-cache.test.ts, packages/paja/src/browser-host.test.ts</files>
  <behavior>
    - Red: a decorated kind-0 query retains matching cached RelayEventResult values when the base query returns none, while nonmatching ids/authors/kinds/since/until/tag filters add nothing.
    - Red: the combined set contains a duplicate event ID once, retaining the base result representation when cache and base carry that ID.
    - Red: cached events plus a base `{ incomplete: true, error: 'relay timeout' }` result retain both base fields exactly; cached data never converts degraded transport truth into a complete claim.
    - Red: cache decoration delegates getEvent, subscribe, close lifecycle, publish, and relay-plan operations to the original router without cache-specific wire fields.
    - Green: the Paja host source guard confirms the adapter constructs the private cache around its one base router and exposes it only through the existing identity and outbox services.
  </behavior>
  <action>In the isolated Paja worktree, add the merge and host-boundary regressions before modifying the decorator. Make decorated `query` await the base router on every request, merge only active-account cached RelayEventResult records that satisfy the original filters, and deduplicate the combined collection by `event.id` while retaining a base entry for an ID already returned by the router. Copy the base `incomplete` and `error` properties exactly when present, including useful events alongside an error; do not synthesize cache status fields or reconstruct relay failure policy. Delegate every router operation other than query unchanged so subscription close ownership, relay planning, signing, and publication remain in the base router. Reuse the existing Paja filter helper rather than hand-writing a second predicate, and keep cache operations bounded to followed kind-0 data. Extend `browser-host.test.ts` with broad source assertions that Paja composes `createPajaSocialCache` with one base router and the existing identity/outbox services, retains the established relay-list/bootstrap path, and exposes no application-specific social service name. Preserve the existing ACL source unchanged. Do not edit the external Writer checkout or schedule a real Paja smoke; that smoke is after Phase 103.</action>
  <verify>
    <automated>cd /workspace/projects/kehto/paja-social-cache-102 && ./node_modules/.bin/vitest run packages/paja/src/browser-social-cache.test.ts packages/paja/src/browser-host.test.ts packages/services/src/outbox-service.test.ts && pnpm --filter @kehto/paja type-check</automated>
  </verify>
  <acceptance_criteria>All cache/base result combinations retain matching event values with event-ID dedupe and exact base incomplete/error fields. Non-query router behavior, ACL directionality, and the normal Paja identity/outbox composition remain intact with no app-specific social service. No Writer source, test, fixture, smoke, or documentation path is changed.</acceptance_criteria>
  <done>PAJA-03 preserves standard OUTBOX filter, deduplication, partial-result, and lifecycle behavior while serving validated private cache data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw contact-list event to active follow set | The host must validate replaceable relay data before it influences profile reads. |
| Previous account async work to active account snapshot | Old background work may settle after a signer/account transition. |
| Private cache merge to normal query result | An optimization must not alter protocol degradation or authorization truth. |
| Paja adapter to runtime ACL/services | Composition must retain existing capability directionality and lifecycle owners. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-102-10 | Tampering | kind-3 replacement selection | high | mitigate | Task 1 verifies candidates, constrains author/kind, applies NIP-01 ordering, and validates p tags at exact boundaries. |
| T-102-11 | Information Disclosure/Tampering | overlapping cache refresh | high | mitigate | Task 1 tests deferred A/B and repeated refreshes; generation and active-key checks discard obsolete background writes. |
| T-102-12 | Repudiation/Tampering | cache/base query merge | high | mitigate | Task 2 preserves base incomplete/error values and validates filter/ID behavior with degraded-result tests. |
| T-102-13 | Elevation of Privilege | router/service composition | high | mitigate | Task 2 delegates non-query operations, retains ACL mappings, and guards the existing identity/outbox-only service path. |
| T-102-14 | Denial of Service | cache warm query scope | medium | mitigate | Task 1 avoids broad profiles on empty follows; Task 2 limits cache behavior to matching followed kind-0 values. |
| T-102-SC | Tampering | dependency supply chain | low | accept | No package installation, manifest update, or lockfile change is planned. |

## ASVS L1 Control-to-Task Mapping

| Control | Threat | Owning task | Evidence |
|---------|--------|-------------|----------|
| V5 input validation | T-102-10 | Task 1 | Signature, author/kind, exact-key boundary, and deterministic replaceable-event tests. |
| V3 session state isolation | T-102-11 | Task 1 | Deferred concurrent refresh tests prove account/generation safety. |
| V5 integrity of result state | T-102-12 | Task 2 | Cache/base merge test matrix preserves partial and error outcomes. |
| V4 authorization | T-102-13 | Task 2 | Router delegation and host guard retain the existing ACL/service boundary. |
</threat_model>

<verification>
1. Run focused cache and identity-service tests after Task 1, then cache/host/outbox tests and Paja type-check after Task 2.
2. Inspect fixtures for exact public-key boundary cases, valid/invalid signatures, reverse arrival order, and deferred refresh settlement order.
3. Confirm the decorator’s returned incomplete/error values match the base result rather than cache availability.
4. Confirm host source guards protect one base router, retained relay bootstrap, and existing identity/outbox construction.
5. Confirm no external Writer path or Phase 103 Blossom functionality appears in the diff or task output.
</verification>

<success_criteria>
- PAJA-01: Contact-list follows are verified, deterministic, input-bounded, and request-safe under concurrency.
- PAJA-02: Active-account refresh cannot retain stale cross-account data and uses only valid base-router profile results.
- PAJA-03: Cache additions are filter-matching and ID-deduplicated while base partial/error semantics and router lifecycle survive exactly.
- Paja remains a standard identity/outbox host with unchanged ACL authority and no real downstream smoke before Phase 103.
</success_criteria>

<output>
Create `.planning/phases/102-paja-standard-nap-social-poc/102-03-SUMMARY.md` when done.
</output>
