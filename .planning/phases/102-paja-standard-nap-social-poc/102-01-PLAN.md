---
phase: 102-paja-standard-nap-social-poc
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: [PAJA-01, PAJA-02, PAJA-03]
must_haves:
  truths:
    - "Phase 102 source work runs only from a clean, freshly fetched Paja implementation worktree based on upstream/main, not from the planning checkout."
    - "The configured Chromium executable can launch Paja browser coverage, or execution stops with the missing executable recorded as a blocker before Phase 102 is claimed complete."
    - "NAP-IDENTITY and the pinned NAP-OUTBOX draft/type authority are rechecked before the standard identity/outbox path is changed."
  artifacts: []
  key_links:
    - from: "dedicated Paja implementation worktree"
      to: "upstream/main"
      via: "fresh fetch and merge-base equality before the first source edit"
    - from: "playwright.config.ts"
      to: "/usr/bin/chromium"
      via: "the configured Chromium executablePath is asserted before browser proof"
  prohibitions:
    - requirement_id: PAJA-01
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "Paja source work must not be performed from the planning checkout or mixed with planning artifacts in an upstream contribution."
      reason: "A clean implementation baseline makes the code reviewable and preserves the planning branch."
    - requirement_id: PAJA-01
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "No Writer source, test, fixture, smoke, or documentation path may be modified, staged, or scheduled by Phase 102."
      reason: "Writer integration remains after Phase 103 and the explicit Phase 104 approval gate."
    - requirement_id: PAJA-03
      category: values
      status: unverified
      flagged: true
      verification: judgment
      statement: "The phase must not represent the removed current-master OUTBOX document as its authority."
      reason: "The PoC is governed by the pinned draft and installed published types under documented upstream drift."
  flagged_assumptions:
    - requirement_id: PAJA-01
      category: boundary
      status: resolved-by-plan
      probe: "What happens at the accepted public-key boundary and immediately on either side?"
      handling: "Plan 03 Task 1 tests exact 64-hex p-tag acceptance and rejects 63-character, 65-character, and non-hex values."
    - requirement_id: PAJA-01
      category: adjacency
      status: resolved-by-plan
      probe: "How do duplicate or touching contact-list candidates combine?"
      handling: "Plan 03 Task 1 normalizes and deduplicates p tags, partitions snapshots by account, and selects a single replaceable kind-3 winner."
    - requirement_id: PAJA-01
      category: empty
      status: resolved-by-plan
      probe: "What happens for signed-out, empty, single-item, and null-like inputs?"
      handling: "Plan 02 Task 2 and Plan 03 Task 1 assert the empty signed-out identity/follows shapes, no-contact result, and one valid follow."
    - requirement_id: PAJA-01
      category: ordering
      status: resolved-by-plan
      probe: "Is equal-timestamp contact-list ordering stable?"
      handling: "Plan 03 Task 1 uses reversed fixture arrival orders and asserts the same selected event."
    - requirement_id: PAJA-01
      category: precision
      status: resolved-by-plan
      probe: "What is the exact tie-breaker when created_at is equal?"
      handling: "Plan 03 Task 1 requires lexicographically lowest event ID after greatest created_at."
    - requirement_id: PAJA-01
      category: concurrency
      status: resolved-by-plan
      probe: "What does an interrupted or parallel identity request guarantee?"
      handling: "Plan 02 Task 2 proves the provider receives the request-start key; Plan 03 Task 1 proves late cache work cannot replace the active account snapshot."
    - requirement_id: PAJA-02
      category: concurrency
      status: resolved-by-plan
      probe: "What happens when account refreshes overlap or an earlier refresh settles last?"
      handling: "Plan 03 Task 1 uses deferred A/B and same-account refreshes to prove generation-guarded background writes."
    - requirement_id: PAJA-03
      category: unclassified
      status: resolved-by-plan
      probe: "How do cached and base OUTBOX result combinations behave?"
      handling: "Plan 03 Task 2 covers cache-only additions, base/cache duplicate IDs, filter misses, and every useful base incomplete/error combination."
---

<objective>
Establish the non-negotiable execution conditions for the Paja Standard-NAP Social PoC before its tracer changes host code.

Purpose: Keep the source contribution isolated, use the selected protocol authority, and fail openly if the required browser harness cannot run.
Output: A clean source worktree ready for the remaining plans, validated NAP authority inputs, and an executable Chromium gate.

Decision scope: D-01: use frozen roadmap, research, repository evidence, and pinned references without further discovery. D-02: take the bounded fastest safe Paja-first route. D-03: retain standard NAP mediation and focused contribution hygiene. D-04: start source from the historical implementation baseline lineage, not planning commits. D-05: verify/fetch upstream and record its current SHA before Paja edits. D-06: retain planning artifacts in the fork rather than an upstream source contribution. D-07: preserve the observed external Writer WIP. D-08: reserve its separate integration worktree for the later phase. D-09: treat its local comparison point as non-authoritative until canonical evidence exists. D-10: do not mutate external Writer remotes, refs, worktrees, index, or source. D-11: implement and verify Paja before downstream source work. D-12: defer cross-repository target proof and dependency normalization to downstream work. D-13: no automated sequence enters external Writer source edits. D-14: only its later explicit approval can authorize that work. D-15: use the pinned NAP-IDENTITY authority for read-only identity behavior. D-16: use the pinned NAP-OUTBOX draft for query-wide events, incomplete, and error semantics. D-17: keep resource-byte behavior for the later resource scope. D-18: keep upload rail behavior for the later upload scope. D-19: keep lower-level Blossom behavior and its product-security gap out of this phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md
@.planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md
@.planning/phases/102-paja-standard-nap-social-poc/102-PATTERNS.md
@.planning/phases/102-paja-standard-nap-social-poc/102-VALIDATION.md
@AGENTS.md
@playwright.config.ts
</context>

## Artifacts this phase produces

1. `packages/paja/src/browser-social-cache.ts` — Paja-private active-account social snapshots and an OUTBOX router decorator, created in Plan 02.
2. `packages/paja/src/browser-social-cache.test.ts` — deterministic cache, ordering, race, filter, and merge regression coverage, created in Plan 02 and expanded in Plan 03.
3. `packages/paja/src/browser-relay-runtime.ts` — a narrow host-owned contact-list loader exported for the private cache, updated in Plan 02.
4. `packages/paja/src/browser-adapter.ts` — one base router composed with the private cache for existing identity and outbox services, updated in Plan 02.
5. `tests/e2e/paja-single-window.spec.ts` — deterministic iframe proof of the standard identity-to-OUTBOX tracer, updated in Plan 02.
6. `packages/services/src/identity-service.test.ts` — request-start public-key snapshot regression, updated in Plan 02.
7. `packages/paja/src/browser-host.test.ts` — host composition guard for the private social cache, updated in Plan 03.
8. `packages/paja/README.md`, `docs/packages/paja.md`, `docs/how-tos/paja-local-authoring.md`, and `.changeset/paja-standard-nap-social-cache.md` — released behavior, boundary, and versioning record, updated in Plan 04.

## Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Logged-in napplet discovers active identity, follows, and followed kind-0 records through standard messages | 02, 03 | COVERED | The Plan 02 tracer crosses identity, Paja cache, base OUTBOX router, and iframe message delivery. |
| REQ | PAJA-01 | Request-start active key and relay-backed follows | 02, 03 | COVERED | Service snapshot test plus validated kind-3/race coverage. |
| REQ | PAJA-02 | Active-account in-memory followed-kind-0 cache refresh | 02, 03 | COVERED | Private cache warm and generation/account isolation. |
| REQ | PAJA-03 | Ordinary OUTBOX query preserves validated events, dedupe, incomplete, and error | 02, 03 | COVERED | Decorator and combination tests retain the base result fields unchanged. |
| RESEARCH | — | No new dependency; reuse services/router/verifier and preserve ACL | 01, 02, 03 | COVERED | Worktree uses existing dependency tree; plans alter Paja composition only. |
| RESEARCH | — | Pinned OUTBOX drift must be recorded without master-conformance claim | 01, 02, 04 | COVERED | Authority check, nearby source/test note, documentation, summary, and PR evidence. |
| RESEARCH | — | Chromium at configured executable path is required for browser proof | 01 | COVERED | A failed noninteractive provision halts with an explicit blocker. |
| CONTEXT | D-01 through D-03 | Frozen, Paja-first, standard-NAP delivery posture | 01, 02, 03 | COVERED | No custom napplet-visible social surface is scheduled. |
| CONTEXT | D-04 through D-06 | Fresh upstream source branch and focused contribution hygiene | 01, 04 | COVERED | Worktree bootstrap and final explicit-path contribution checks. |
| CONTEXT | D-07 through D-10, D-13 through D-14 | Preserve and do not enter Writer work | 01 through 04 | COVERED | Every plan has an explicit external-Writer exclusion. |
| CONTEXT | D-11 through D-12 | Paja completion precedes downstream proof/integration | 01 through 04 | COVERED | No Phase 103 or real downstream smoke is included. |
| CONTEXT | D-15 through D-16 | Identity and pinned OUTBOX authority | 01 through 04 | COVERED | Exact refs guide the cache and its evidence. |
| CONTEXT | D-17 through D-19 | Resource/upload authority and hardening are later work | 01 through 04 | COVERED AS EXCLUDED | No Blossom, resource, or downstream upload implementation is planned. |

<tasks>

<task type="auto">
  <name>Task 1: Create and validate the isolated Paja implementation worktree</name>
  <read_first>
    - AGENTS.md (dirty-state, branch, NAP-conformance, and explicit-path staging rules)
    - .planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md (D-04 through D-16)
    - .planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md (protocol authority, package constraint, and source baseline)
    - packages/paja/package.json (locked workspace dependencies and scripts)
  </read_first>
  <files>None — this task creates or validates a linked Git worktree and its untracked dependency symlink; it must not edit a tracked source, test, document, lockfile, or planning file.</files>
  <action>Per D-04 through D-06 and D-11, do not execute Phase 101; instead, prepare the Paja source location required for Phase 102. From the planning checkout, verify the `upstream` remote is `https://github.com/kehto/web.git`; add it only when absent, and halt rather than rewriting a different existing URL. Fetch `upstream/main`, then create `/workspace/projects/kehto/paja-social-cache-102` as a linked worktree on `feat/paja-standard-nap-social-cache` directly from the freshly fetched ref. If the directory or branch already exists, resume only when it is clean and its merge-base with the refreshed ref is that ref; otherwise stop without reset, clean, rebase, or force operations. Provide the worktree its existing dependency tree through an untracked `node_modules` symlink to this checkout only after asserting both lockfiles are byte-identical; halt if they differ rather than installing or changing packages. Recheck the installed `@napplet/nap@0.28.0` identity/outbox declarations against the pinned authority decision: NAP-IDENTITY `6461e4b37c29dc09a20dff35d9515889c4433874` is master-identical for this work, while NAP-OUTBOX `4589a8f9a16d8aa29b3740e2b3b0cdca11e0976e` plus installed types govern this PoC under upstream drift. Per D-07 through D-10 and D-13 through D-14, do not inspect, mutate, stage, or schedule any external Writer path. Record the absolute worktree path, fresh upstream SHA, type-contract check, and OUTBOX drift status in the task summary.</action>
  <verify>
    <automated>PAJA_WORKTREE=/workspace/projects/kehto/paja-social-cache-102; test -f "$PAJA_WORKTREE/.git" && test -z "$(git -C "$PAJA_WORKTREE" status --porcelain)" && test "$(git -C "$PAJA_WORKTREE" merge-base upstream/main HEAD)" = "$(git -C "$PAJA_WORKTREE" rev-parse upstream/main)" && test -L "$PAJA_WORKTREE/node_modules" && cmp -s pnpm-lock.yaml "$PAJA_WORKTREE/pnpm-lock.yaml" && rg -n 'IdentityGetFollows|OutboxQuery|OutboxResult' node_modules/.pnpm/@napplet+nap@0.28.0/node_modules/@napplet/nap/dist/{identity,outbox}/types.d.ts</automated>
  </verify>
  <acceptance_criteria>A clean Paja source worktree is based on the fetched upstream ref, uses no new package installation, has the existing dependency tree available, and has recorded the pinned NAP authority plus OUTBOX upstream drift. No Writer source, test, fixture, smoke, or documentation path has been touched or scheduled.</acceptance_criteria>
  <done>The remaining plans have an isolated Paja implementation location and a verified protocol/type baseline without executing Phase 101.</done>
</task>

<task type="auto">
  <name>Task 2: Provision the configured Chromium executable or halt the phase visibly</name>
  <read_first>
    - playwright.config.ts (the required executable path and browser project)
    - .planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md (Environment Availability and Wave 0 requirements)
    - .planning/phases/102-paja-standard-nap-social-poc/102-VALIDATION.md (browser proof is mandatory)
    - AGENTS.md (full browser verification policy)
  </read_first>
  <files>None — this task validates or provisions the host executable and must not modify Playwright configuration or tracked test infrastructure.</files>
  <action>Per D-02 and D-11, resolve the browser prerequisite before the production tracer. Assert `/usr/bin/chromium` is executable and capture its version. If absent, attempt only the Fedora-consistent noninteractive system provision `sudo -n dnf install -y chromium`; then reassert the same executable path. If passwordless provisioning, `dnf`, or the required executable remains unavailable, stop Phase 102 immediately with a summary blocker that names `/usr/bin/chromium`, the failed command/result, and the fact that the required Paja browser proof was not waived. Do not redirect Playwright to a cached browser, change `playwright.config.ts`, downgrade the browser proof to a manual claim, or start source changes after this task blocks.</action>
  <verify>
    <automated>test -x /usr/bin/chromium && /usr/bin/chromium --version && pnpm exec playwright test tests/e2e/paja-single-window.spec.ts --project=chromium --list</automated>
  </verify>
  <acceptance_criteria>The exact Chromium executable configured by the repository is runnable and Playwright can enumerate the deterministic Paja browser suite. If the assertion cannot be met, the phase is explicitly blocked rather than marked complete; no Writer source, test, fixture, smoke, or documentation path is changed.</acceptance_criteria>
  <done>Browser proof is executable at the repository-configured path, or Phase 102 is stopped with an actionable environment blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning checkout to implementation worktree | Planning-only artifacts and unrelated dirty state must not enter Paja source commits. |
| Kehto source worktree to upstream/main | A mutable remote ref becomes the implementation baseline. |
| Protocol documents/types to host implementation | Pinned identity and draft OUTBOX sources govern wire behavior under a documented drift condition. |
| Host browser configuration to Playwright | Required deterministic browser proof depends on a system executable outside the repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-102-01 | Tampering | Paja source baseline | high | mitigate | Task 1 fetches the named upstream, asserts the clean worktree merge-base, and never resets existing work. |
| T-102-02 | Spoofing/Tampering | NAP authority inputs | high | mitigate | Task 1 rechecks installed published types and records the pinned identity ref plus the draft OUTBOX drift status. |
| T-102-03 | Denial of Service | Paja browser verification | medium | mitigate | Task 2 asserts the configured executable, attempts only noninteractive Fedora provisioning, and blocks visibly on failure. |
| T-102-04 | Elevation of Privilege | external Writer checkout | high | mitigate | Task 1 follows D-07 through D-10 and D-13 through D-14: no access, staging, or scheduled work against that checkout. |
| T-102-SC | Tampering | dependency supply chain | high | mitigate | Task 1 does not invoke a package install or alter manifests/lockfiles; it permits the existing dependency tree only after lockfile equality. |

## ASVS L1 Control-to-Task Mapping

| Control | Threat | Owning task | Evidence |
|---------|--------|-------------|----------|
| V1/V4 integrity and change control | T-102-01 | Task 1 | Fresh ref, clean worktree, and explicit-path source isolation checks. |
| V5 trustworthy contract input | T-102-02 | Task 1 | Pinned authority/type inspection and nearby drift recording in later source/tests. |
| V8 operational availability | T-102-03 | Task 2 | Executable assertion or explicit blocker; browser coverage is never silently omitted. |
| V4 authorization boundary | T-102-04 | Task 1 | D-07 through D-10 and D-13 through D-14 scope exclusion. |
</threat_model>

<verification>
1. Confirm the Paja source worktree is clean, based on the fetched upstream ref, and has no planning artifacts staged.
2. Confirm the lockfile equality and existing dependency symlink before any source command runs; no package is added or upgraded.
3. Confirm Chromium is executable at the path in `playwright.config.ts`; otherwise stop and preserve the blocker in the summary.
4. Confirm the task summary records the exact identity ref, OUTBOX draft ref, installed type version, and the current-master OUTBOX drift statement.
5. Confirm no Writer source, test, fixture, smoke, or documentation path was edited, staged, or scheduled.
</verification>

<success_criteria>
- A verified, isolated Paja source worktree is ready for source changes without executing Phase 101.
- The standard identity/OUTBOX authority is recorded precisely, including the draft-spec drift.
- Browser verification is runnable or the phase is visibly blocked before source execution.
- The hard external-Writer scope fence remains intact.
</success_criteria>

<output>
Create `.planning/phases/102-paja-standard-nap-social-poc/102-01-SUMMARY.md` when done.
</output>
