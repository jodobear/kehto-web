---
phase: 102-paja-standard-nap-social-poc
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/102-paja-standard-nap-social-poc/102-IMPLEMENTATION-NOTE.md
autonomous: true
requirements: [PAJA-01, PAJA-02, PAJA-03]
must_haves:
  truths:
    - "Before a Phase 102 source, test, or documentation edit, the executor records a fail-closed current-master NAP authority comparison and installed type-contract evidence in a tracked Phase 102 implementation note."
    - "The configured Chromium executable is runnable, or execution stops with the missing executable recorded as a blocker before Phase 102 is claimed complete."
    - "Phase 102 runs only from the orchestrator-supplied cwd; this plan performs protocol/environment preflight and leaves repository topology to GSD."
  artifacts:
    - path: .planning/phases/102-paja-standard-nap-social-poc/102-IMPLEMENTATION-NOTE.md
      provides: "Timestamped current-master SHA, pinned/master NAP-IDENTITY byte-comparison result, master-tree OUTBOX absence proof, installed type-contract evidence, and the adopted upstream-drift disposition."
  key_links:
    - from: .planning/phases/102-paja-standard-nap-social-poc/102-IMPLEMENTATION-NOTE.md
      to: "napplet/naps master SHA plus pinned NAP-IDENTITY and NAP-OUTBOX refs"
      via: "exact raw-document fetches, master-tree inspection, byte comparison, and recorded source URLs/hashes"
    - from: "playwright.config.ts"
      to: "/usr/bin/chromium"
      via: "the configured Chromium executablePath is asserted before any browser suite is scheduled"
  prohibitions:
    - requirement_id: PAJA-01
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "Phase 102 source work uses only the orchestrator-supplied cwd; this plan performs no repository-topology operations."
      reason: "GSD owns execution topology, while this plan owns only protocol/environment preflight and Paja scope."
    - requirement_id: PAJA-01
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "No Writer source, test, fixture, smoke, or documentation path may be modified, staged, or scheduled by Phase 102."
      reason: "Writer integration remains after Phase 103 and the explicit Phase 104 approval gate."
    - requirement_id: PAJA-03
      category: values
      status: unverified
      flagged: true
      verification: judgment
      statement: "The phase must not represent the removed current-master OUTBOX document as its authority."
      reason: "The PoC is governed by the pinned draft and installed published types under documented upstream drift."
  flagged_assumptions:
    - requirement_id: PAJA-01
      category: boundary
      status: resolved-by-plan
      probe: "What happens at the accepted public-key boundary and immediately on either side?"
      handling: "Plan 03 Task 1 tests exact 64-hex p-tag acceptance and rejects 63-character, 65-character, and non-hex values."
    - requirement_id: PAJA-01
      category: adjacency
      status: resolved-by-plan
      probe: "How do duplicate or touching contact-list candidates combine?"
      handling: "Plan 03 Task 1 normalizes and deduplicates p tags, partitions snapshots by account, and selects a single replaceable kind-3 winner."
    - requirement_id: PAJA-01
      category: empty
      status: resolved-by-plan
      probe: "What happens for signed-out, empty, single-item, and null-like inputs?"
      handling: "Plan 02 Task 2 and Plan 03 Task 1 assert the empty signed-out identity/follows shapes, no-contact result, and one valid follow."
    - requirement_id: PAJA-01
      category: ordering
      status: resolved-by-plan
      probe: "Is equal-timestamp contact-list ordering stable?"
      handling: "Plan 03 Task 1 uses reversed fixture arrival orders and asserts the same selected event."
    - requirement_id: PAJA-01
      category: precision
      status: resolved-by-plan
      probe: "What is the exact tie-breaker when created_at is equal?"
      handling: "Plan 03 Task 1 requires lexicographically lowest event ID after greatest created_at."
    - requirement_id: PAJA-01
      category: concurrency
      status: resolved-by-plan
      probe: "What does an interrupted or parallel identity request guarantee?"
      handling: "Plan 02 Task 2 proves the provider receives the request-start key; Plan 03 Task 1 proves late cache work cannot replace the active account snapshot."
    - requirement_id: PAJA-02
      category: concurrency
      status: resolved-by-plan
      probe: "What happens when account refreshes overlap or an earlier refresh settles last?"
      handling: "Plan 03 Task 1 uses deferred A/B and same-account refreshes to prove generation-guarded background writes."
    - requirement_id: PAJA-03
      category: unclassified
      status: resolved-by-plan
      probe: "How do cached and base OUTBOX result combinations behave?"
      handling: "Plan 03 Task 2 covers cache-only additions, base/cache duplicate IDs, filter misses, and every useful base incomplete/error combination."
---

<objective>
Establish the fail-closed protocol and environment preflight for the Paja Standard-NAP Social PoC before its tracer changes host code.

Purpose: Recheck the adopted protocol authority from the executor-supplied GSD worktree and fail openly if the required browser executable cannot run.
Output: A tracked Phase 102 authority-evidence note and an executable Chromium gate; no repository-topology, source implementation, or downstream execution work.

Decision scope: D-01: use frozen roadmap, research, repository evidence, and pinned references without further discovery. D-02: take the bounded fastest safe Paja-first route. D-03: retain standard NAP mediation and focused contribution hygiene. D-04 through D-06: execution starts from the orchestrator-provided HEAD after planning has integrated upstream/main; GSD supplies the cwd and planning artifacts stay out of the later focused source contribution. D-07 through D-10 and D-13 through D-14: preserve Writer WIP and do not inspect, mutate, stage, schedule, or authorize Writer source work. D-11: implement and verify Paja before downstream source work. D-12: defer cross-repository target proof and dependency normalization to downstream work. D-15: use the pinned NAP-IDENTITY authority for read-only identity behavior. D-16: use the pinned NAP-OUTBOX draft for query-wide events, incomplete, and error semantics. D-17: keep resource-byte behavior for the later resource scope. D-18: keep upload rail behavior for the later upload scope. D-19: keep lower-level Blossom behavior and its product-security gap out of this phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md
@.planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md
@.planning/phases/102-paja-standard-nap-social-poc/102-PATTERNS.md
@.planning/phases/102-paja-standard-nap-social-poc/102-VALIDATION.md
@AGENTS.md
@playwright.config.ts
</context>

## Artifacts this phase produces

1. `.planning/phases/102-paja-standard-nap-social-poc/102-IMPLEMENTATION-NOTE.md` — tracked, fail-closed current-master/pinned authority and installed-type evidence produced in Plan 01; it is planning evidence, not an upstream source-contribution file.
2. `packages/paja/src/browser-social-cache.ts` — Paja-private active-account social snapshots and an OUTBOX router decorator, created in Plan 02.
3. `packages/paja/src/browser-social-cache.test.ts` — deterministic cache, ordering, race, filter, and merge regression coverage, created in Plan 02 and expanded in Plan 03.
4. `packages/paja/src/browser-relay-runtime.ts` — a narrow host-owned contact-list loader for the private cache, updated in Plan 02.
5. `packages/paja/src/browser-adapter.ts` — one base router composed with the private cache for existing identity and outbox services, updated in Plan 02.
6. `tests/e2e/paja-single-window.spec.ts` — deterministic iframe proof of the standard identity-to-OUTBOX tracer, updated in Plan 02 and executed only in Plan 04's final gate.
7. `packages/services/src/identity-service.test.ts` — captured request-start public-key contract regression, updated in Plan 02 without changing `identity-service.ts`.
8. `packages/paja/src/browser-host.test.ts` — host composition guard for the private social cache, updated in Plan 03.
9. `packages/paja/README.md`, `docs/packages/paja.md`, `docs/how-tos/paja-local-authoring.md`, and `.changeset/paja-standard-nap-social-cache.md` — released behavior, boundary, and versioning record, updated in Plan 04.

## Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Logged-in napplet discovers active identity, follows, and followed kind-0 records through standard messages | 02, 03 | COVERED | The Plan 02 tracer crosses identity, Paja cache, base OUTBOX router, and iframe message delivery. |
| REQ | PAJA-01 | Request-start active key and relay-backed follows | 02, 03 | COVERED | Service snapshot test plus validated kind-3/race coverage. |
| REQ | PAJA-02 | Active-account in-memory followed-kind-0 cache refresh | 02, 03 | COVERED | Private cache warm and generation/account isolation. |
| REQ | PAJA-03 | Ordinary OUTBOX query preserves validated events, dedupe, incomplete, and error | 02, 03 | COVERED | Decorator and combination tests retain the base result fields unchanged. |
| RESEARCH | — | No new dependency; reuse services/router/verifier and preserve ACL | 01, 02, 03 | COVERED | No package installation or manifest/lockfile update is planned; plans alter Paja composition only. |
| RESEARCH | — | Pinned OUTBOX drift must be recorded without master-conformance claim | 01, 02, 04 | COVERED | Plan 01 writes fail-closed authority evidence; Plans 02 and 04 retain it in nearby implementation evidence and the final PR contract. |
| RESEARCH | — | Required in-memory cache ignores generic simulation cache mode | 02, 03, 04 | COVERED | The adopted disposition is implemented, tested, and documented as private active-account memory state. |
| RESEARCH | — | Chromium at configured executable path is required for browser proof | 01, 04 | COVERED | Plan 01 blocks on an unavailable executable; Plan 04 runs the only browser suite in the final gate. |
| CONTEXT | D-01 through D-03 | Frozen, Paja-first, standard-NAP delivery posture | 01, 02, 03 | COVERED | No custom napplet-visible social surface is scheduled. |
| CONTEXT | D-04 through D-06 | Executor-owned implementation context and focused contribution hygiene | 01, 04 | COVERED | GSD supplies execution topology; planning evidence remains outside the upstream source contribution. |
| CONTEXT | D-07 through D-10, D-13 through D-14 | Preserve and do not enter Writer work | 01 through 04 | COVERED | Every plan has an explicit Writer exclusion. |
| CONTEXT | D-11 through D-12 | Paja completion precedes downstream proof/integration | 01 through 04 | COVERED | No Phase 103 or real downstream smoke is included. |
| CONTEXT | D-15 through D-16 | Identity and pinned OUTBOX authority | 01 through 04 | COVERED | Exact refs guide the cache and its evidence. |
| CONTEXT | D-17 through D-19 | Resource/upload authority and hardening are later work | 01 through 04 | COVERED AS EXCLUDED | No Blossom, resource, or downstream upload implementation is planned. |

<tasks>

<task type="auto">
  <name>Task 1: Verify protocol authority and persist the Phase 102 contract evidence</name>
  <read_first>
    - AGENTS.md (NAP-conformance, scope, documentation, and explicit-path staging rules)
    - .planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md (D-04 through D-16)
    - .planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md (resolved authority, cache-mode, and browser dispositions)
    - packages/paja/package.json (locked workspace dependencies and scripts)
  </read_first>
  <files>.planning/phases/102-paja-standard-nap-social-poc/102-IMPLEMENTATION-NOTE.md</files>
  <action>Per D-01, D-03, D-11, D-15, and D-16, run this authority probe immediately before any Phase 102 source, test, or documentation edit from the orchestrator-supplied cwd; do not execute Phase 101 or perform Kehto repository-topology operations. Use a temporary directory outside tracked files and a strict `set -euo pipefail` sequence. Resolve `MASTER_SHA` with `git ls-remote --refs https://github.com/napplet/naps.git refs/heads/master`; require exactly one lowercase 40-hex SHA. With `curl --fail --silent --show-error --location`, fetch `naps/NAP-IDENTITY.md` at pinned `6461e4b37c29dc09a20dff35d9515889c4433874` and at `MASTER_SHA`, fetch `naps/NAP-OUTBOX.md` at pinned `4589a8f9a16d8aa29b3740e2b3b0cdca11e0976e`, and fetch the recursive GitHub tree JSON for `MASTER_SHA`. Require `cmp -s` to prove the two NAP-IDENTITY files are byte-identical. Use Node to parse the fetched tree JSON and exit nonzero unless `tree` is an array and it contains no entry whose `path` is exactly `naps/NAP-OUTBOX.md`. Require the installed `node_modules/.pnpm/@napplet+nap@0.28.0/node_modules/@napplet/nap/dist/identity/types.d.ts` and `outbox/types.d.ts` files, then inspect declarations matching `IdentityGetFollows`, `OutboxQuery`, and `OutboxResult`. Write `.planning/phases/102-paja-standard-nap-social-poc/102-IMPLEMENTATION-NOTE.md` with a `## Executable checks` section containing the exact rerunnable `git ls-remote`, raw/API fetch URLs, SHA-256, `cmp -s`, Node tree-inspection, and declaration-inspection commands used; then record the execution timestamp, `MASTER_SHA`, exact raw/API URLs, SHA-256 values for the fetched documents/tree, byte-comparison result, OUTBOX path-absence result, installed declaration paths/version and matched identifiers, and this exact disposition: `Pinned NAP-OUTBOX 4589a8f9a16d8aa29b3740e2b3b0cdca11e0976e plus installed @napplet/nap@0.28.0 types govern the Phase 102 PoC under upstream drift; no current-master conformance claim.` Any unavailable authority source, malformed SHA/tree, nonidentical identity bytes, present current-master OUTBOX path, missing declaration, or changed disposition is a hard stop; do not replace it with a local-code claim. Per D-07 through D-10 and D-13 through D-14, do not inspect, mutate, stage, or schedule Writer source.</action>
  <verify>
    <automated>test -s .planning/phases/102-paja-standard-nap-social-poc/102-IMPLEMENTATION-NOTE.md && rg -n 'Executable checks|git ls-remote|MASTER_SHA|6461e4b37c29dc09a20dff35d9515889c4433874|4589a8f9a16d8aa29b3740e2b3b0cdca11e0976e|byte-identical|naps/NAP-OUTBOX.md.*absent|IdentityGetFollows|OutboxQuery|OutboxResult|no current-master conformance claim' .planning/phases/102-paja-standard-nap-social-poc/102-IMPLEMENTATION-NOTE.md</automated>
  </verify>
  <acceptance_criteria>The tracked implementation note proves a freshly resolved master SHA, byte-identical pinned/master NAP-IDENTITY, absent `naps/NAP-OUTBOX.md` at that same master, and installed `@napplet/nap@0.28.0` identity/outbox contract identifiers. Failure to establish any fact stops the phase, while the recorded OUTBOX disposition makes no current-master claim. No Writer source, test, fixture, smoke, or documentation path has been touched or scheduled.</acceptance_criteria>
  <done>Phase 102 has its exact protocol authority evidence and adopted upstream-drift contract before the tracer begins.</done>
</task>

<task type="auto">
  <name>Task 2: Provision the configured Chromium executable or halt the phase visibly</name>
  <read_first>
    - playwright.config.ts (the required executable path and browser project)
    - .planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md (resolved Environment Availability disposition)
    - .planning/phases/102-paja-standard-nap-social-poc/102-VALIDATION.md (final browser-proof gate)
    - AGENTS.md (browser verification policy)
  </read_first>
  <files>None — this task validates or provisions the host executable and must not modify Playwright configuration or tracked test infrastructure.</files>
  <action>Per D-02 and D-11, resolve the hard browser prerequisite before the production tracer. Assert `/usr/bin/chromium` is executable and capture its version. If absent, attempt only the Fedora-consistent noninteractive system provision `sudo -n dnf install -y chromium`, then reassert that same executable path. If passwordless provisioning, `dnf`, or the required executable remains unavailable, stop Phase 102 immediately with a summary blocker that names `/usr/bin/chromium`, the failed command/result, and the fact that required browser proof was not waived. Do not redirect Playwright to a cached browser, change `playwright.config.ts`, downgrade browser proof to a manual claim, or start source changes after this task blocks. Do not run Playwright in Wave 0; Plan 04 owns the final browser-inclusive gate after the focused checks.</action>
  <verify>
    <automated>test -x /usr/bin/chromium && /usr/bin/chromium --version</automated>
  </verify>
  <acceptance_criteria>The exact Chromium executable configured by the repository is runnable. If the assertion cannot be met, the phase is explicitly blocked rather than marked complete; no Writer source, test, fixture, smoke, or documentation path is changed.</acceptance_criteria>
  <done>The final browser proof has its configured executable, or Phase 102 is stopped with an actionable environment blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote NAP authority to Phase 102 implementation | Mutable current-master data and pinned documents must be captured and compared before they govern host behavior. |
| Installed `@napplet/nap` declarations to host implementation | The locked published type contract must be present and inspected rather than inferred from local source. |
| GSD execution topology to planned work | The orchestrator supplies the execution cwd; plan tasks perform no competing repository-topology operation. |
| Host browser configuration to Playwright | Required deterministic browser proof depends on a system executable outside the repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-102-01 | Spoofing/Tampering | NAP authority inputs | high | mitigate | Task 1 resolves a live master SHA, fetches exact documents/tree, compares identity bytes, proves master OUTBOX absence, and records hashes/URLs. |
| T-102-02 | Tampering | installed type contract | high | mitigate | Task 1 requires the locked identity/outbox declaration files and named contract identifiers before edits. |
| T-102-03 | Denial of Service | Paja browser verification | medium | mitigate | Task 2 asserts the configured executable, attempts only noninteractive Fedora provisioning, and blocks visibly on failure. |
| T-102-04 | Elevation of Privilege | Writer scope boundary | high | mitigate | Task 1 follows D-07 through D-10 and D-13 through D-14: no Writer source access, staging, or scheduled work. |
| T-102-SC | Tampering | dependency supply chain | low | accept | No package installation, manifest update, or lockfile change is planned. |

## ASVS L1 Control-to-Task Mapping

| Control | Threat | Owning task | Evidence |
|---------|--------|-------------|----------|
| V1/V4 integrity and change control | T-102-01 | Task 1 | Fail-closed master/pinned document and tree evidence recorded in the tracked implementation note. |
| V5 trustworthy contract input | T-102-02 | Task 1 | Locked installed declarations and named type-contract inspection. |
| V8 operational availability | T-102-03 | Task 2 | Executable assertion or explicit blocker; browser coverage is never silently omitted. |
| V4 authorization boundary | T-102-04 | Task 1 | D-07 through D-10 and D-13 through D-14 Writer exclusion. |
</threat_model>

<verification>
1. Before any Phase 102 source/test/docs edit, confirm the tracked implementation note records the freshly resolved master SHA, fetch URLs/hashes, byte-identical identity result, same-SHA OUTBOX absence, installed `@napplet/nap@0.28.0` identifiers, and exact drift disposition.
2. Confirm the authority probe fails closed for unavailable/invalid master data, nonidentical identity bytes, a present master OUTBOX path, missing installed declarations, or a disposition mismatch.
3. Confirm Chromium is executable at the path in `playwright.config.ts`; otherwise stop and preserve the blocker in the summary.
4. Confirm Plan 01 uses the orchestrator-supplied cwd and performs no repository-topology operation.
5. Confirm no Writer source, test, fixture, smoke, or documentation path was edited, staged, or scheduled.
</verification>

<success_criteria>
- The standard identity/OUTBOX authority is recorded precisely in a tracked implementation note, including the adopted draft-spec drift disposition.
- Browser verification has its configured executable or the phase is visibly blocked before source execution.
- Phase 101 is not executed, Phase 103 is not entered, and the Writer scope fence remains intact.
- Plan 01 performs only protocol/environment preflight; no manual Git topology or source implementation work occurs.
</success_criteria>

<output>
Create `.planning/phases/102-paja-standard-nap-social-poc/102-01-SUMMARY.md` when done.
</output>
