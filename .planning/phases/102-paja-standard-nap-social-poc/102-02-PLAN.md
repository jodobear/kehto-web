---
phase: 102-paja-standard-nap-social-poc
plan: 02
type: execute
wave: 1
depends_on: [102-01]
files_modified:
  - packages/paja/src/browser-social-cache.ts
  - packages/paja/src/browser-social-cache.test.ts
  - packages/paja/src/browser-relay-runtime.ts
  - packages/paja/src/browser-adapter.ts
  - tests/e2e/paja-single-window.spec.ts
  - packages/services/src/identity-service.test.ts
autonomous: true
requirements: [PAJA-01, PAJA-02, PAJA-03]
must_haves:
  truths:
    - "A logged-in napplet receives the active public key and follows through the existing identity service, with the follows lookup bound to the public key obtained for that request."
    - "Paja warms a private, memory-only snapshot for the active account: a verified kind-3 contact list drives a base-router kind-0 query for its follows."
    - "The normal outbox service receives a decorated existing router and returns ordinary RelayEventResult values on the tracer path."
    - "A deterministic Paja iframe proves identity.getFollows and outbox.query without any application-specific social message surface."
  artifacts:
    - path: packages/paja/src/browser-social-cache.ts
      provides: "Paja-internal social snapshot factory with follows provider, active refresh, and OutboxRouter decorator."
      exports: [createPajaSocialCache]
    - path: packages/paja/src/browser-social-cache.test.ts
      provides: "Focused happy-path cache regression coverage using injected host seams."
    - path: packages/paja/src/browser-relay-runtime.ts
      provides: "Narrow host-owned contact-list loader for the private cache."
      exports: [createPajaContactListLoader]
    - path: packages/paja/src/browser-adapter.ts
      provides: "One base outbox router composed with the private cache for existing identity and outbox services."
    - path: tests/e2e/paja-single-window.spec.ts
      provides: "Browser-level standard identity-to-OUTBOX tracer using signed memory-relay fixtures."
    - path: packages/services/src/identity-service.test.ts
      provides: "Deferred-signer request-snapshot regression for the standard follows provider seam."
  key_links:
    - from: packages/paja/src/browser-adapter.ts
      to: packages/paja/src/browser-social-cache.ts
      via: "one base router feeds refreshActiveIdentity, getFollows, and decorate before createIdentityService/createOutboxService"
    - from: packages/paja/src/browser-social-cache.ts
      to: packages/paja/src/browser-relay-runtime.ts
      via: "createPajaContactListLoader uses the existing bootstrap-relay path for raw kind-3 acquisition"
    - from: packages/paja/src/browser-social-cache.ts
      to: packages/services/src/outbox-service.ts
      via: "the decorator returns OutboxResult with RelayEventResult entries for the unmodified standard envelope handler"
    - from: tests/e2e/paja-single-window.spec.ts
      to: packages/paja/src/browser-adapter.ts
      via: "the compiled Paja server routes fixture identity.getFollows and outbox.query envelopes through the actual service map"
  prohibitions:
    - requirement_id: PAJA-02
      category: values
      status: unverified
      flagged: true
      verification: judgment
      statement: "The social snapshot must remain Paja-private and must not be re-exported as a napplet-visible service or namespace."
      reason: "The cache is an optimization behind the selected standard NAP boundary."
    - requirement_id: PAJA-02
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "The required social snapshot must remain account-scoped memory and must not be coupled to generic simulation.cache.mode."
      reason: "Generic simulation cache controls do not define this required PoC behavior."
    - requirement_id: PAJA-03
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "Warm data must come from the base OUTBOX router as RelayEventResult values, not from a second relay client or raw kind-0 records."
      reason: "The established router owns validation, routing, and event identity semantics."
---

<objective>
Build the production-quality tracer through the existing Paja identity and OUTBOX services: one signed-in account, one verified follow list, and one followed kind-0 record returned through ordinary messages.

Purpose: Prove the chosen private-cache composition and the real iframe wire path before edge-case expansion.
Output: The private cache module, narrow contact-list loader, adapter wiring, service snapshot regression, and deterministic browser tracer.

Per D-03 and D-11, this is Paja-only standard-NAP composition. Per D-15, it retains the read-only identity contract. Per D-16, it uses the pinned draft `4589a8f9a16d8aa29b3740e2b3b0cdca11e0976e` and installed `@napplet/nap@0.28.0` types for OUTBOX result behavior, with drift recorded rather than a current-master claim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md
@.planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md
@.planning/phases/102-paja-standard-nap-social-poc/102-PATTERNS.md
@.planning/phases/102-paja-standard-nap-social-poc/102-VALIDATION.md
@packages/paja/src/browser-adapter.ts
@packages/paja/src/browser-relay-runtime.ts
@packages/paja/src/browser-upload.ts
@packages/paja/src/browser-upload.test.ts
@packages/services/src/identity-service.ts
@packages/services/src/identity-service.test.ts
@packages/services/src/outbox-service.ts
@tests/e2e/paja-single-window.spec.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Deliver one signed-in identity-to-followed-profile path through Paja</name>
  <precondition>The Wave 0 worktree `/workspace/projects/kehto/paja-social-cache-102` is clean, based on the freshly fetched upstream/main, and `/usr/bin/chromium` passed the configured executable assertion.</precondition>
  <read_first>
    - .planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md (D-03, D-11, D-15, and D-16)
    - .planning/phases/102-paja-standard-nap-social-poc/102-RESEARCH.md (Architecture Patterns 1-3 and NAP authority status)
    - .planning/phases/102-paja-standard-nap-social-poc/102-PATTERNS.md (social-cache, adapter, relay-runtime, and browser-test patterns)
    - packages/paja/src/browser-adapter.ts (createOutboxRouter, createDevServices, createPajaAdapter)
    - packages/paja/src/browser-relay-runtime.ts (bootstrap relay, contact-list, and filter helpers)
    - packages/paja/src/browser-upload.ts (generation-scoped snapshot pattern)
    - tests/e2e/paja-single-window.spec.ts (local Paja server and manual iframe envelope helpers)
  </read_first>
  <files>packages/paja/src/browser-social-cache.ts, packages/paja/src/browser-social-cache.test.ts, packages/paja/src/browser-relay-runtime.ts, packages/paja/src/browser-adapter.ts, tests/e2e/paja-single-window.spec.ts</files>
  <behavior>
    - Red: a valid signed kind-3 contact event for active account A with one valid p tag causes `identity.getFollows` to return that normalized public key through the existing result envelope and original correlation ID.
    - Red: the same active account warms a kind-0 query through the one base OutboxRouter and a subsequent ordinary `outbox.query({ kinds: [0], authors: [followed] })` carries a RelayEventResult for that profile.
    - Red: the deterministic browser fixture sends only standard identity/outbox envelopes, receives the active key, follows, and kind-0 query result, and sees no added social domain.
    - Green: adapter creation starts a non-blocking active-identity refresh and signer changes request another background refresh without delaying service construction.
  </behavior>
  <action>Work only in `/workspace/projects/kehto/paja-social-cache-102`. Start with the focused unit and browser assertions above, then implement the smallest complete path. Add `createPajaContactListLoader` to `browser-relay-runtime.ts`; it must reuse the existing bootstrap relay selection and request the captured author’s kind 3 records without duplicating NIP-07/NIP-46 relay logic. Add the module-private `createPajaSocialCache` factory in `browser-social-cache.ts`, with injected base router, contact loader, verifier, active-pubkey getter, and optional signer-change subscription. Its `getFollows(capturedPubkey)` must use that supplied key throughout asynchronous work and return a copied normalized follow list. Its active refresh must capture the active key once, load/verify the selected contact event, query followed kind-0 records through the supplied base router, and retain only returned RelayEventResult values for that account. Use a generation counter and active-key comparison to discard obsolete background writes; a correlated follows request retains its captured account response. Keep the cache memory-only and active-account-scoped even when generic simulation cache mode is disabled. In `browser-adapter.ts`, build `createOutboxRouter` once, compose one social cache around it, pass `socialCache.getFollows` to the existing identity service and `socialCache.decorate(baseRouter)` to the existing outbox service, then start the non-blocking refresh. Preserve the existing relay/identity capability guards, service names, ACL route, base router verification, and all non-query router operations. Add a short source or test-adjacent authority note naming NAP-IDENTITY `6461e4b37c29dc09a20dff35d9515889c4433874`, NAP-OUTBOX draft `4589a8f9a16d8aa29b3740e2b3b0cdca11e0976e`, installed `@napplet/nap@0.28.0` types, and the current-master OUTBOX document absence. Extend the existing Paja browser fixture with valid signed memory-relay kind-3/kind-0 records and manual standard envelope assertions; it must drive the actual iframe service map rather than a direct cache call. Do not add a social service key, re-export the cache from `packages/paja/src/index.ts`, create another relay pool, change ACL mappings, or touch any Writer source, test, fixture, smoke, or documentation path.</action>
  <verify>
    <automated>cd /workspace/projects/kehto/paja-social-cache-102 && ./node_modules/.bin/vitest run packages/paja/src/browser-social-cache.test.ts && pnpm --filter @kehto/paja build && pnpm exec playwright test tests/e2e/paja-single-window.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>The tracer proves a real signed-in Paja iframe can request the standard active identity, follows, and a followed kind-0 profile through the existing identity/outbox services. The cache has no public package export or injected service name, uses one base router, records the exact NAP authority/drift note nearby, and changes no Writer source, test, fixture, smoke, or documentation path.</acceptance_criteria>
  <done>PAJA-01 through PAJA-03 have one runnable, production-quality happy path from captured identity to a standard OUTBOX result.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the generic request-start identity snapshot contract</name>
  <precondition>The Plan 02 tracer’s Paja worktree exists and its dependency tree resolves the existing identity-service test suite.</precondition>
  <read_first>
    - .planning/phases/101-baseline-writer-contribution-preflight/101-CONTEXT.md (D-15)
    - packages/services/src/identity-service.ts (getCurrentPubkey, sendOptionalProviderResult, and identity.getFollows handling)
    - packages/services/src/identity-service.test.ts (collector and async assertion conventions)
    - .planning/phases/102-paja-standard-nap-social-poc/102-PATTERNS.md (identity provider regression pattern)
  </read_first>
  <files>packages/services/src/identity-service.test.ts</files>
  <action>In the isolated Paja worktree, add a deferred-signer regression to `identity-service.test.ts` before altering service code. Start `identity.getFollows` while `getPublicKey()` is pending for account A, change the mutable signer source to account B before resolving A, then resolve A. Assert the host follows provider is called exactly once with A, the correlated `identity.getFollows.result` retains the original request ID, and its returned follow list is A’s result. Keep the existing signed-out `identity.getPublicKey` empty-string and provider-error envelope behavior unchanged. If the test exposes a real generic contract defect, make the smallest service-source repair in the same worktree and update this plan’s execution summary; otherwise leave `identity-service.ts` untouched. Per D-15, retain read-only identity behavior and do not add a custom identity message or any signer/key exposure. Do not touch Writer paths.</action>
  <verify>
    <automated>cd /workspace/projects/kehto/paja-social-cache-102 && ./node_modules/.bin/vitest run packages/services/src/identity-service.test.ts packages/paja/src/browser-social-cache.test.ts</automated>
  </verify>
  <acceptance_criteria>A deferred request demonstrates that the existing identity service passes the request-start public key into the follows provider even if signer selection changes later, while result correlation and signed-out semantics remain standard. No Writer source, test, fixture, smoke, or documentation path is changed.</acceptance_criteria>
  <done>PAJA-01’s captured-key guarantee is protected at both the generic identity-service seam and the Paja tracer boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Signer identity to Paja cache | Mutable account selection crosses asynchronous contact-list and profile warm operations. |
| Untrusted relay event to follow set | Kind-3 tags influence which profile authors Paja reads. |
| Paja cache to standard outbox service | Private stored values are merged into a napplet-visible normal result. |
| Paja host to sandboxed iframe | The iframe receives service envelopes but must not receive host relay, signer, or cache authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|----------|----------|-----------|----------|-------------|-----------------|
| T-102-05 | Information Disclosure/Tampering | async account snapshots | high | mitigate | Task 1 captures the key, partitions state by account, and generation-checks background writes; Task 2 proves request correlation. |
| T-102-06 | Tampering | kind-3 relay input | high | mitigate | Task 1 verifies the selected contact event before deriving follows and accepts only normalized valid public keys. |
| T-102-07 | Elevation of Privilege | cache-backed profile read | high | mitigate | Task 1 warms through and decorates the existing base router, leaves ACL mapping and non-query router methods unchanged, and adds no service namespace. |
| T-102-08 | Repudiation | OUTBOX protocol claim | medium | mitigate | Task 1 records the pinned draft/types and master-path drift adjacent to the implementation/test. |
| T-102-09 | Denial of Service | followed-profile warm | medium | mitigate | Task 1 performs a narrow kind-0 authors query only when validated follows exist; no feed/pagination behavior is introduced. |
| T-102-SC | Tampering | package-manager supply chain | low | accept | This plan adds no package, manifest, or lockfile change; its worktree uses the validated existing dependency tree from Plan 01. |

## ASVS L1 Control-to-Task Mapping

| Control | Threat | Owning task | Evidence |
|---------|--------|-------------|----------|
| V2/V3 identity and session isolation | T-102-05 | Tasks 1-2 | Captured-key provider test plus generation/account-scoped cache behavior. |
| V5 untrusted input validation | T-102-06 | Task 1 | Verified contact event and validated normalized p tags before profile warm. |
| V4 access control | T-102-07 | Task 1 | Existing identity/outbox service and ACL route remain the only iframe path. |
| V5 protocol integrity | T-102-08 | Task 1 | Exact authority and drift note is colocated with the changed behavior. |
</threat_model>

<verification>
1. Run the focused social-cache and identity-service tests after each tracer commit.
2. Build `@kehto/paja` before the selected browser test because the browser suite imports Paja’s compiled package entry.
3. Confirm the browser fixture uses signed memory relay events and sends only normal identity/outbox envelopes through the iframe.
4. Inspect the adapter service map: existing service names remain, the cache is not exported from the package barrel, and ACL source is unchanged.
5. Confirm the nearby authority note distinguishes identity master conformity from draft OUTBOX upstream drift.
6. Confirm the implementation worktree has no Writer source, test, fixture, smoke, or documentation changes.
</verification>

<success_criteria>
- PAJA-01: The captured identity key determines the correlated follows lookup even across an async signer change.
- PAJA-02: Paja privately warms followed kind-0 records for the active account without durable storage or a new app-visible surface.
- PAJA-03: The tracer returns ordinary RelayEventResult data through the unmodified standard outbox envelope handler.
- The deterministic browser tracer is green with the configured Chromium executable.
</success_criteria>

<output>
Create `.planning/phases/102-paja-standard-nap-social-poc/102-02-SUMMARY.md` when done.
</output>
