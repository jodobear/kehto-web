---
phase: 103-paja-blossom-rail-poc
plan: "04"
type: execute
wave: 4
depends_on:
  - 103-03
files_modified:
  - tests/e2e/paja-single-window.spec.ts
  - packages/shell/src/shell-init.test.ts
  - packages/shell/src/napplet-namespace.test.ts
autonomous: true
requirements:
  - UPLOAD-02
  - POC-02
must_haves:
  truths:
    - "A real opaque iframe can perform the standard upload.upload then resource.bytes journey against controlled Blossom servers and receive exactly the stored verified bytes."
    - "Browser evidence proves ordered replicas, one retry for transient 5xx, failed-proof continuation, consent denial before egress, and cancellation after a durable first copy."
    - "The iframe sees the standard upload and resource APIs but no Blossom namespace, direct network credential, authorization event, private key, or window.nostr surface."
    - "Shell capability support remains distinct from current upload.info availability and the injected upload namespace remains limited to normal NAP-UPLOAD methods."
  artifacts:
    - "tests/e2e/paja-single-window.spec.ts: multi-server scripted Blossom fixture, stored GET bytes, response queues, header capture, and browser assertions for resource previews."
    - "packages/shell/src/shell-init.test.ts: support-versus-readiness capability regression."
    - "packages/shell/src/napplet-namespace.test.ts: standard upload/resource namespace-shape regression."
  key_links:
    - "opaque iframe -> runtime -> Paja replica controller -> controlled Blossom servers -> verifier -> exact resource grant -> opaque iframe resource.bytes result."
    - "shell-init.ts -> napplet-namespace.ts via advertised upload domain and normal upload method set, with availability reported in upload.info rather than capability removal."
  prohibitions:
    - requirement_id: POC-02
      category: interoperability
      status: unresolved
      verification: null
      statement: "The browser proof MUST NOT make a proprietary Paja or low-level Blossom API necessary for a standard-NAP client to upload or preview media."
      reason: "Spec-less prohibition probe fallback; browser and namespace regressions provide evidence while the item remains explicitly flagged-unverified."
---

<objective>
Turn the tracer and unit contracts into adversarial browser proof of the standard-only Paja Blossom rail, then protect the shell capability and namespace boundary with focused regressions.

Purpose: A secure host architecture is only credible when the real sandboxed iframe can complete the journey without inheriting upload/network/key authority.
Output: Controlled multi-server Playwright evidence and shell API-boundary tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-paja-blossom-rail-poc/103-CONTEXT.md
@.planning/phases/103-paja-blossom-rail-poc/103-RESEARCH.md
@.planning/phases/103-paja-blossom-rail-poc/103-PATTERNS.md
@.planning/phases/103-paja-blossom-rail-poc/103-03-SUMMARY.md
@tests/e2e/paja-single-window.spec.ts
@packages/shell/src/shell-init.ts
@packages/shell/src/shell-init.test.ts
@packages/shell/src/napplet-namespace.ts
@packages/shell/src/napplet-namespace.test.ts

<interfaces>
- `sendFixtureMessage()` and `readFixtureMessage()` already drive standard NAP envelopes through the real opaque iframe.
- `BlossomTestServer` currently records PUT bytes and authorization; extend it rather than substituting a mock upload service.
- `buildShellCapabilities()` advertises upload when the host wires an upload hook, while `upload.info` reports its current `enabled` state.
- The injected `makeUpload()` exposes info, upload, status, and onStatus under `window.napplet.upload`; it must remain the only client upload API.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the real opaque-iframe Blossom fixture into adversarial end-to-end proof</name>
  <read_first>
    - `git -C /workspace/projects/napplets/naps show a7cc17463cbf5d9cb87884b31071bc4fc826034c:naps/NAP-UPLOAD.md`
    - `git -C /workspace/projects/napplets/naps show fa6bcc6935aa19e7b70ab2a2c721dafca77c78e1:naps/NAP-RESOURCE.md`
    - `git -C /workspace/projects/napplets/naps show ca1d7ba594e6790785dc770227085d8648d39631:naps/NAP-BLOSSOM.md`
    - tests/e2e/paja-single-window.spec.ts
    - packages/paja/src/browser-upload.ts
    - packages/paja/src/blossom-verifier.ts
    - packages/paja/src/browser-host.ts
    - packages/services/src/resource-service.ts
    - docs/policies/SHELL-RESOURCE-POLICY.md
  </read_first>
  <files>tests/e2e/paja-single-window.spec.ts</files>
  <behavior>
    - Three configured fixture servers receive attempt traffic in configuration order; a 503 receives one retry, a malformed descriptor or altered stored GET continues, and only verified ordered successes populate url/fallbackUrls.
    - The standard resource.bytes result contains the original `[0,1,2,3,254,255]` payload after upload, not arbitrary remote content.
    - Consent denial happens before PUT; post-first-copy cancellation returns cancelled without url/fallbackUrls and logs a host-only retained-copy warning.
    - Decoded BUD-11 auth carries unpadded Base64url, expected kind/t/x/server/expiration tags, X-SHA-256, content length, and never enters iframe messages.
  </behavior>
  <action>
    Begin with failing Playwright assertions and extend the existing in-process fixture, not a mock service. Add `BlossomTestServer.responseQueues`, `storedBlobs`, `getRequests`, `queuePutResponse()`, `replaceStoredBlob()`, and `holdNextRequest()` so each server can script one 503, a malformed descriptor, a mismatched stored GET, a valid persistent object, and a deliberately stalled next replica. Persist uploads by SHA-256 and answer BUD-01 GET from that map; retain explicit test-only loopback verifier construction at `startPajaServer()`.

    Drive the opaque iframe through standard `upload.info`, `upload.upload`, `upload.status`, and `resource.bytes` envelopes. Use the real SHA-256 `7ea646958715ed687aa9ac2f5d785feb1a93411f4f25fdd6c7fcc6ab07fdf0e3`, compare resource `bodyBase64` and the returned Blob bytes to the fixture vector, and prove an altered stored blob creates neither a completed URL nor a preview. Assert all configured attempts and returned URL order, one retry on the transient response, continued later replicas after local failures, and no automatic timer-based attempt cancellation, per D-05 through D-10.

    Exercise denied consent before any PUT and teardown while the next replica is held after the first verified copy. Verify only one terminal cancelled result reaches the iframe, both URL fields are absent, and the parent Paja message log has the retained-copy diagnostic. Inspect the sandboxed frame directly: its NAP namespace exposes standard upload/resource methods, its `window.nostr` is absent, no authorization event/credential is recorded in iframe messages, and it has no low-level Blossom object. This proves D-01, D-02, D-13, D-18, D-20, and D-21 with the pinned drafts, not current-master conformance.
  </action>
  <verify>
    <automated>CI=1 corepack pnpm exec playwright test tests/e2e/paja-single-window.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Playwright runs the real Paja server and opaque iframe with controlled local Blossom services; no public server or relay is used.
    - The browser test asserts every BUD-11 header/tag, ordered attempt, retry count, full stored-byte verification consequence, and resource preview byte equality.
    - Consent and cancellation tests prove no hidden egress before consent and no napplet success URL after cancellation.
    - The iframe boundary assertions cover standard upload/resource presence and denial of direct key, auth-event, and low-level Blossom authority.
  </acceptance_criteria>
  <done>Real browser evidence proves the configured-replica Blossom journey and its hostile/race paths through only standard NAP messages.</done>
  <commit>test(103-04): prove standard Blossom replication in opaque iframe</commit>
</task>

<task type="auto">
  <name>Task 2: Lock capability advertisement and injected namespace to the standard surface</name>
  <read_first>
    - `git -C /workspace/projects/napplets/naps show a7cc17463cbf5d9cb87884b31071bc4fc826034c:naps/NAP-SHELL.md`
    - `git -C /workspace/projects/napplets/naps show a7cc17463cbf5d9cb87884b31071bc4fc826034c:naps/NAP-UPLOAD.md`
    - packages/shell/src/shell-init.ts
    - packages/shell/src/shell-init.test.ts
    - packages/shell/src/napplet-namespace.ts
    - packages/shell/src/napplet-namespace.test.ts
    - packages/paja/src/browser-upload.ts
    - packages/paja/src/browser-adapter.ts
  </read_first>
  <files>packages/shell/src/shell-init.test.ts, packages/shell/src/napplet-namespace.test.ts</files>
  <action>
    Add focused shell tests without changing public shell production code. In `shell-init.test.ts`, prove a host with a wired Blossom-capable upload hook advertises the `upload` domain regardless of a temporarily unavailable signer/configuration; use the Paja browser runtime tests to prove the complementary `upload.info.enabled: false` readiness signal. This enforces support-versus-availability rather than deleting capability support, per D-03 and the pinned NAP-SHELL `a7cc17463cbf5d9cb87884b31071bc4fc826034c`.

    In `napplet-namespace.test.ts`, instantiate injected `upload` and `resource` domains, assert the callable upload API is limited to `info`, `upload`, `status`, and `onStatus`, and assert resource preview remains callable only through `resource.bytes` / `bytesAsObjectURL`. Assert a non-domain candidate is not injected and that parent-source trust/correlation still governs results. Keep the test semantic rather than adding a shell alias or a new cancellation/replica API, per D-01, D-02, D-18, and D-23.
  </action>
  <verify>
    <automated>corepack pnpm exec vitest run packages/shell/src/shell-init.test.ts packages/shell/src/napplet-namespace.test.ts packages/paja/src/browser-upload.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Capability support remains present when Paja's backend currently reports unavailable, while availability remains a normal upload.info/result fact.
    - Namespace tests prove standard upload/resource methods and parent-origin correlation without extending window.napplet for Blossom transport, credentials, replica diagnostics, or cancellation.
    - Shell tests cite the pinned NAP-SHELL/UPLOAD refs and explicitly avoid a current-master draft claim.
  </acceptance_criteria>
  <done>The browser proof and shell regressions jointly demonstrate that standard NAP capability support is sufficient and no Paja authority crosses the iframe boundary.</done>
  <commit>test(103-04): guard standard upload capability boundary</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| opaque sandboxed iframe → ShellBridge | The fixture sends only untrusted standard envelopes. |
| Paja host → controlled Blossom fixture | The browser test observes real request headers, stored GETs, and response ordering. |
| shell capability handshake → injected namespace | Advertised support determines available domain objects before authored app code runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Residual risk |
|---|---|---|---|---|---|---|---|
| T-103-01 | Information disclosure | iframe upload path | high | mitigate | Browser assertions prove no direct server selection, credentials, authorization event, or key API reaches the frame. | Opaque-frame namespace and message-log checks. | An authorized standard upload can still disclose the bytes the user consented to store. |
| T-103-02 | Tampering | remote descriptor/blob | high | mitigate | Scripted altered GET and malformed descriptor cases prove no result/grant bypasses stored proof. | Playwright storage/proof scenarios. | Verified object bytes are retained in the window grant until teardown. |
| T-103-04 | Information disclosure | preview grant | high | mitigate | Browser executes standard resource.bytes only after verified grant and unit layer has cross-window rejection. | Resource preview vector assertion plus Plan 01 unit coverage. | The owning active window can preview its own verified Blob. |
| T-103-06 | Repudiation | cancellation race | high | mitigate | Hold a later replica after verified success, trigger teardown, and assert cancelled standard outcome plus host-only diagnostic. | Browser cancellation scenario. | Durable copies already stored by a compliant remote server can remain. |
| T-103-SC | Tampering | package manager | low | accept | No package changes are needed for controlled fixture coverage. | Diff scope audit. | Existing dependency posture remains unchanged. |
</threat_model>

<verification>
Run the focused Playwright spec after Task 1 and the focused shell/Paja Vitest set after Task 2. The final wave runs both again through the release gate.
</verification>

<success_criteria>
A real standard-NAP client journey is browser-proven across policy, transport, verification, resource preview, and authority isolation; an implementation cannot pass by exercising only mocked layers.
</success_criteria>

## Artifacts this phase produces

- `tests/e2e/paja-single-window.spec.ts` — `BlossomTestServer.responseQueues`, `storedBlobs`, `getRequests`, `queuePutResponse()`, `replaceStoredBlob()`, `holdNextRequest()`, and full standard-NAP browser scenarios.
- `packages/shell/src/shell-init.test.ts` — wired-support versus `upload.info` readiness regression.
- `packages/shell/src/napplet-namespace.test.ts` — normal upload/resource method-set and parent-source trust regression.

<output>
Create `.planning/phases/103-paja-blossom-rail-poc/103-04-SUMMARY.md` when done.
</output>
