---
phase: quick-260729-80s-sync-fork-main
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260729-80s-sync-github-fork-main-with-kehto-web-ups/260729-80s-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements:
  - UPLOAD-02
  - POC-02
status: planned
quick_id: 260729-80s
created: 2026-07-29
must_haves:
  truths:
    - "The fork baseline and all dependent local branches retain their original replay ranges after rebasing."
    - "Every remote rewrite is guarded by the exact origin object fetched before the rebase."
    - "PR #217 and Phase 103 retain their dependency order and documented quality disposition."
  artifacts:
    - path: ".planning/quick/260729-80s-sync-github-fork-main-with-kehto-web-ups/260729-80s-SUMMARY.md"
      provides: "Recorded refs, gate outcomes, leases, and synchronization disposition"
    - path: ".planning/STATE.md"
      provides: "Current project-state record without roadmap mutation"
  key_links:
    - from: "feat/paja-standard-nap-social-cache-current"
      to: "main"
      via: "rebase onto the synced fork baseline and guarded PR-head push"
    - from: "feat/103-paja-blossom-rail"
      to: "feat/paja-standard-nap-social-cache-current"
      via: "rebase preserving the 20-commit Phase 103 suffix"
---

# Quick Task 260729-80s: Sync Fork `main` and Rebase the Paja Stack Safely

## Goal

Rebase the fork's six commits on current `upstream/main`, then rebase PR #217, the local Phase 103 source branch, and the planning branch in dependency order without losing commits, altering unrelated worktrees, or force-pushing an unverified history. Preserve the PR's focused social-cache scope and the Phase 103 dependency relationship. Record the completed operation in the quick summary and project state; do not update the roadmap.

## Known topology to revalidate before any write

- `upstream/main` is `a3e73e75`; fork `origin/main` and local `main` are `6e1d0ead`; their merge base is `d4ba157d`. The fork has six unique commits and upstream has 340 unique commits. The fork-only range contains one merge commit, so its replay must use `--rebase-merges`.
- PR #217's head and local branch `feat/paja-standard-nap-social-cache-current` are `81185b45`, with 28 commits above `upstream/main@297b5478`. The remote PR head is the same commit and the PR targets `main`.
- `feat/103-paja-blossom-rail` is `ae5e4283`, has 20 commits above the PR head, and contains two merge commits. It is local-only: `origin` has no Phase 103 branch to rewrite.
- The primary worktree branch `docs/phase-101-preflight-context` is `aed7a59a`, is 34 commits ahead of its remote `c1b8e5a`, and contains 97 commits above the old fork `main` with four merge commits. It is not descended from the Phase 103 source branch.
- Leave `/workspace/projects/kehto/paja-social-cache-contribution`, `/workspace/projects/kehto/paja-social-cache-upstream-audit`, and both locked Claude worktrees untouched. Never use reset, checkout, worktree prune, or a force option on any worktree operation.
- Phase 103 remains blocked at its mandatory AI-slop gate. Per D-26 recorded in `.planning/STATE.md`, the four unrelated runtime warnings are not a permitted edit target for this focused source branch.

## Dependency graph

| Task | Needs | Creates | Execution rule |
|---|---|---|---|
| 1 | Clean named worktrees; readable remotes | Immutable local backup refs and verified rebase inputs | First; do not begin a rebase until it passes. |
| 2 | Task 1 backup refs and topology assertions | Rebases in local `main` → PR #217 → Phase 103 → planning order | Sequential because each new base is the next branch's parent. |
| 3 | Task 2 local histories and ranges | Gate evidence, guarded remote updates, quick summary, and state record | Last; no remote update before its local verification checks pass. |

## Tasks

<tasks>

<task type="tracer">
  <name>Task 1: Snapshot live refs, assert worktree safety, and establish recovery refs</name>
  <precondition>The primary, PR #217, and Phase 103 worktrees are clean; the quick plan itself has been committed so rebasing the primary planning worktree will not be blocked by an uncommitted plan artifact.</precondition>
  <files>.git/refs/backup/260729-80s/*, /workspace/projects/kehto/.tmp-fork-main-sync-260729-80s</files>
  <action>From `/workspace/projects/kehto/web`, run `git fetch --prune origin` followed by `git fetch --prune upstream`. Re-list worktrees and assert the exact branch-to-worktree mapping for the primary planning worktree, `/workspace/projects/kehto/paja-social-cache-rebase-probe`, and `/workspace/projects/kehto/paja-blossom-rail`; check their status output is empty. Inventory but do not enter, modify, unlock, prune, remove, or reassign the contribution, audit, or locked Claude worktrees. Capture post-fetch remote values before any rebase: origin main, origin PR head, origin planning head, upstream main, local main, PR head, Phase 103 head, and the primary planning head. Create durable local backup refs under `refs/backup/260729-80s/` for each captured value, naming the remote and local variants separately. Verify every backup ref resolves to the captured object.

Compute the four replay boundaries, then create backup refs for each boundary as well: fork merge base of origin main and upstream main, PR merge base with upstream main, Phase merge base with the old PR head, and planning merge base with old fork main. Assert the expected shape before proceeding: local main equals the fetched origin main; PR and its origin head agree; the Phase branch is based on the PR head; the planning branch is based on old fork main and is not based on the Phase source; and the Phase branch is absent from origin. Confirm the known counts and merge topology (fork six with one merge, PR 28 with no merge, Phase 20 with two merges). A forward-only upstream advance is allowed only when the former `a3e73e75` is an ancestor of the fetched upstream tip; recompute the current fork/upstream comparison and preserve the complete fork-only range. If origin main was rewritten rather than advanced, any PR remote head differs from the local PR head, a planning remote commit is not an ancestor of the local planning head, an expected parent/count is different, a target worktree is dirty, or a rebase is already in progress, stop before changing branch refs. Do not infer which commits can be overwritten.

Create only the disposable detached worktree `/workspace/projects/kehto/.tmp-fork-main-sync-260729-80s` at the backed-up origin-main object after verifying that directory does not already exist. This worktree is the sole location for rewriting the fork-main sequence; it avoids switching or repurposing any existing worktree.</action>
  <verify>
    <automated>git fetch --prune origin &amp;&amp; git fetch --prune upstream; git worktree list --porcelain; git -C /workspace/projects/kehto/web status --porcelain; git -C /workspace/projects/kehto/paja-social-cache-rebase-probe status --porcelain; git -C /workspace/projects/kehto/paja-blossom-rail status --porcelain; git show-ref --verify refs/backup/260729-80s/origin-main; git show-ref --verify refs/backup/260729-80s/pr-local; git show-ref --verify refs/backup/260729-80s/phase-local; git show-ref --verify refs/backup/260729-80s/planning-local</automated>
  </verify>
  <done>All mutable starting points and the exact pre-fetch remote lease values have local backup refs; the three target worktrees are clean; topology/count checks explain the complete replay ranges; and only the new detached scratch worktree may be used for the fork-main rebase.</done>
</task>

<task type="auto">
  <name>Task 2: Rebase the dependency stack locally with range proof and conflict discipline</name>
  <precondition>Task 1 backup refs exist, all topology assertions passed, and the disposable detached fork-main worktree is registered at the backed-up origin-main object.</precondition>
  <reversibility rating="costly">Remote branch history will later be replaced, but Task 1 preserves immutable local recovery refs and this task performs no remote writes.</reversibility>
  <files>.git/refs/heads/main, .git/refs/heads/feat/paja-standard-nap-social-cache-current, .git/refs/heads/feat/103-paja-blossom-rail, .git/refs/heads/docs/phase-101-preflight-context</files>
  <action>Recover all old tips and bases from `refs/backup/260729-80s/`; do not use remembered hashes. In the detached scratch worktree, run `git rebase --rebase-merges --onto <fetched-upstream-main> <fork-merge-base>` to replay exactly the fork-only history. Save its resulting tip as the new local main, verify it is not checked out in any worktree, then advance local `main` with `git branch -f main <new-main>`.

Rebase the existing checked-out dependency worktrees in this order only: in `/workspace/projects/kehto/paja-social-cache-rebase-probe`, run `git rebase --rebase-merges --onto <new-main> <old-pr-upstream-base>`; save the resulting new PR tip. In `/workspace/projects/kehto/paja-blossom-rail`, run `git rebase --rebase-merges --onto <new-pr-tip> <old-pr-tip>`; save the resulting Phase tip. Finally, in `/workspace/projects/kehto/web`, after asserting it remains on `docs/phase-101-preflight-context`, run `git rebase --rebase-merges --onto <new-main> <old-main-tip>`; save the resulting planning tip. This last range is all commits unique to the old fork main, not only the 34 commits ahead of the planning branch's remote, so the planning history is replayed once and the fork-main commits are supplied by the new base rather than duplicated.

On every conflict, before changing a file run `git status --short`, `git diff --name-only --diff-filter=U`, `git rebase --show-current-patch`, and inspect the common ancestor, ours, and theirs for every unmerged path. Resolve only after proving the resolution preserves the replayed commit's focused purpose and does not introduce a path outside that branch's original diff. Abort the active rebase with `git rebase --abort` and stop before any remote push if a conflict cannot be attributed to the displayed commit, alters the PR's focused social-cache scope, would absorb the unrelated runtime baseline prohibited by D-26, changes an upstream policy without an established source commit, or leaves an unproven merge result. An abort must be followed by a read-only assertion that the active worktree returned to its backup tip; do not reset another branch or worktree.

After all four rebases complete, inspect four `git range-diff` comparisons: old fork-only range against upstream-to-new-main, old PR range against new-main-to-new-PR, old Phase range against new-PR-to-new-Phase, and old planning range against new-main-to-new-planning. Require the respective replay counts to remain six, 28, 20, and the captured planning-range count (97 at planning time). Require ancestry new upstream → new main → new PR → new Phase and new main → new planning, while Phase must remain non-ancestor of planning. Run `git diff --check` on every rebased range. Treat an unexpected dropped, added, reordered, or materially changed range-diff entry as a stop condition until it is explained from upstream overlap; do not push a history merely because Git completed the rebase.</action>
  <verify>
    <automated>git merge-base --is-ancestor "$(git rev-parse refs/remotes/upstream/main)" "$(git rev-parse main)" &amp;&amp; git merge-base --is-ancestor "$(git rev-parse main)" "$(git rev-parse feat/paja-standard-nap-social-cache-current)" &amp;&amp; git merge-base --is-ancestor "$(git rev-parse feat/paja-standard-nap-social-cache-current)" "$(git rev-parse feat/103-paja-blossom-rail)" &amp;&amp; git merge-base --is-ancestor "$(git rev-parse main)" "$(git rev-parse docs/phase-101-preflight-context)" &amp;&amp; ! git merge-base --is-ancestor "$(git rev-parse feat/103-paja-blossom-rail)" "$(git rev-parse docs/phase-101-preflight-context)"; git range-diff refs/backup/260729-80s/fork-merge-base..refs/backup/260729-80s/origin-main refs/backup/260729-80s/upstream-main..main; git range-diff refs/backup/260729-80s/pr-upstream-base..refs/backup/260729-80s/pr-local main..feat/paja-standard-nap-social-cache-current; git range-diff refs/backup/260729-80s/pr-local..refs/backup/260729-80s/phase-local feat/paja-standard-nap-social-cache-current..feat/103-paja-blossom-rail; git range-diff refs/backup/260729-80s/origin-main..refs/backup/260729-80s/planning-local main..docs/phase-101-preflight-context; git diff --check main..feat/paja-standard-nap-social-cache-current &amp;&amp; git diff --check feat/paja-standard-nap-social-cache-current..feat/103-paja-blossom-rail &amp;&amp; git diff --check main..docs/phase-101-preflight-context</automated>
  </verify>
  <done>Local `main` contains current upstream plus exactly the preserved fork-only sequence; the new PR head has the preserved focused PR range; Phase 103 has the preserved 20-commit suffix on that PR; the planning branch has its complete old-main-relative range once; all backup refs remain available; and no remote ref has changed.</done>
</task>

<task type="auto">
  <name>Task 3: Revalidate implementation tips, push only verified refs under exact leases, and record the outcome</name>
  <precondition>Task 2 completed with accepted range-diff output, clean rebased worktrees, and no active rebase state.</precondition>
  <reversibility rating="costly">The three remote force updates replace public fork and PR history, so each uses the exact origin object captured after Task 1's fetch and is preceded by local proof plus a fresh remote-object comparison.</reversibility>
  <files>.planning/quick/260729-80s-sync-github-fork-main-with-kehto-web-ups/260729-80s-SUMMARY.md, .planning/STATE.md</files>
  <action>From the PR and Phase source worktrees, first run `corepack pnpm install --frozen-lockfile`, then run `corepack pnpm build`, `corepack pnpm type-check`, `corepack pnpm test:unit`, `corepack pnpm docs:check`, and `npx --no-install aislop scan -d`, requiring the mandated 100/100 result. Run the focused `CI=1 corepack pnpm exec playwright test tests/e2e/paja-single-window.spec.ts` and the full `CI=1 corepack pnpm exec playwright test` on both implementation tips. Preserve logs and exact head IDs for the quick summary. If PR #217 fails a required gate, stop without a remote push. If Phase 103 still fails only at the inherited quality baseline named in the blocker, leave its locally rebased source branch and all backup refs intact, do not publish the absent Phase branch, and record the continuing block without changing the prohibited runtime tests or the AI-slop configuration. Any different Phase failure stops all remote operations.

Determine the planning-branch gate set from `git diff --name-only <new-main>...<new-planning>`, not from its branch name. A range containing source, test, package, or published-document paths must receive the same build, type, unit, docs, AI-slop, and relevant Playwright proof as an implementation tip; a range limited to `.planning/` receives `git diff --check` and a source-scope audit only. In either case, retain the path list and result in the summary. Do not classify the planning branch as planning-only until the diff proves it; its known history includes mirrored Phase 102 source commits.

Only after the applicable gates and scope audits pass, compare each current remote object with its captured Task 1 backup object using `git ls-remote --heads origin` immediately before its push. Push fork main, the PR head branch, and the planning branch in that order with no bare force option: `git push origin <new-main>:refs/heads/main --force-with-lease=main:<backed-up-origin-main>`, `git push origin <new-pr>:refs/heads/feat/paja-standard-nap-social-cache-current --force-with-lease=feat/paja-standard-nap-social-cache-current:<backed-up-origin-pr>`, and, after recording the operation, `git push origin <final-planning>:refs/heads/docs/phase-101-preflight-context --force-with-lease=docs/phase-101-preflight-context:<backed-up-origin-planning>`. A changed remote object is an immediate stop before its corresponding push; never replace a lease value with a fresh value to bypass that protection. The local-only Phase branch receives no push. After the PR branch push, query PR #217 through `gh pr view 217 --repo kehto/web --json state,baseRefName,headRefName,headRefOid,mergeStateStatus` and require an open PR targeting `main` with the new PR OID.

Create the quick summary with old and new IDs, backup-ref namespace, each replay-range result, conflict outcomes, each command and result, exact lease OIDs, remote verification, the Phase 103 gate disposition, and the planning-diff classification. Update only the dated activity/accumulated-context facts in `.planning/STATE.md`; retain the Phase 103 blocked status unless the relevant gate result and subsequent Phase workflow actually resolve it. Do not update `.planning/ROADMAP.md`. Stage only the summary and state paths, commit them with a conventional `docs(quick-260729-80s): record fork sync rebase` subject and the required Claude co-author trailer, then push the resulting planning tip with its saved planning-branch lease. Finally remove only `/workspace/projects/kehto/.tmp-fork-main-sync-260729-80s` with ordinary `git worktree remove` after proving it is clean; do not use a force flag.</action>
  <verify>
    <automated>git -C /workspace/projects/kehto/paja-social-cache-rebase-probe status --porcelain &amp;&amp; git -C /workspace/projects/kehto/paja-blossom-rail status --porcelain &amp;&amp; git ls-remote --heads origin main feat/paja-standard-nap-social-cache-current docs/phase-101-preflight-context &amp;&amp; gh pr view 217 --repo kehto/web --json state,baseRefName,headRefName,headRefOid,mergeStateStatus &amp;&amp; git diff --check HEAD~1..HEAD</automated>
  </verify>
  <done>Every pushed branch was locally verified and updated only under its exact pre-fetch lease; PR #217 remains focused, open, and based on the synced fork main; Phase 103 remains a local child of the new PR and is either quality-green or explicitly recorded as still blocked under D-26; the planning branch preserves its rebased history plus an operation record; no unrelated worktree or roadmap entry changed; and the disposable worktree has been removed without force.</done>
</task>

</tasks>

## Must-haves

- The fork's replay range is based on fetched `upstream/main` and contains the full fork-only commit sequence exactly once.
- PR #217 retains its 28-commit social-cache range on the new fork baseline, remains open against `main`, and does not gain planning, Graphify, Writer, generated, Blossom, or unrelated runtime-cleanup scope.
- The 20 Phase 103 commits remain a child range of the new PR head; the local source branch is never published while its mandatory gate remains blocked.
- The planning branch is rebased from its complete old-main-relative range, not from its 34-commit remote divergence, and remains independent of the Phase 103 source branch.
- Every remote rewrite checks the saved pre-fetch remote object immediately before a `--force-with-lease=<branch>:<oid>` push.
- Local backup refs allow recovery of the original fork, PR, Phase, planning, and remote lease points after any stop condition.
- The quick summary and state contain the actual IDs and gate results. The roadmap is unchanged.

## Threat model

| Boundary | Threat | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| upstream and origin remotes → local refs | Remote moved while rebases were prepared | high | mitigate | Fetch before capture, store the fetched objects under backup refs, compare `ls-remote` immediately before each push, and use the captured OID in the corresponding lease. |
| local branch stack → GitHub PR | PR scope or parent is changed by replay/conflict handling | high | mitigate | Use fixed merge-base ranges, inspect every conflict before resolution, compare old/new ranges with `range-diff`, assert ancestry/counts, and block remote updates on unexplained range changes. |
| local worktree administration → unrelated agents | A rebase or cleanup changes an occupied worktree | high | mitigate | Rebase only the three named clean target worktrees, use a new detached scratch worktree for main, and never prune, unlock, remove, reset, or switch another worktree. |
| Phase 103 source → quality policy | A baseline warning is hidden by unrelated source/config edits | medium | mitigate | Re-run the mandatory scan, preserve the blocked disposition under D-26 when it remains below target, and exclude runtime baseline/config changes from conflict resolutions. |
| planning artifact commit → milestone record | Synchronization metadata inaccurately marks Phase 103 complete | medium | mitigate | Record command output and actual IDs, retain the blocked phase state unless the phase workflow resolves it, and exclude the roadmap from the commit. |

## Source coverage audit

| Source | Item | Task | Status |
|---|---|---|---|
| GOAL | Sync fork main with upstream and rebase the dependent PR, Phase source, and planning branches safely | 1–3 | COVERED |
| REQ | No roadmap requirement is assigned to this quick Git-history operation; it does not implement UPLOAD-02 or POC-02 | N/A | OUT OF SCOPE |
| RESEARCH | Level 0 discovery: Git rebase, worktree, range-diff, and lease semantics are established repository tooling; no library/API choice is introduced | 1–3 | COVERED |
| CONTEXT | Preserve other worktrees, PR #217 scope, Phase dependency order, exact lease protection, full gate reruns, planning-diff classification, and D-26 runtime-boundary rule | 1–3 | COVERED |

## Success criteria

1. Backup refs make every original local tip and every remote lease OID recoverable.
2. Range proofs show the expected fork, PR, Phase, and planning sequences rebased without duplicates or unexplained changes.
3. Required gates have current results for each implementation tip, and branch-specific outcomes determine whether it may be pushed.
4. Fork main, PR #217, and planning branch remote updates use exact pre-fetch `--force-with-lease` values only after verification; no Phase 103 remote is created.
5. `/workspace/projects/kehto/web/.planning/quick/260729-80s-sync-github-fork-main-with-kehto-web-ups/260729-80s-SUMMARY.md` and `.planning/STATE.md` record the result without a roadmap edit.
