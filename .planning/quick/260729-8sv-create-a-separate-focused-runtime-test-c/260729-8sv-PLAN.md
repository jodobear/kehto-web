---
phase: quick-260729-8sv-runtime-test-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/runtime/src/discovery.test.ts
  - packages/runtime/src/dispatch.test.ts
  - .planning/quick/260729-8sv-create-a-separate-focused-runtime-test-c/260729-8sv-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements:
  - QUICK-260729-8SV
status: planned
quick_id: 260729-8sv
created: 2026-07-29
must_haves:
  truths:
    - "The four flagged runtime registry test cases assert observable service attachment, removal, replacement, and message-dispatch outcomes."
    - "The cleanup branch starts directly from freshly fetched upstream/main and the source PR diff contains only the two declared runtime test files."
    - "Focused tests, build, type-check, unit tests, docs validation, whitespace audit, and the AI-slop scan all pass; the scan reports 100 / 100 Healthy."
    - "PR #217, the Phase 103 worktree/ref, and refs/backup/260729-80s remain unchanged; stack resumption waits for the cleanup PR to merge."
  artifacts:
    - path: "packages/runtime/src/discovery.test.ts"
      provides: "Behavioral regression coverage for runtime service registration, removal, and replacement."
    - path: "packages/runtime/src/dispatch.test.ts"
      provides: "End-to-end service routing and post-unregistration fallback coverage."
    - path: ".planning/quick/260729-8sv-create-a-separate-focused-runtime-test-c/260729-8sv-SUMMARY.md"
      provides: "Cleanup branch base, NAP authority, gate evidence, PR URL, and held-stack disposition."
    - path: ".planning/STATE.md"
      provides: "Accurate Phase 103 blocker state after the isolated cleanup PR is opened."
  key_links:
    - from: "packages/runtime/src/discovery.test.ts"
      to: "packages/runtime/src/runtime.ts"
      via: "registered NIP-5D session followed by keys.registerAction routes through the mutable keys service entry"
      pattern: "registerService\\('keys'"
    - from: "packages/runtime/src/dispatch.test.ts"
      to: "packages/runtime/src/domain-handlers.ts"
      via: "keys.registerAction receives a service reply while registered and the runtime fallback after removal"
      pattern: "keys\\.registerAction"
    - from: "test/runtime-registry-assertions"
      to: "upstream/main"
      via: "new dedicated worktree created at the fetched upstream object and source-only pull request against kehto/web main"
      pattern: "upstream/main"
---

<objective>
Isolate and remove the four inherited runtime-test quality findings without enlarging PR #217 or the Phase 103 contribution.

Purpose: Restore the repository-wide mandatory AI-slop baseline to 100 / 100 through meaningful Runtime lifecycle tests, so the blocked Phase 103 quality gate can later be rerun on its preserved stack.
Output: A source-only cleanup PR from a new upstream-based worktree, plus a planning-branch summary and state record that explicitly defer the stack rebase until that PR merges. This enforces D-26's separation of unrelated runtime cleanup from the focused Phase 103 source branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/103-paja-blossom-rail-poc/103-06-BLOCKER.md
@.planning/quick/260729-80s-sync-github-fork-main-with-kehto-web-ups/260729-80s-PLAN.md
@/workspace/projects/kehto/paja-social-cache-rebase-probe/packages/runtime/src/discovery.test.ts
@/workspace/projects/kehto/paja-social-cache-rebase-probe/packages/runtime/src/dispatch.test.ts
@/workspace/projects/kehto/paja-social-cache-rebase-probe/packages/runtime/src/runtime.ts
@/workspace/projects/kehto/paja-social-cache-rebase-probe/packages/runtime/src/domain-handlers.ts
@/workspace/projects/kehto/paja-social-cache-rebase-probe/packages/runtime/src/test-utils.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Create an upstream-based cleanup worktree and prove one registered-service dispatch path</name>
  <precondition>The primary planning worktree, the PR #217 worktree, and the Phase 103 worktree are clean; both origin (the fork) and upstream (kehto/web) remotes exist; and `$HOME/.worktrees/kehto/test-runtime-registry-assertions` is not already attached to another branch.</precondition>
  <files>packages/runtime/src/discovery.test.ts</files>
  <action>From `/workspace/projects/kehto/web`, record the current planning HEAD and leave that checkout on `docs/phase-101-preflight-context`; do not rebase, reset, checkout, push, or otherwise rewrite it. Fetch only `upstream` with pruning, record the fetched `upstream/main` OID (currently expected to advance from `a3e73e75` only by normal upstream history), and create branch `test/runtime-registry-assertions` in the dedicated worktree `$HOME/.worktrees/kehto/test-runtime-registry-assertions` directly at that fetched object. Use `git worktree list --porcelain` before creation and halt rather than repurposing an occupied worktree. Do not enter, alter, unlock, prune, or remove `/workspace/projects/kehto/paja-social-cache-rebase-probe`, `/workspace/projects/kehto/paja-social-cache-contribution`, `/workspace/projects/kehto/paja-blossom-rail`, the locked Claude worktrees, or any `refs/backup/260729-80s/*` ref.

Before changing a test, perform the project-required NAP check against the authoritative `napplet/naps` NAP-KEYS source, recording the exact repository ref and blob/commit in the future summary and PR body. Confirm the existing keys request/result vocabulary used by the test; the cleanup changes no wire contract. If the authoritative source cannot be obtained, record the spec gap and stop before a Kehto edit instead of establishing NAP behavior locally. In the new cleanup worktree, use existing dependencies only: if its root `node_modules` is absent, compare its `pnpm-lock.yaml` byte-for-byte with the clean rebase-probe worktree and create an ignored symlink to that worktree's root `node_modules`; halt on a lockfile mismatch and do not run a package installation.

Replace the first flagged lifecycle expectation in `discovery.test.ts` with a full Runtime path. Add only the existing Vitest spy import and existing runtime test helpers needed to register `WINDOW_ID` as a NIP-5D session. Register a `keys` ServiceHandler whose message callback is a spy, send a valid `keys.registerAction` envelope through `runtime.handleMessage`, and assert that the callback receives the expected window ID, envelope, and runtime reply callback while no fallback reply is recorded. This exercises the mutable service registry through the public NIP-5D dispatcher rather than asserting that registration merely completes. Leave production Runtime code, test utilities, dependency metadata, AI-slop policy, and every file outside the declared test scope untouched, per D-26.</action>
  <verify>
    <automated>cd "$HOME/.worktrees/kehto/test-runtime-registry-assertions" &amp;&amp; corepack pnpm exec vitest run packages/runtime/src/discovery.test.ts -t "registerService adds a service to the registry"</automated>
  </verify>
  <done>The cleanup worktree is a new `test/runtime-registry-assertions` branch rooted at the fetched upstream/main OID, NAP-KEYS authority is recorded, and the registration test proves a session-authorized keys envelope reaches the new handler.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the remaining registry no-op checks with lifecycle and fallback assertions</name>
  <precondition>Task 1's dedicated cleanup worktree exists at the fetched upstream baseline, its focused tracer test is green, and its only tracked source edit is `packages/runtime/src/discovery.test.ts`.</precondition>
  <files>packages/runtime/src/discovery.test.ts, packages/runtime/src/dispatch.test.ts</files>
  <action>In the cleanup worktree, replace the remaining two flagged cases in `discovery.test.ts` and the flagged lifecycle case in `dispatch.test.ts`; retain the existing test organization and make no Runtime implementation change. For unregistration, use a `keys` handler with an `onUnregistered` spy, register then remove it, dispatch `keys.registerAction` from a registered session, and assert both the detach hook and the built-in `keys.registerAction.result` fallback. Prove the detached handler did not receive this post-removal envelope.

For replacement, register two distinct `keys` handlers under the same name, instrument the original detach hook and both message callbacks, then dispatch one registered-session envelope. Assert that replacement detaches the original handler and routes the exact envelope only to the replacement handler. In `dispatch.test.ts`, turn the lifecycle case into a complete dispatcher assertion: a registered `keys` handler must use its provided send callback to emit a distinguishable result; after `unregisterService`, a second request must receive the canonical Runtime fallback instead, with no second custom-handler invocation. Use the existing `createNip5dSessionEntry`, `findEnvelopeResponse`, `MockRuntimeContext`, and Vitest patterns rather than reaching into private Runtime state. Preserve the current NAP message shapes and ACL/session setup so the tests prove service-registry behavior, not an unrelated ingress rejection.</action>
  <verify>
    <automated>cd "$HOME/.worktrees/kehto/test-runtime-registry-assertions" &amp;&amp; corepack pnpm exec vitest run packages/runtime/src/discovery.test.ts packages/runtime/src/dispatch.test.ts</automated>
  </verify>
  <done>All four previously flagged test cases prove observable attachment, detachment, replacement, callback routing, or fallback behavior; the two focused runtime test files pass together without modifying production code or configuration.</done>
</task>

<task type="auto">
  <name>Task 3: Run release-quality gates, publish a source-only cleanup PR, and record the merge-wait state</name>
  <precondition>Task 2 is green, the cleanup branch contains only the two declared runtime-test paths relative to its captured upstream/main base, and GitHub CLI authentication can push to the fork and create a pull request for kehto/web.</precondition>
  <reversibility rating="costly">The public source commit and pull request are recoverable but visible to collaborators; publish only after the exact scope and required gate evidence are recorded.</reversibility>
  <files>.planning/quick/260729-8sv-create-a-separate-focused-runtime-test-c/260729-8sv-SUMMARY.md, .planning/STATE.md</files>
  <action>From `$HOME/.worktrees/kehto/test-runtime-registry-assertions`, derive `BASE` from its fetched upstream/main tracking ref and require the sorted range path list from `BASE...HEAD` to equal exactly `packages/runtime/src/discovery.test.ts` and `packages/runtime/src/dispatch.test.ts`. Run `git diff --check "$BASE...HEAD"`, the two focused Vitest files, `corepack pnpm build`, `corepack pnpm type-check`, `corepack pnpm test:unit`, `corepack pnpm docs:check`, and `npx --no-install aislop scan -d`. Require the scan's 100 / 100 Healthy result and a clean source-worktree status after all gates. Playwright is not in scope because this diff changes only Node-runtime unit tests and touches neither playground nor shell/browser wiring; record that scope-based determination with the other gate evidence rather than executing an unrelated browser suite.

Stage only the two runtime test paths, recheck the staged path list and whitespace diff, and create one conventional source commit, `test(runtime): replace registry tautologies`, with the required `Co-Authored-By: Claude &lt;noreply@anthropic.com&gt;` trailer. Push only `test/runtime-registry-assertions` to the fork origin without a force option. Open a PR in `kehto/web` targeting `main` from the fork branch. Its body must state the inherited 85/100 baseline cause, the four behavioral test replacements, the exact NAP-KEYS authority/ref checked and whether the existing vocabulary is conformant, every gate result, the source-only two-file scope, the no-changeset rationale, and the Playwright scope determination. Query the opened PR and require an open state, `main` base, fork cleanup head, and an OID equal to the local source commit.

Only after the source PR exists, return to `/workspace/projects/kehto/web` and create the quick summary plus a narrowly dated `.planning/STATE.md` update. Record the captured base/head IDs, NAP authority, gates, source-scope proof, PR URL/OID, and that Phase 103 remains blocked until the cleanup PR merges. State that the separate continuation will recover the preserved `refs/backup/260729-80s` stack, rerun its gates, and use exact lease-guarded force pushes only after merge. Do not perform that continuation now: do not alter local main, PR #217, Phase 103, its source worktree, the backup refs, or the planning branch's topology. Commit only the planning summary and state locally on the planning branch with a conventional docs commit and the required co-author trailer; do not push that planning branch as part of this cleanup operation and do not update the roadmap.</action>
  <verify>
    <automated>cd "$HOME/.worktrees/kehto/test-runtime-registry-assertions" &amp;&amp; BASE="$(git merge-base upstream/main HEAD)" &amp;&amp; test "$(git diff --name-only "$BASE...HEAD" | LC_ALL=C sort)" = "$(printf '%s\n' packages/runtime/src/discovery.test.ts packages/runtime/src/dispatch.test.ts | LC_ALL=C sort)" &amp;&amp; git diff --check "$BASE...HEAD" &amp;&amp; corepack pnpm exec vitest run packages/runtime/src/discovery.test.ts packages/runtime/src/dispatch.test.ts &amp;&amp; corepack pnpm build &amp;&amp; corepack pnpm type-check &amp;&amp; corepack pnpm test:unit &amp;&amp; corepack pnpm docs:check &amp;&amp; npx --no-install aislop scan -d</automated>
  </verify>
  <done>The opened cleanup PR targets kehto/web main, contains exactly the two runtime test files and one source commit, has complete required gate evidence including AI-slop at 100 / 100, and the planning branch locally records a merge-wait disposition without moving the preserved PR #217 or Phase 103 stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning worktree → cleanup worktree | Planning metadata and the isolated source contribution must not cross into each other's commits. |
| upstream/main → cleanup branch | The cleanup branch must inherit the current upstream baseline, not a locally rebased fork or Phase 103 history. |
| cleanup branch → fork origin and kehto/web PR | A narrowly reviewed test-only contribution becomes externally visible. |
| NIP-5D fixture → Runtime dispatcher | Test inputs must traverse the real session and service-routing boundary instead of certifying an internal no-op. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260729-8sv-01 | Tampering | cleanup branch provenance | high | mitigate | Fetch upstream immediately before worktree creation, branch directly at the fetched object, and assert the final diff against that base. |
| T-260729-8sv-02 | Spoofing | runtime test fixture and service routing | medium | mitigate | Register a real NIP-5D session, use the public Runtime API, assert exact handler calls/replies, and distinguish service replies from built-in fallback responses. |
| T-260729-8sv-03 | Repudiation | PR and planning record | medium | mitigate | Capture base/head OIDs, NAP authority, full gate output, diff path proof, PR metadata, and the deferred-stack status in the summary/state record. |
| T-260729-8sv-04 | Tampering | preserved PR #217 and Phase 103 stack | high | mitigate | Do not switch, rewrite, force-push, or modify named worktrees or refs; defer every rebase and lease-guarded push until the cleanup PR has merged, per D-26. |
| T-260729-8sv-SC | Tampering | dependency supply chain | low | accept | No package-manager installation or dependency/configuration change is permitted; reuse only a lockfile-identical existing dependency tree. |
</threat_model>

<verification>
1. Confirm the cleanup worktree is on `test/runtime-registry-assertions` and its first parent is the freshly fetched upstream/main object.
2. Run the focused runtime tests after the tracer and after all four replacements.
3. Before committing, require an exact two-path source diff, `git diff --check`, and clean status after every quality gate.
4. Require build, type-check, full unit suite, docs check, and the AI-slop scan; the latter must report 100 / 100 Healthy.
5. Confirm the PR is open against `kehto/web:main` with the local cleanup commit OID, then verify that no PR #217, Phase 103, or backup-stack ref changed.
6. Verify the planning summary/state commit is local to the planning branch and absent from the source PR range.

## Source coverage audit

| Source | ID | Required item | Plan task | Status | Notes |
|--------|----|---------------|-----------|--------|-------|
| GOAL | — | Isolated runtime-test cleanup PR that restores the global quality baseline without disturbing the preserved stack | 1–3 | COVERED | The source branch, behavioral assertions, gates, PR, and merge-wait record are all explicit. |
| REQ | QUICK-260729-8SV | Four Runtime test assertions become behaviorally meaningful; source PR stays two-file scoped | 1–3 | COVERED | This quick task has no ROADMAP phase_req_ids or shipped package-output requirement. |
| RESEARCH | — | Level 0: existing Runtime API, domain handler, Vitest helpers, and gate commands supply the required pattern; no new dependency or external API choice exists | 1–3 | COVERED | Package installation is excluded. |
| CONTEXT | D-26 | Keep unrelated runtime cleanup out of the focused Phase 103 contribution and leave PR #217/Phase 103 stack intact | 1–3 | COVERED | Cleanup is isolated in a new upstream worktree; stack resume waits for merge. |
| CONTEXT | user constraints | Use a dedicated worktree/branch from refreshed upstream, execute gates, open the fork PR, and retain planning artifacts outside the PR | 1–3 | COVERED | The plan specifies paths, branch, exact scope proof, PR metadata, and local planning record. |
</verification>

<success_criteria>
1. The four formerly non-observable registry tests now fail if attachment, removal, replacement, handler reply routing, or post-removal fallback regresses.
2. The cleanup branch is born from fresh upstream/main and its PR diff has exactly `packages/runtime/src/discovery.test.ts` and `packages/runtime/src/dispatch.test.ts`.
3. Required non-browser gates are green, with AI-slop at 100 / 100 and no configuration or dependency change.
4. The cleanup PR is open against kehto/web main; no changeset is present because package output is unchanged.
5. The planning summary and state retain the blocked Phase 103 disposition and defer the backup-stack rebase, validation, and lease-guarded force pushes until the cleanup PR merges.
</success_criteria>

<output>
Create `.planning/quick/260729-8sv-create-a-separate-focused-runtime-test-c/260729-8sv-SUMMARY.md` when done.
</output>
