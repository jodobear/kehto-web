---
phase: quick-260731-299-fork-main-mirror
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260731-299-sync-fork-origin-main-exactly-to-current/260731-299-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements:
  - QUICK-260731-299
status: planned
quick_id: 260731-299
created: 2026-07-31
must_haves:
  truths:
    - "Fork origin/main is byte-for-byte the freshly fetched upstream/main tip, with no historic fork-planning commits replayed."
    - "A fork-owned integration branch contains the verified PR #217 feature delta on current upstream contracts, while PR #217 itself remains unchanged and open."
    - "Phase 103 retains only its own source suffix on the pinned integration base and is published only if every required gate passes."
    - "The planning-only branch remains planning-only after rebasing onto the mirrored main, and its rewrite is protected by the captured remote lease."
    - "The planning record states the exact safe repin route after PR #217 merges."
  artifacts:
    - path: ".planning/quick/260731-299-sync-fork-origin-main-exactly-to-current/260731-299-SUMMARY.md"
      provides: "Captured refs, leases, range proofs, gate results, and PR-merge repin instructions."
    - path: ".planning/STATE.md"
      provides: "Current Phase 103/integration-base disposition without a roadmap mutation."
  key_links:
    - from: "origin/main"
      to: "upstream/main"
      via: "exact-OID update protected by the pre-write origin/main lease"
    - from: "integration/fork-v1.29-paja-social-blossom-pr217-pinned"
      to: "verified PR #217 head"
      via: "current-upstream rebase of the audited PR feature range"
    - from: "feat/103-paja-blossom-rail"
      to: "integration/fork-v1.29-paja-social-blossom-pr217-pinned"
      via: "replay of only the saved Phase 103 suffix above its preserved old PR parent"
---

<objective>
Make the fork default branch a clean mirror of current upstream, then preserve forward progress by pinning the reviewed PR #217 feature delta in a fork-owned integration branch and rebasing Phase 103 on it.

Purpose: Fork planning history must not contaminate `main`, while Phase 103 can proceed from a verified current-contract integration base without merging or altering PR #217.
Output: Durable recovery refs, synced fork `main`, a gated integration branch, a conditionally published Phase 103 branch, and a rebased planning-only record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260729-80s-sync-github-fork-main-with-kehto-web-ups/260729-80s-PLAN.md
@.planning/quick/260729-9sr-reorganize-fork-owned-work-coherently-ke/260729-9sr-PLAN.md
@.planning/debug/resolved/pr-217-dependency-regress.md
@.planning/phases/103-paja-blossom-rail-poc/103-06-BLOCKER.md
</context>

## Locked-decision map

- D-01: `origin/main` becomes exactly the newly fetched `upstream/main`; the six obsolete fork-planning commits are not replayed.
- D-02: The verified PR #217 head is integrated only into the named fork-owned pin; PR #217 stays open, unmerged, and unmodified.
- D-03: Phase 103 is rebased from its saved Phase-only parent/range, retains recovery refs, and may be published only after all gates pass.
- D-04: The planning-only branch carries `.planning/**` only and is lease-rewritten from the mirrored main.
- D-05: All unrelated worktrees and every existing `refs/backup/*` object remain intact; no upstream push or PR merge occurs.

## Dependency graph

| Task | Needs | Creates | Execution rule |
|---|---|---|---|
| 1 | Clean named worktrees and reachable remotes | Backup namespace, exact remote leases, mirror `main` | First; halt before every write on ref or worktree mismatch. |
| 2 | Task 1 refs and synced main | Gated pinned integration branch and rebased/gated Phase 103 source | Second; PR #217 ref is read-only throughout. |
| 3 | Tasks 1–2 range/gate evidence | Rebased planning branch, Summary, State record, repin procedure | Last; push the planning branch only after its final path proof. |

<tasks>

<task type="tracer">
  <name>Task 1: Snapshot every mutable input and mirror fork main to current upstream</name>
  <precondition>The planning-only worktree is clean on `docs/fork-v1.29-paja-social-blossom-planning-only` and this quick plan is committed before its later rebase; the Phase 103 worktree is clean; all worktrees listed in the preflight manifest remain registered.</precondition>
  <reversibility rating="costly">D-01 replaces the fork default-branch history, but immutable local backup refs plus an exact remote lease preserve every pre-write object.</reversibility>
  <files>.git/refs/backup/260731-299/*, refs/heads/main, refs/remotes/origin/main</files>
  <action>From `/workspace/projects/kehto/v129-planning-only`, record the complete `git worktree list --porcelain` manifest and status for the planning worktree, `/workspace/projects/kehto/paja-blossom-rail`, the PR/rebase-probe worktree, contribution/audit worktrees, the detached temporary sync worktree, and locked agent worktrees. Per D-05, inventory all `refs/backup/*` without modifying, pruning, unlocking, removing, switching, or repurposing any of them. Fetch `origin` and `upstream` with pruning, then query both remotes directly. Capture the post-fetch/direct OIDs for local main, origin main, upstream main, origin planning branch, the PR #217 head branch, local Phase 103, local historical Phase parent `refs/backup/260729-9sr/pr-217-local`, and the verified PR head from `gh pr view 217 --repo kehto/web --json state,baseRefName,headRefOid,mergeStateStatus`.

Require the PR to remain open against `main`, its remote/GitHub head to equal `5a1cd985cbf05a31c1789aecf32a4bac4dc4d3c3`, and the fresh upstream tip to be a descendant of the known `3a4d71a8f8860890cdcbf8fa25a11780fbe7a55f`; any changed PR head, rewritten upstream history, dirty target, in-progress rebase, unexpected checked-out `main`, or remote disagreement stops the operation before a ref write. Create a new dated backup namespace with separate refs for every captured local/remote value and the old Phase parent; verify each ref resolves exactly. Treat captured `origin/main` as the sole main lease and captured `origin/docs/fork-v1.29-paja-social-blossom-planning-only` as the later planning lease.

Per D-01, prove neither local nor origin main may contribute a replay range: record `git log --left-right --cherry-pick --oneline <upstream>...<origin-main>`, then require the new local `main` to equal the fetched upstream OID using an expected-old-value `git update-ref` or a branch update only after proving it is not checked out. Push only `<upstream-oid>:refs/heads/main` with `--force-with-lease=refs/heads/main:<captured-origin-main-oid>`; do not use a bare force flag. Re-query origin and require its main OID and local main OID both equal the saved upstream OID. Record the complete worktree/ref manifest for Task 3 comparison.</action>
  <verify>
    <automated>git -C /workspace/projects/kehto/v129-planning-only show-ref --verify refs/backup/260731-299/origin-main &amp;&amp; git -C /workspace/projects/kehto/v129-planning-only show-ref --verify refs/backup/260731-299/upstream-main &amp;&amp; git -C /workspace/projects/kehto/v129-planning-only show-ref --verify refs/backup/260731-299/pr217-head &amp;&amp; test "$(git -C /workspace/projects/kehto/v129-planning-only rev-parse main)" = "$(git -C /workspace/projects/kehto/v129-planning-only rev-parse upstream/main)" &amp;&amp; test "$(git -C /workspace/projects/kehto/v129-planning-only ls-remote --heads origin refs/heads/main | cut -f1)" = "$(git -C /workspace/projects/kehto/v129-planning-only rev-parse upstream/main)"</automated>
  </verify>
  <done>Recovery refs and exact leases exist for all mutable points; local/origin main equal the freshly fetched upstream tip; the former fork-planning sequence is preserved only in backup refs; and no protected worktree or upstream ref changed.</done>
</task>

<task type="auto">
  <name>Task 2: Pin current-contract PR #217 content and rebase the preserved Phase 103 suffix</name>
  <precondition>Task 1 completed its exact mirror proof; `refs/backup/260731-299/pr217-head` remains the verified PR head; the historical Phase parent `refs/backup/260729-9sr/pr-217-local` is an ancestor of the Phase 103 tip; and the named integration branch does not already exist locally or on origin.</precondition>
  <reversibility rating="costly">The new integration and optional Phase remote branches are externally visible, but each is additive or lease-guarded and their prior local tips remain under dated recovery refs.</reversibility>
  <files>$HOME/.worktrees/kehto/integration-fork-v1.29-paja-social-blossom-pr217-pinned, refs/heads/integration/fork-v1.29-paja-social-blossom-pr217-pinned, refs/heads/feat/103-paja-blossom-rail</files>
  <action>Create `/home/at/.worktrees/kehto/integration-fork-v1.29-paja-social-blossom-pr217-pinned` only after confirming no existing registered worktree owns that path or branch. Start `integration/fork-v1.29-paja-social-blossom-pr217-pinned` at the saved PR #217 head, determine its exact upstream parent with `git merge-base <pr-head> <synced-upstream-main>`, and require that parent to be `7fef1d51`'s full object plus an ancestor of the saved upstream tip. Rebase the feature range with `git rebase --rebase-merges --onto <synced-upstream-main> <pr-base>` in this new worktree. Do not check out, modify, force-push, close, comment on, or merge PR #217. On a conflict, inspect the current patch, base/ours/theirs, and affected path before resolution; abort and stop if the result would restore a stale contract, broaden the reviewed social-cache scope, or cannot be justified by current upstream overlap.

Prove the integration range with `git range-diff <pr-base>..<saved-pr-head> <synced-upstream-main>..HEAD`, ancestry assertions, and `git diff --check`. Run the repository's semver-aware guard as `node scripts/check-napplet-dependency-direction.mjs <synced-upstream-main> HEAD`; compare the complete package-manifest, lockfile, and JSR metadata deltas from the old and new feature ranges and require no dependency/version regression. Require the expected PR feature paths and the absence of planning artifacts in the integration diff. Materialize exactly the locked dependency tree with `corepack pnpm install --frozen-lockfile`; if Turbo cannot locate pnpm, use the established temporary Corepack pnpm shim on PATH without changing package-manager configuration. Run build, type-check, unit, docs, AI-slop requiring `100 / 100 Healthy`, the focused Paja Playwright spec, the full Playwright suite, the dependency-direction guard, and whitespace checks. Push this newly named branch once with a normal `git push -u origin`; verify remote OID equals the gated integration tip.

Then preserve and replay only Phase 103 work: capture its current tip and the saved old PR-parent tip in the new backup namespace, assert the parent is an ancestor of the Phase tip, and run `git rebase --rebase-merges --onto <integration-tip> <saved-phase-parent>` in `/workspace/projects/kehto/paja-blossom-rail`. This deliberately excludes the obsolete PR prefix from the Phase replay per D-03. Require a Phase range-diff between `<saved-phase-parent>..<old-phase-tip>` and `<integration-tip>..<new-phase-tip>`, exact new ancestry, and a Phase diff that excludes planning artifacts and unrelated cleanup. Run the same frozen-install, build, type, unit, docs, 100/100 AI-slop, focused/full Playwright, dependency-direction, and whitespace gates from the rebased Phase worktree. Publish `feat/103-paja-blossom-rail` to origin only if every required gate passes, using a lease against any captured remote ref (or a normal new-branch push if absent); otherwise retain the rebased local branch and backups, publish nothing for Phase 103, and record the precise blocker. No upstream push is permitted.</action>
  <verify>
    <automated>cd /home/at/.worktrees/kehto/integration-fork-v1.29-paja-social-blossom-pr217-pinned &amp;&amp; BASE="$(git rev-parse refs/backup/260731-299/upstream-main)" &amp;&amp; git merge-base --is-ancestor "$BASE" HEAD &amp;&amp; node scripts/check-napplet-dependency-direction.mjs "$BASE" HEAD &amp;&amp; corepack pnpm install --frozen-lockfile &amp;&amp; corepack pnpm build &amp;&amp; corepack pnpm type-check &amp;&amp; corepack pnpm test:unit &amp;&amp; corepack pnpm docs:check &amp;&amp; npx --no-install aislop scan -d &amp;&amp; CI=1 corepack pnpm exec playwright test tests/e2e/paja-single-window.spec.ts &amp;&amp; CI=1 corepack pnpm exec playwright test &amp;&amp; git diff --check</automated>
  </verify>
  <done>The fork has a normally pushed current-upstream integration pin whose replay preserves PR #217's verified feature delta and current dependency contracts; PR #217 remains untouched; Phase 103 contains only its preserved suffix above that pin and is remotely published only with complete green evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Rebase the planning-only record, publish it under lease, and record the merge repin</name>
  <precondition>Tasks 1–2 have preserved their backup refs and produced accepted range/gate outcomes; the planning worktree is clean on `docs/fork-v1.29-paja-social-blossom-planning-only`; and its remote still equals Task 1's captured planning-branch lease immediately before its push.</precondition>
  <reversibility rating="costly">D-04 rewrites the existing fork planning branch, but an immutable old tip and a captured remote lease allow safe recovery if another writer moves it.</reversibility>
  <files>.planning/quick/260731-299-sync-fork-origin-main-exactly-to-current/260731-299-SUMMARY.md, .planning/STATE.md</files>
  <action>In `/workspace/projects/kehto/v129-planning-only`, recover the old main, old planning tip, and planning remote lease from `refs/backup/260731-299/` rather than remembered hashes. Before rebasing, require every path in `<old-main>..<old-planning-tip>` to begin `.planning/`; preserve the branch tip under a dedicated backup ref. Rebase with `git rebase --rebase-merges --onto <synced-main> <old-main>` and inspect/abort unresolved conflicts rather than accepting a path outside `.planning/**`. Compare old/new planning ranges with `git range-diff`, require local main ancestry, run `git diff --check`, and require every path in `<synced-main>..HEAD` to remain under `.planning/`.

Create the quick summary with pre/post OIDs, all lease OIDs, backup namespaces, worktree-preservation checks, mirror proof, integration/Phase range proofs, package/lock/JSR audit, exact command outcomes, Phase publication decision, and the fact that PR #217 was not changed. Update only the dated activity, integration-base, and Phase 103 continuation facts in STATE; do not mark Phase 103 complete or change ROADMAP. Per D-05, document the mandatory repin procedure: after PR #217 merges, fetch upstream; require the saved PR head to be an ancestor of the upstream merge result; back up the then-current integration/Phase tips and capture integration/Phase origin leases; move the integration branch to merged `upstream/main` without replaying PR content now present in main; replay only the saved Phase suffix above the old integration tip; range-diff, rerun the full gates, then lease-push both fork branches. A changed lease, a missing merged PR head, an unexplained range change, or a failing gate halts that future procedure.

Stage only the summary and STATE paths, commit with `docs(quick-260731-299): record fork mirror and integration pin` plus the required Claude co-author trailer, then prove the final branch remains planning-only from synced main. Query origin directly and require its planning OID still equals the saved Task 1 lease; lease-push `docs/fork-v1.29-paja-social-blossom-planning-only` with that exact expected OID, verify the resulting remote head equals local HEAD, and re-compare the saved worktree/ref manifest to ensure no unrelated ref or worktree changed. Do not push upstream or merge any pull request.</action>
  <verify>
    <automated>cd /workspace/projects/kehto/v129-planning-only &amp;&amp; BASE="$(git rev-parse refs/backup/260731-299/upstream-main)" &amp;&amp; git merge-base --is-ancestor "$BASE" HEAD &amp;&amp; git diff --check "$BASE..HEAD" &amp;&amp; git diff --name-only "$BASE..HEAD" | while IFS= read -r path; do case "$path" in .planning/*) ;; *) exit 1 ;; esac; done &amp;&amp; test "$(git ls-remote --heads origin refs/heads/docs/fork-v1.29-paja-social-blossom-planning-only | cut -f1)" = "$(git rev-parse HEAD)"</automated>
  </verify>
  <done>The planning branch is a lease-protected, planning-only descendant of mirrored main; Summary and State record the actual result and future post-merge repin; every protected worktree/backup ref remains intact; and no upstream ref or pull request was changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream/origin remotes → local refs | A remote could move while the local mirror and replays are prepared. |
| reviewed PR #217 → fork integration branch | Content replay must retain the reviewed feature delta without restoring superseded contracts. |
| old PR stack → Phase 103 | Phase history has a distinct parent range that must not replay the old PR prefix. |
| local planning record → fork planning branch | A shared public branch rewrite needs scope proof and an exact lease. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260731-299-01 | Tampering | origin/main | high | mitigate | Capture post-fetch direct OID, back it up, compare immediately before the exact force-with-lease mirror update, and verify final equality to upstream. |
| T-260731-299-02 | Tampering | integration replay | high | mitigate | Pin the reviewed PR OID, rebase only its verified base-to-head range, range-diff, run the dependency-direction guard, and block contract regressions. |
| T-260731-299-03 | Tampering | Phase 103 replay | high | mitigate | Use the saved old PR-parent as the Phase-only range boundary, retain backup tips, range-diff, and publish only after every gate passes. |
| T-260731-299-04 | Elevation of privilege | remote operations | high | mitigate | Limit writes to fork origin, use normal new-branch pushes or captured leases, and prohibit upstream pushes and PR merge actions. |
| T-260731-299-05 | Repudiation | continuation state | medium | mitigate | Record actual IDs, commands, gate outcomes, publication decisions, and the mechanically safe repin route in Summary and State. |
| T-260731-299-SC | Tampering | package installation | low | mitigate | Frozen-lockfile materialization plus diff/status checks and the dependency-direction guard prevent dependency-tree mutation. |
</threat_model>

<verification>
1. Compare local/origin main OIDs to the saved fetched upstream OID after the lease-protected mirror update.
2. Require PR #217 GitHub/remote identity checks before integration and verify the PR is still open afterward.
3. Require integration and Phase range-diffs, full gate chains, dependency-direction proof, and clean path/scope audits before their remote publication.
4. Require planning range-diff, a planning-prefix-only final diff, exact remote lease, and post-push OID equality.
5. Re-compare worktree/ref preservation manifests and require no upstream remote write.
</verification>

## Source coverage audit

| Source | ID | Required item | Task | Status |
|---|---|---|---|---|
| GOAL | — | Mirror fork main to current upstream while retaining a forward-progress Phase 103 base | 1–3 | COVERED |
| REQ | QUICK-260731-299 | Safe main mirror, pinned integration, Phase/planning rebases, gate evidence, and documented repin | 1–3 | COVERED |
| RESEARCH | — | Level 0: established Git backup, lease, rebase, range-diff, worktree, and repository gate patterns | 1–3 | COVERED |
| CONTEXT | D-01 | Exact current-upstream origin/main mirror without replayed planning commits | 1 | COVERED |
| CONTEXT | D-02 | Preserve unmodified PR #217 while producing a fork-owned current-contract pin | 2 | COVERED |
| CONTEXT | D-03 | Preserve/rebase only Phase 103 suffix and publish only after green gates | 2 | COVERED |
| CONTEXT | D-04 | Preserve planning-only scope and lease-push its existing branch | 3 | COVERED |
| CONTEXT | D-05 | Preserve all backup refs/worktrees; prohibit upstream push and PR merge; record merge repin | 1–3 | COVERED |

<success_criteria>
1. `origin/main`, local `main`, and the freshly fetched `upstream/main` resolve to one OID after Task 1.
2. `integration/fork-v1.29-paja-social-blossom-pr217-pinned` is current-upstream based, contains the audited PR #217 delta, passes the full specified gates, and leaves PR #217 untouched.
3. Phase 103 is a range-proven child of the integration pin and has a remote branch only after its equivalent full gate chain passes.
4. `docs/fork-v1.29-paja-social-blossom-planning-only` differs from mirrored main only under `.planning/`, is updated under the captured lease, and records the actual operation.
5. Summary provides a tested-on-paper post-merge repin sequence that reuses upstream's merged PR content and replays only Phase 103 work.
</success_criteria>

<output>
Create `.planning/quick/260731-299-sync-fork-origin-main-exactly-to-current/260731-299-SUMMARY.md` when done.
</output>
