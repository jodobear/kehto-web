---
phase: quick-260729-9sr-planning-only-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260729-9sr-reorganize-fork-owned-work-coherently-ke/260729-9sr-PLAN.md
  - .planning/quick/260729-9sr-reorganize-fork-owned-work-coherently-ke/260729-9sr-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements:
  - QUICK-260729-9SR
status: planned
quick_id: 260729-9sr
created: 2026-07-29
must_haves:
  truths:
    - "`origin/test/runtime-registry-assertions` remains exactly `3866fbc041463a6de0c82bd325256a9e5ede189b`."
    - "`docs/fork-v1.29-paja-social-blossom-planning-only` starts at local `main` `15822032bc5eb853a9d1f6c6501012c7481e875d` and contains planning artifacts only."
    - "The initial extraction commit has a `.planning` tree byte-identical to the captured mixed planning source tree."
    - "The final branch changes only `.planning/**` from local main; its documented self-referential delta from the captured source is limited to the quick summary and State record."
    - "Only a normal push to fork `origin` publishes the new planning branch; `origin/main`, the mixed planning remote, upstream, PR #217, Phase 103, protected backup refs, and existing worktrees retain their captured identities."
  artifacts:
    - path: ".planning/quick/260729-9sr-reorganize-fork-owned-work-coherently-ke/260729-9sr-PLAN.md"
      provides: "Committed extraction procedure captured in the source planning tree."
    - path: ".planning/quick/260729-9sr-reorganize-fork-owned-work-coherently-ke/260729-9sr-SUMMARY.md"
      provides: "New-branch record of source/base IDs, tree proofs, scope proof, protected refs, publication result, and continuation boundary."
    - path: ".planning/STATE.md"
      provides: "New-branch activity record distinguishing planning-only publication from deferred cleanup and stack work."
  key_links:
    - from: "refs/backup/260729-9sr/source-planning-tip"
      to: "docs/fork-v1.29-paja-social-blossom-planning-only initial extraction commit"
      via: "git restore of the complete `.planning` subtree followed by matching tree-object and path-diff proof"
      pattern: "source-planning-tip"
    - from: "docs/fork-v1.29-paja-social-blossom-planning-only"
      to: "origin/docs/fork-v1.29-paja-social-blossom-planning-only"
      via: "ordinary `git push -u origin` after the local tree and preservation audits"
      pattern: "docs/fork-v1.29-paja-social-blossom-planning-only"
    - from: "refs/backup/260729-9sr/preexisting/*"
      to: "pre-extraction backup refs, PR #217, Phase 103, and remote tracking values"
      via: "object-ID comparison before and after extraction"
      pattern: "refs/backup/260729-9sr"
---

<objective>
Extract the complete planning record from the mixed branch into a clean, fork-owned planning branch rooted at the locally synchronized fork baseline, then publish only that branch after proving scope and preservation invariants.

Purpose: Keep milestone planning reviewable and recoverable without pushing the mixed implementation history or changing protected source and cleanup work.
Output: `docs/fork-v1.29-paja-social-blossom-planning-only` in `/workspace/projects/kehto/v129-planning-only`, local recovery refs under `refs/backup/260729-9sr/`, a planning-only commit sequence, and its quick summary/State record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260729-80s-sync-github-fork-main-with-kehto-web-ups/260729-80s-PLAN.md
@.planning/quick/260729-8sv-create-a-separate-focused-runtime-test-c/260729-8sv-PLAN.md
</context>

## Locked-decision map

- D-01: Keep `origin/test/runtime-registry-assertions` fixed at `3866fbc041463a6de0c82bd325256a9e5ede189b`.
- D-02: Extract a planning-only branch and never push the current mixed `docs/phase-101-preflight-context` history.

## Dependency graph

| Task | Needs | Creates | Execution rule |
|---|---|---|---|
| 1 | Committed quick plan on the mixed branch; known local `main`; readable fork remotes | Immutable source/protection refs and a clean destination worktree | First; halt if any protected identity or destination precondition differs. |
| 2 | Task 1 source/base refs and destination worktree | Exact `.planning` extraction commit on the new branch | Second; performs the full subtree materialization and proof before operational records exist. |
| 3 | Task 2 exact-tree commit; Task 1 protection manifest | Final planning records, final source-scope audit, and fork-only branch publication | Last; no remote operation until all local proofs pass. |

<tasks>

<task type="tracer">
  <name>Task 1: Freeze the mixed planning source and create its isolated fork destination</name>
  <precondition>The current primary worktree is clean on `docs/phase-101-preflight-context`, and this quick plan is already committed in its HEAD before extraction begins.</precondition>
  <files>.git/refs/backup/260729-9sr/*, .git/info/260729-9sr-worktrees-before, /workspace/projects/kehto/v129-planning-only</files>
  <action>From `/workspace/projects/kehto/web`, use read-only Git checks to confirm the current branch remains `docs/phase-101-preflight-context`, its HEAD descends from `b87350b0`, local `main` is exactly `15822032bc5eb853a9d1f6c6501012c7481e875d`, `refs/remotes/origin/main` is exactly `6e1d0eadc7384fd6e0db4efa4765fe0d7f41ab19`, and `upstream/main` is exactly `a3e73e75a2cda7253474147a56f066e808a97624`. Query fork `origin` directly and require the cleanup branch OID to equal `3866fbc041463a6de0c82bd325256a9e5ede189b` per D-01. Do not fetch, rebase, reset, switch, delete, or push any existing branch or worktree in this task.

Set `SOURCE_TIP` to the committed primary HEAD and `BASE_TIP` to local `main`. Before creating anything, require the destination directory, local `docs/fork-v1.29-paja-social-blossom-planning-only` ref, remote `origin/docs/fork-v1.29-paja-social-blossom-planning-only` ref, and `refs/backup/260729-9sr/` namespace to be absent. Record the complete preexisting `git worktree list --porcelain` output in the common Git directory under `info/260729-9sr-worktrees-before`. Mirror every preexisting `refs/backup/*` object under `refs/backup/260729-9sr/preexisting/` without altering the source refs, then create immutable backup refs for `SOURCE_TIP`, `BASE_TIP`, the local PR #217 branch, the local Phase 103 branch, and the remote-tracking refs for the cleanup, PR #217, origin main, and mixed planning branches. Verify every new ref resolves to the object captured. This makes pre-extraction state recoverable while preserving all earlier backup refs and their values.

Create `docs/fork-v1.29-paja-social-blossom-planning-only` with `git worktree add -b` at `BASE_TIP` in `/workspace/projects/kehto/v129-planning-only`. Use this `/workspace` path per the explicit locked task constraint, rather than the repository's usual worktree root. Require the new worktree to be clean, attached to the new branch, and exactly at `BASE_TIP`; require every previously registered worktree to retain its recorded HEAD, branch/detached state, and lock metadata, allowing only the new destination stanza. Do not repurpose the temporary fork-sync worktree or any PR, Phase 103, audit, contribution, or locked Claude worktree. D-02 requires the mixed source branch to remain a preserved recovery history, so this task creates no commit or remote update on it.</action>
  <verify>
    <automated>test "$(git branch --show-current)" = docs/phase-101-preflight-context &amp;&amp; git merge-base --is-ancestor b87350b0 HEAD &amp;&amp; test "$(git rev-parse main)" = 15822032bc5eb853a9d1f6c6501012c7481e875d &amp;&amp; test "$(git rev-parse refs/remotes/origin/test/runtime-registry-assertions)" = 3866fbc041463a6de0c82bd325256a9e5ede189b &amp;&amp; test "$(git ls-remote --heads origin refs/heads/test/runtime-registry-assertions | cut -f1)" = 3866fbc041463a6de0c82bd325256a9e5ede189b &amp;&amp; test "$(git rev-parse refs/backup/260729-9sr/source-planning-tip)" = "$(git rev-parse HEAD)" &amp;&amp; test "$(git -C /workspace/projects/kehto/v129-planning-only branch --show-current)" = docs/fork-v1.29-paja-social-blossom-planning-only &amp;&amp; test "$(git -C /workspace/projects/kehto/v129-planning-only rev-parse HEAD)" = 15822032bc5eb853a9d1f6c6501012c7481e875d</automated>
  </verify>
  <done>The mixed source tip and protected preexisting state have durable backup refs, the cleanup branch is proven unchanged at its locked OID, all prior worktrees remain registered, and the new clean worktree is rooted exactly at local main without modifying an existing branch or remote.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize and commit the exact planning subtree on the clean branch</name>
  <files>/workspace/projects/kehto/v129-planning-only/.planning/**</files>
  <action>Work only in `/workspace/projects/kehto/v129-planning-only`. Resolve `SOURCE_TIP` and `BASE_TIP` from the Task 1 backup refs rather than remembered hashes, assert HEAD still equals `BASE_TIP`, and restore the complete `.planning` tree from `SOURCE_TIP` into both the index and worktree with `git restore --source=&lt;SOURCE_TIP&gt; --staged --worktree -- .planning`. This must carry every planning addition, modification, deletion, and rename, including phase 101–103 artifacts and both preceding quick-plan directories; do not select files individually.

Before committing, compare the staged tree against the source tree and require every staged path relative to `BASE_TIP` to start with `.planning/`. Run the whitespace audit and reject any status path outside that prefix. This path-prefix proof is also the explicit exclusion audit for implementation, test, package, changeset, documentation, and Graphify paths. Stage only `.planning`, recheck the staged name-status list, and create one coherent commit with subject `docs(fork-v1.29-paja-social-blossom): extract planning-only history` plus the required Claude co-author trailer. Save the resulting `EXTRACTION_TIP` in the task record. Prove the `.planning` tree object at `EXTRACTION_TIP` is byte-identical to `SOURCE_TIP:.planning`, then preserve that commit unchanged as the exact source snapshot for Task 3. D-02 prohibits replaying or publishing the mixed source commits themselves; this branch consists of the local-main base plus the new planning-only commit.</action>
  <verify>
    <automated>DEST=/workspace/projects/kehto/v129-planning-only; SOURCE=refs/backup/260729-9sr/source-planning-tip; BASE=refs/backup/260729-9sr/local-main; test "$(git -C "$DEST" rev-parse "$SOURCE:.planning")" = "$(git -C "$DEST" rev-parse HEAD:.planning)" &amp;&amp; git -C "$DEST" diff --quiet "$SOURCE" HEAD -- .planning &amp;&amp; git -C "$DEST" diff --check "$BASE..HEAD" &amp;&amp; git -C "$DEST" diff --name-only "$BASE..HEAD" | while IFS= read -r path; do case "$path" in .planning/*) ;; *) exit 1 ;; esac; done</automated>
  </verify>
  <done>The clean branch has one planning-only extraction commit above local main, its complete `.planning` subtree exactly matches the captured mixed-source tree, and every changed path from the baseline is inside `.planning/`.</done>
</task>

<task type="auto">
  <name>Task 3: Record the extraction on the new branch, prove final scope, and publish only to fork origin</name>
  <precondition>The fork remote accepts a normal push for `docs/fork-v1.29-paja-social-blossom-planning-only`; if transport authentication fails, halt at the dynamic authentication gate after retaining the local planning-only commits and without attempting another remote operation.</precondition>
  <reversibility rating="costly">Publishing the new fork branch is externally visible, but it is an additive branch with durable local source and protection refs; its ordinary push does not rewrite an existing remote ref.</reversibility>
  <files>/workspace/projects/kehto/v129-planning-only/.planning/quick/260729-9sr-reorganize-fork-owned-work-coherently-ke/260729-9sr-SUMMARY.md, /workspace/projects/kehto/v129-planning-only/.planning/STATE.md</files>
  <action>In the new planning-only worktree, retain `EXTRACTION_TIP` from Task 2 and again prove it has the exact `.planning` tree of `SOURCE_TIP`. Create the quick summary on this new branch, never on the mixed source worktree. Record `SOURCE_TIP`, `BASE_TIP`, `EXTRACTION_TIP`, branch/worktree names, every protected OID captured in Task 1, the complete scope/tree proof, the normal fork-origin push command, and the continuation boundary. Use an `Edit` limited to the dated activity and accumulated-context facts in this branch's `.planning/STATE.md`; retain the Phase 103 blocked status and do not update the roadmap.

The summary and State record must say that the existing cleanup branch remains held at the D-01 OID, cleanup PR creation is separately blocked on GitHub PR authentication, no cleanup PR is created here, and later cleanup/stack continuation operates from fork-owned topic branches only. They must also say that this task creates no upstream branch, performs no upstream push, does not merge to `origin/main`, and leaves the mixed `docs/phase-101-preflight-context` remote unchanged. Stage exactly the summary and State paths, commit `docs(quick-260729-9sr): record planning-only extraction` with the required Claude co-author trailer, and capture the final local tip.

Before publication, require the initial extraction tree equality, require the final comparison from `SOURCE_TIP` to the final tip to list exactly the summary and State paths as the documented self-referential delta, and require every comparison from `BASE_TIP` to the final tip to have the `.planning/` prefix. Re-compare the complete `refs/backup/260729-9sr/preexisting/` mirror to its original refs; require the local PR #217 and Phase 103 refs plus direct fork values for the cleanup, PR #217, origin main, and mixed planning branches to equal their snapshots. Parse the saved worktree manifest and require every preexisting stanza to retain its captured metadata, allowing only the intended new worktree. Then push only `docs/fork-v1.29-paja-social-blossom-planning-only` to `origin` with `git push -u origin docs/fork-v1.29-paja-social-blossom-planning-only:refs/heads/docs/fork-v1.29-paja-social-blossom-planning-only`, without any force option. Query the remote head afterward and require it equals the final local tip. Do not fetch or push `upstream`, and do not push, delete, force-update, merge, or rebase any existing fork branch.</action>
  <verify>
    <automated>DEST=/workspace/projects/kehto/v129-planning-only; SOURCE=refs/backup/260729-9sr/source-planning-tip; BASE=refs/backup/260729-9sr/local-main; test "$(git -C "$DEST" rev-parse "$SOURCE:.planning")" = "$(git -C "$DEST" rev-parse "$(git -C "$DEST" rev-parse HEAD~1):.planning")" &amp;&amp; test "$(git -C "$DEST" diff --name-only "$SOURCE..HEAD" | LC_ALL=C sort)" = "$(printf '%s\n' .planning/STATE.md .planning/quick/260729-9sr-reorganize-fork-owned-work-coherently-ke/260729-9sr-SUMMARY.md | LC_ALL=C sort)" &amp;&amp; git -C "$DEST" diff --check "$BASE..HEAD" &amp;&amp; git -C "$DEST" diff --name-only "$BASE..HEAD" | while IFS= read -r path; do case "$path" in .planning/*) ;; *) exit 1 ;; esac; done &amp;&amp; test "$(git ls-remote --heads origin refs/heads/test/runtime-registry-assertions | cut -f1)" = 3866fbc041463a6de0c82bd325256a9e5ede189b &amp;&amp; test "$(git ls-remote --heads origin refs/heads/docs/fork-v1.29-paja-social-blossom-planning-only | cut -f1)" = "$(git -C "$DEST" rev-parse HEAD)"</automated>
  </verify>
  <done>The final remote planning branch is a normal fork-origin publication whose full baseline diff is planning-only; the mixed source branch, origin main, cleanup branch, PR #217, Phase 103, prior backup refs, and prior worktrees retain their captured identities; and the only final delta from the frozen source planning tree is the summary and State record on the new branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mixed planning source → clean destination | The extraction must preserve the complete planning tree while preventing mirrored implementation history from entering the new branch. |
| existing worktrees and backup refs → Git operations | Shared recovery points and active topic work must survive new branch/worktree creation unchanged. |
| local planning branch → fork origin | A public fork branch may be published only after local scope and identity proofs, without rewriting any remote branch. |
| cleanup PR auth status → continuation record | Missing GitHub PR authority must remain an explicit blocker rather than causing an unrelated upstream or source-branch action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260729-9sr-01 | Tampering | branch base and source snapshot | high | mitigate | Assert local main and source ancestry, create durable source/base refs before extraction, then compare the initial `.planning` tree objects. |
| T-260729-9sr-02 | Tampering | planning-only scope | high | mitigate | Restore only the source `.planning` subtree into a clean baseline worktree, require every baseline diff path to have the planning prefix, and run whitespace checks. |
| T-260729-9sr-03 | Tampering | protected refs and worktrees | high | mitigate | Mirror all preexisting backup objects, snapshot protection refs and worktree metadata, then compare each captured identity before publication. |
| T-260729-9sr-04 | Elevation of privilege | fork remote publication | high | mitigate | Require a nonexistent destination remote branch, use one named non-force origin push, verify the returned remote OID, and avoid all upstream operations. |
| T-260729-9sr-05 | Repudiation | deferred cleanup/stack continuation | medium | mitigate | Put cleanup-auth blocker, protected OIDs, scope proofs, and explicit continuation boundaries in the new-branch quick summary and State record. |
| T-260729-9sr-SC | Tampering | dependency supply chain | low | accept | The operation installs no package and changes no dependency, lockfile, or build configuration. |
</threat_model>

<verification>
1. Confirm the captured source tip contains this committed quick plan and local main remains `15822032bc5eb853a9d1f6c6501012c7481e875d` before creating the new branch.
2. Confirm fork `origin/test/runtime-registry-assertions` is `3866fbc041463a6de0c82bd325256a9e5ede189b` before and after all local work.
3. At the Task 2 extraction tip, compare `SOURCE_TIP:.planning` and `HEAD:.planning` object IDs and require an empty `.planning` diff.
4. On the final tip, require every baseline diff path to begin `.planning/`; require the source-to-final path list to be precisely the State record and quick summary self-delta; run `git diff --check`.
5. Compare the backup-ref mirror, protected branch snapshots, direct fork refs, and saved worktree metadata before publishing; permit only the new worktree and new fork planning branch.
6. Verify the only remote write is the normal origin push of `docs/fork-v1.29-paja-social-blossom-planning-only`, then require the remote OID equals the final local tip.

## Source coverage audit

| Source | ID | Required item | Plan task | Status | Notes |
|--------|----|---------------|-----------|--------|-------|
| GOAL | — | Reorganize fork-owned planning work into a coherent clean planning branch while preserving protected topic work | 1–3 | COVERED | Source freeze, exact subtree commit, final proof, and additive fork publication are explicit. |
| REQ | QUICK-260729-9SR | Preserve the clean planning history, exact source exclusion, recovery state, and fork-only continuation boundary | 1–3 | COVERED | This quick task is Git/planning maintenance rather than a ROADMAP feature requirement. |
| RESEARCH | — | Level 0: established Git worktree, backup-ref, subtree-restore, tree-object, diff, and normal-push mechanics; no library or external API choice | 1–3 | COVERED | No discovery artifact or package install is needed. |
| CONTEXT | D-01 | Hold the cleanup branch at `3866fbc` | 1, 3 | COVERED | Direct fork OID checks occur before and after extraction. |
| CONTEXT | D-02 | Create and publish a clean planning-only branch without publishing mixed history | 1–3 | COVERED | New baseline worktree, exact planning subtree proof, scope audit, and named origin push enforce it. |
| CONTEXT | topology/constraints | Preserve origin main, mixed remote, PR #217, Phase 103, prior backup refs, and all worktrees | 1, 3 | COVERED | Captured ref/worktree manifests are revalidated before the sole push. |
| CONTEXT | continuation boundary | Place summary/State on the new branch, retain cleanup-auth blocker, avoid upstream branch/PR work and merging | 3 | COVERED | Final self-delta proof and explicit State/Summary record keep the next actions separate. |
</verification>

<success_criteria>
1. `origin/test/runtime-registry-assertions` remains `3866fbc041463a6de0c82bd325256a9e5ede189b` and every protected topic ref/worktree has its captured identity after publication.
2. `docs/fork-v1.29-paja-social-blossom-planning-only` begins at local main `15822032bc5eb853a9d1f6c6501012c7481e875d`; its initial extraction commit has a `.planning` tree exactly equal to the captured mixed source tip.
3. The new branch has no baseline diff path outside `.planning/`, including no implementation, test, package, changeset, published-document, or Graphify path.
4. The final docs record lives only on the new planning branch, and its measured source-tree delta is limited to the quick summary and State record.
5. Fork origin receives only an additive, non-force `docs/fork-v1.29-paja-social-blossom-planning-only` branch update; no upstream remote operation, origin-main merge, cleanup PR creation, or stack continuation is performed.
</success_criteria>

<output>
Create `.planning/quick/260729-9sr-reorganize-fork-owned-work-coherently-ke/260729-9sr-SUMMARY.md` on `docs/fork-v1.29-paja-social-blossom-planning-only` when done.
</output>
