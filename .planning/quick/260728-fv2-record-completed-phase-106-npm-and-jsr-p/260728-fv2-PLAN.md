---
quick_id: 260728-fv2
slug: record-completed-phase-106-npm-and-jsr-p
date: 2026-07-28
branch: chore/phase-106-publication-closeout
---

# Quick: Record completed Phase 106 publication and downstream proof

## Goal

Correct the Phase 106 closeout so it preserves the original PR-readiness
boundary as historical evidence while separately recording the completed
post-merge release. Completion must be supported by the exact `main` SHA,
successful CI and Pages runs, the successful npm/JSR release workflow, direct
registry metadata, and a clean downstream Paja install/import/build smoke test.

## Tasks

- [x] Add a post-merge publication closure section to the Phase 106 release
      checklist and verification report. Preserve the earlier 13/13
      PR-readiness verdict as historical scope rather than rewriting it as
      registry-publication proof.
- [x] Replace stale current-state claims in `STATE.md`, `PROJECT.md`, and
      `ROADMAP.md` with the merged and published state. Preserve the Phase 105
      12/24 UI audit as explicit non-passing debt.
- [x] Record the exact seven npm/JSR versions, Napplet 0.29 compatibility,
      release run, direct registry checks, and clean consumer proof.
- [x] Verify the changed planning artifacts against live GitHub/npm/JSR state,
      run `git diff --check`, and commit atomically. Push and open the PR as the
      repository SDLC handoff after the task artifacts are complete.

## Authoritative evidence

- Release source: `main` at
  `54ef2ead03ee0c37783727468b8658b6dc224137`.
- Main CI: GitHub Actions run `30299386244`, successful.
- Pages build/deploy: GitHub Actions run `30299386308`, successful.
- Release: GitHub Actions run `30350331202`, successful, with both the npm
  OIDC and topologically ordered JSR publish steps successful.
- Direct registry versions:
  `@kehto/acl@0.16.0`, `@kehto/cli@0.3.0`,
  `@kehto/firewall@0.4.0`, `@kehto/paja@0.9.0`,
  `@kehto/runtime@0.19.0`, `@kehto/services@0.17.0`, and
  `@kehto/shell@0.18.0`.
- Clean consumer proof: a fresh npm project resolves
  `@kehto/paja@0.9.0`, `@napplet/core@0.29.0`, and
  `@napplet/nap@0.29.0`; ESM import and Node-platform esbuild bundle pass.

## Verification

- Re-query all seven npm `latest` tags and peer dependency metadata.
- Re-query all seven JSR `latest` versions and Paja's published `jsr.json`.
- Confirm release run `30350331202`, CI run `30299386244`, and Pages run
  `30299386308` remain successful on the exact source SHA.
- Confirm no active planning artifact still claims PR #204 is open or that
  publication remains unexecuted.
- Run `git diff --check`.

## NAP conformance

No NAP or NIP-5D behavior changes. This task records release and registry
evidence for already-verified Phase 106 behavior; the previously checked
`napplet/naps` authority refs remain unchanged.
